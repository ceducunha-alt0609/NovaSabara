===== index.html =====
LINES 7456 SIZE 424603
localStorage: 5
sessionStorage: 0
indexedDB: 0
supabase: 0
firebase: 0
seed: 3
demo: 0
backup: 24
restore: 0
import: 415
export: 21
reset: 22
clear(): 0
removeItem: 1
setItem: 2
toISOString().split: 0
new Date(: 6
mensal: 154
pag: 38
aluno: 74
turma: 140
valor: 29
desconto: 28
multa: 0
juros: 0
venc: 0
serviceWorker.register: 1
caches.delete: 0
location.reload: 0

===== sw.js =====
LINES 44 SIZE 1161
localStorage: 0
sessionStorage: 0
indexedDB: 0
supabase: 0
firebase: 0
seed: 0
demo: 0
backup: 0
restore: 0
import: 0
export: 0
reset: 0
clear(): 0
removeItem: 0
setItem: 0
toISOString().split: 0
new Date(: 0
mensal: 0
pag: 0
aluno: 0
turma: 0
valor: 0
desconto: 0
multa: 0
juros: 0
venc: 0
serviceWorker.register: 0
caches.delete: 1
location.reload: 0

===== service-worker.js =====
LINES 46 SIZE 1098
localStorage: 0
sessionStorage: 0
indexedDB: 0
supabase: 0
firebase: 0
seed: 0
demo: 0
backup: 0
restore: 0
import: 0
export: 0
reset: 0
clear(): 0
removeItem: 0
setItem: 0
toISOString().split: 0
new Date(: 0
mensal: 1
pag: 0
aluno: 0
turma: 0
valor: 0
desconto: 0
multa: 0
juros: 0
venc: 0
serviceWorker.register: 0
caches.delete: 1
location.reload: 0

===== manifest.json =====
LINES 71 SIZE 1490
localStorage: 0
sessionStorage: 0
indexedDB: 0
supabase: 0
firebase: 0
seed: 0
demo: 0
backup: 0
restore: 0
import: 0
export: 0
reset: 0
clear(): 0
removeItem: 0
setItem: 0
toISOString().split: 0
new Date(: 0
mensal: 2
pag: 0
aluno: 0
turma: 0
valor: 0
desconto: 0
multa: 0
juros: 0
venc: 0
serviceWorker.register: 0
caches.delete: 0
location.reload: 0


### localStorage
l.checked = !!value;
      });

      if(state.selectedTurma && commercialTable[state.selectedTurma]) selectedTurma = state.selectedTurma;
      if(state.selectedHour) selectedHour = Number(state.selectedHour);

      isRestoringState = false;
      updateAll();
    }

    function saveState(){
      if(isRestoringState) return;
      try{
        localStorage.setItem('novaSabaraCalculadoraState', JSON.stringify(collectState()));
        const status = document.getElementById('saveStatus');
        if(status) status.textContent = `Salvo automaticamente às ${new Date().toLocaleTimeString('pt-BR', {hour:'2-digit', minute:'2-digit'})}`;
      }catch(e){
        console.warn('Não foi possível salvar:', e);
      }
    }

    function scheduleSave(){
      clearTimeout(saveTimer);
      saveTimer = setTimeout(saveState, 250);
    }

    function loadState(){
      try{
        const raw = localStorage.getItem('novaSabaraCalculadoraState');
        if(raw) applyState(JSON.parse(raw));
      }catch(e){
        console.warn('Não foi possível carregar dados salvos:', e);
      }
    }

    function exportBackup(){
      const blob = new Blob([JSON.stringify(collectState(), null, 2)], {type:'application/json'});
      const url = URL.create
---
maticamente às ${new Date().toLocaleTimeString('pt-BR', {hour:'2-digit', minute:'2-digit'})}`;
      }catch(e){
        console.warn('Não foi possível salvar:', e);
      }
    }

    function scheduleSave(){
      clearTimeout(saveTimer);
      saveTimer = setTimeout(saveState, 250);
    }

    function loadState(){
      try{
        const raw = localStorage.getItem('novaSabaraCalculadoraState');
        if(raw) applyState(JSON.parse(raw));
      }catch(e){
        console.warn('Não foi possível carregar dados salvos:', e);
      }
    }

    function exportBackup(){
      const blob = new Blob([JSON.stringify(collectState(), null, 2)], {type:'application/json'});
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url;
      a.download = `backup-calculadora-nova-sabara-${new Date().toISOString().slice(0,10)}.json`;
      document.body.appendChild(a);
      a.click();
      a.remove();
      URL.revokeObjectURL(url);
      showToast('Backup exportado!');
    }

    function importBackup(file){
      if(!file) return;
      const reader = new FileReader();
      reader.onload = () => {
        try{
          const state = JSON.parse(reader.result);
          applyState(state);

---
e);
          saveState();
          showToast('Backup importado!');
        }catch(e){
          alert('Não foi possível importar este backup. Verifique se o arquivo é JSON válido.');
        }
      };
      reader.readAsText(file);
    }

    function clearSavedData(){
      if(confirm('Deseja limpar os dados salvos neste navegador?')){
        localStorage.removeItem('novaSabaraCalculadoraState');
        showToast('Dados locais limpos!');
      }
    }

    function buildSmartAlerts(){
      const alerts = [];
      const calc = getCommercialCalc();
      const form = getFormationCalc();
      let precision = null;
      try{ precision = getPrecisionData(); }catch(e){}

      if(calc.mensalidade < form.minima){
        alerts.push({type:'danger', title:'Mensalidade abaixo do mínimo', text:'A simulação atual está abaixo do custo mínimo. Evite aprovar esta proposta sem ajuste.'});
      }else if(calc.mensalidade < form.segura){
        alerts.push({type:'warn', title:'Mensalidade abaixo da segura', text:'A proposta cobre o mínimo, mas não protege suficientemente contra inadimplência e reserva.'});
      }else{
        alerts.push({type:'ok', title:'Mensalidade comercial saudável', text:'A simulação atual está acima da referênci
---
t = `IPCA de ${pct}% ao ano → reajuste de referência para comparativo`;
    }
    const ipcaEl2 = document.getElementById('ipcaRate');
    if(ipcaEl2){ ipcaEl2.addEventListener('input', updateIPCABanner); }
    updateIPCABanner();

    // Histórico de Reajustes
    const HIST_KEY = 'novaSabaraHistorico_v2';
    function loadHistory(){ try{ const r=localStorage.getItem(HIST_KEY); return r?JSON.parse(r):[]; }catch(e){ return []; } }
    function saveHistory(h){ try{ localStorage.setItem(HIST_KEY,JSON.stringify(h)); }catch(e){} }

    function renderHistory(){
      const tbody = document.getElementById('historyTableBody');
      const wrap = document.getElementById('historyTableWrap');
      const empty = document.getElementById('historyEmpty');
      if(!tbody) return;
      const history = loadHistory();
      if(history.length === 0){
        if(wrap) wrap.style.display = 'none';
        if(empty) empty.style.display = '';
        return;
      }
      if(wrap) wrap.style.display = '';
      if(empty) empty.style.display = 'none';
      const money3 = new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'});
      tbody.innerHTML = [...history].reverse().map(e => `
        <tr>
          <td>${e.date}</td>
          <td>$
---
Id('ipcaRate');
    if(ipcaEl2){ ipcaEl2.addEventListener('input', updateIPCABanner); }
    updateIPCABanner();

    // Histórico de Reajustes
    const HIST_KEY = 'novaSabaraHistorico_v2';
    function loadHistory(){ try{ const r=localStorage.getItem(HIST_KEY); return r?JSON.parse(r):[]; }catch(e){ return []; } }
    function saveHistory(h){ try{ localStorage.setItem(HIST_KEY,JSON.stringify(h)); }catch(e){} }

    function renderHistory(){
      const tbody = document.getElementById('historyTableBody');
      const wrap = document.getElementById('historyTableWrap');
      const empty = document.getElementById('historyEmpty');
      if(!tbody) return;
      const history = loadHistory();
      if(history.length === 0){
        if(wrap) wrap.style.display = 'none';
        if(empty) empty.style.display = '';
        return;
      }
      if(wrap) wrap.style.display = '';
      if(empty) empty.style.display = 'none';
      const money3 = new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'});
      tbody.innerHTML = [...history].reverse().map(e => `
        <tr>
          <td>${e.date}</td>
          <td>${e.turma} · ${e.hour}h</td>
          <td>${money3.format(e.currentPrice)}</td>
          <td>${money3.format(e.recomme
---

### seed

---

---

---

### backup
sentação</strong>
              <small>Tela limpa para reunião</small>
            </span>
          </button>

          <button class="side-link" data-view="configuracoes" aria-label="Ir para Configurações">
            <span class="side-emoji">⚙️</span>
            <span>
              <strong>Configurações</strong>
              <small>Salvar, backup e app</small>
            </span>
          </button>

          <button class="side-link" data-view="relatorios" aria-label="Ir para Relatórios">
            <span class="side-emoji">🖨️</span>
            <span>
              <strong>Relatórios</strong>
              <small>Impressão e orçamentos</small>
            </span>
          </button>
        
          <button class="side-link" data-view="historico" aria-label="Ir para Histórico de Reajustes">
            <span class="side-emoji">📅</span>
            <span>
              <strong>Histórico</strong>
              <small>Reajustes e evolução</small>
            </span>
          </button>

        </nav>

        <div class="sidebar-box">
          <small>Atalhos</small>
          <button class="sidebar-mini-btn" id="goBudgetBtn">Ir para orçamento</button>
          <button class="sidebar-mini-btn" id="goPrintBtn">Ir para 
---
     <p>Os dados são salvos automaticamente neste navegador para evitar retrabalho.</p>
              <div class="settings-status" id="saveStatus">Aguardando alterações...</div>
              <button class="btn secondary" id="clearStorageBtn">Limpar dados salvos</button>
            </div>

            <div class="settings-card">
              <h3>Backup</h3>
              <p>Exporte um arquivo JSON com todos os campos da calculadora e restaure quando precisar.</p>
              <div class="actions">
                <button class="btn" id="exportBackupBtn">Exportar backup</button>
                <label class="import-label">
                  Importar backup
                  <input id="importBackupInput" type="file" accept=".json,application/json">
                </label>
              </div>
            </div>

            <div class="settings-card">
              <h3>Instalar como app</h3>
              <p>Esta versão está preparada para virar PWA. Ao finalizar o design, gere o pacote com manifest, service worker e ícones.</p>
              <div class="app-checklist">
                <span>✅ HTML único funcional</span>
                <span>✅ Dados locais</span>
                <span>✅ Backup/importação</span>
                
---
ass="btn secondary" id="clearStorageBtn">Limpar dados salvos</button>
            </div>

            <div class="settings-card">
              <h3>Backup</h3>
              <p>Exporte um arquivo JSON com todos os campos da calculadora e restaure quando precisar.</p>
              <div class="actions">
                <button class="btn" id="exportBackupBtn">Exportar backup</button>
                <label class="import-label">
                  Importar backup
                  <input id="importBackupInput" type="file" accept=".json,application/json">
                </label>
              </div>
            </div>

            <div class="settings-card">
              <h3>Instalar como app</h3>
              <p>Esta versão está preparada para virar PWA. Ao finalizar o design, gere o pacote com manifest, service worker e ícones.</p>
              <div class="app-checklist">
                <span>✅ HTML único funcional</span>
                <span>✅ Dados locais</span>
                <span>✅ Backup/importação</span>
                <span>⏳ Manifest + Service Worker no pacote final</span>
              </div>
            </div>

            <div class="settings-card">
              <h3>Proposta para família</h3>
              <p>Us
---
id="clearStorageBtn">Limpar dados salvos</button>
            </div>

            <div class="settings-card">
              <h3>Backup</h3>
              <p>Exporte um arquivo JSON com todos os campos da calculadora e restaure quando precisar.</p>
              <div class="actions">
                <button class="btn" id="exportBackupBtn">Exportar backup</button>
                <label class="import-label">
                  Importar backup
                  <input id="importBackupInput" type="file" accept=".json,application/json">
                </label>
              </div>
            </div>

            <div class="settings-card">
              <h3>Instalar como app</h3>
              <p>Esta versão está preparada para virar PWA. Ao finalizar o design, gere o pacote com manifest, service worker e ícones.</p>
              <div class="app-checklist">
                <span>✅ HTML único funcional</span>
                <span>✅ Dados locais</span>
                <span>✅ Backup/importação</span>
                <span>⏳ Manifest + Service Worker no pacote final</span>
              </div>
            </div>

            <div class="settings-card">
              <h3>Proposta para família</h3>
              <p>Use o modo apresentaçã
---
lass="settings-card">
              <h3>Backup</h3>
              <p>Exporte um arquivo JSON com todos os campos da calculadora e restaure quando precisar.</p>
              <div class="actions">
                <button class="btn" id="exportBackupBtn">Exportar backup</button>
                <label class="import-label">
                  Importar backup
                  <input id="importBackupInput" type="file" accept=".json,application/json">
                </label>
              </div>
            </div>

            <div class="settings-card">
              <h3>Instalar como app</h3>
              <p>Esta versão está preparada para virar PWA. Ao finalizar o design, gere o pacote com manifest, service worker e ícones.</p>
              <div class="app-checklist">
                <span>✅ HTML único funcional</span>
                <span>✅ Dados locais</span>
                <span>✅ Backup/importação</span>
                <span>⏳ Manifest + Service Worker no pacote final</span>
              </div>
            </div>

            <div class="settings-card">
              <h3>Proposta para família</h3>
              <p>Use o modo apresentação e os relatórios para atendimento. O orçamento inclui mensalidade, alimentação, ativida
---
ckup</h3>
              <p>Exporte um arquivo JSON com todos os campos da calculadora e restaure quando precisar.</p>
              <div class="actions">
                <button class="btn" id="exportBackupBtn">Exportar backup</button>
                <label class="import-label">
                  Importar backup
                  <input id="importBackupInput" type="file" accept=".json,application/json">
                </label>
              </div>
            </div>

            <div class="settings-card">
              <h3>Instalar como app</h3>
              <p>Esta versão está preparada para virar PWA. Ao finalizar o design, gere o pacote com manifest, service worker e ícones.</p>
              <div class="app-checklist">
                <span>✅ HTML único funcional</span>
                <span>✅ Dados locais</span>
                <span>✅ Backup/importação</span>
                <span>⏳ Manifest + Service Worker no pacote final</span>
              </div>
            </div>

            <div class="settings-card">
              <h3>Proposta para família</h3>
              <p>Use o modo apresentação e os relatórios para atendimento. O orçamento inclui mensalidade, alimentação, atividades, matrícula e material inicial.</p>
   
---
card">
              <h3>Instalar como app</h3>
              <p>Esta versão está preparada para virar PWA. Ao finalizar o design, gere o pacote com manifest, service worker e ícones.</p>
              <div class="app-checklist">
                <span>✅ HTML único funcional</span>
                <span>✅ Dados locais</span>
                <span>✅ Backup/importação</span>
                <span>⏳ Manifest + Service Worker no pacote final</span>
              </div>
            </div>

            <div class="settings-card">
              <h3>Proposta para família</h3>
              <p>Use o modo apresentação e os relatórios para atendimento. O orçamento inclui mensalidade, alimentação, atividades, matrícula e material inicial.</p>
              <button class="btn secondary" id="settingsGoPresentation">Abrir modo apresentação</button>
            </div>
          </div>
        </div>

        <aside class="panel result-card">
          <div class="section-title">
            <h2>Status do sistema</h2>
            <span class="pill">Resumo</span>
          </div>
          <div class="summary" id="settingsSummary"></div>
          <div class="insight">
            <strong>Recomendação:</strong><br>
            Depois de validar os v
---
result-card">
          <div class="section-title">
            <h2>Status do sistema</h2>
            <span class="pill">Resumo</span>
          </div>
          <div class="summary" id="settingsSummary"></div>
          <div class="insight">
            <strong>Recomendação:</strong><br>
            Depois de validar os valores reais, exporte um backup e guarde como referência oficial da tabela da escola.
          </div>
        </aside>
      </div>
    </section>

    <section id="relatorios" class="view">
      <div class="panel">
        <div class="section-title">
          <h2>Relatórios e orçamentos para impressão</h2>
          <span class="pill">PDF / Impressora</span>
        </div>

        <div class="report-toolbar">
          <button class="btn" id="printAllBtn">Imprimir relatório completo</button>
          <button class="btn secondary" id="printBudgetBtn">Imprimir orçamento da simulação atual</button>
          <button class="btn secondary" id="copyBudgetBtn">Copiar orçamento</button>
        </div>

        <div class="fields no-print" style="margin-bottom:18px">
          <div class="field">
            <label>Nome da escola</label>
            <input id="schoolName" type="text" value="Escola de Educação Infan
---
learTimeout(saveTimer);
      saveTimer = setTimeout(saveState, 250);
    }

    function loadState(){
      try{
        const raw = localStorage.getItem('novaSabaraCalculadoraState');
        if(raw) applyState(JSON.parse(raw));
      }catch(e){
        console.warn('Não foi possível carregar dados salvos:', e);
      }
    }

    function exportBackup(){
      const blob = new Blob([JSON.stringify(collectState(), null, 2)], {type:'application/json'});
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url;
      a.download = `backup-calculadora-nova-sabara-${new Date().toISOString().slice(0,10)}.json`;
      document.body.appendChild(a);
      a.click();
      a.remove();
      URL.revokeObjectURL(url);
      showToast('Backup exportado!');
    }

    function importBackup(file){
      if(!file) return;
      const reader = new FileReader();
      reader.onload = () => {
        try{
          const state = JSON.parse(reader.result);
          applyState(state);
          saveState();
          showToast('Backup importado!');
        }catch(e){
          alert('Não foi possível importar este backup. Verifique se o arquivo é JSON válido.');
        }
      };
      reader.rea
---
atch(e){
        console.warn('Não foi possível carregar dados salvos:', e);
      }
    }

    function exportBackup(){
      const blob = new Blob([JSON.stringify(collectState(), null, 2)], {type:'application/json'});
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url;
      a.download = `backup-calculadora-nova-sabara-${new Date().toISOString().slice(0,10)}.json`;
      document.body.appendChild(a);
      a.click();
      a.remove();
      URL.revokeObjectURL(url);
      showToast('Backup exportado!');
    }

    function importBackup(file){
      if(!file) return;
      const reader = new FileReader();
      reader.onload = () => {
        try{
          const state = JSON.parse(reader.result);
          applyState(state);
          saveState();
          showToast('Backup importado!');
        }catch(e){
          alert('Não foi possível importar este backup. Verifique se o arquivo é JSON válido.');
        }
      };
      reader.readAsText(file);
    }

    function clearSavedData(){
      if(confirm('Deseja limpar os dados salvos neste navegador?')){
        localStorage.removeItem('novaSabaraCalculadoraState');
        showToast('Dados locais limpos!');
      }
   
---
'application/json'});
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url;
      a.download = `backup-calculadora-nova-sabara-${new Date().toISOString().slice(0,10)}.json`;
      document.body.appendChild(a);
      a.click();
      a.remove();
      URL.revokeObjectURL(url);
      showToast('Backup exportado!');
    }

    function importBackup(file){
      if(!file) return;
      const reader = new FileReader();
      reader.onload = () => {
        try{
          const state = JSON.parse(reader.result);
          applyState(state);
          saveState();
          showToast('Backup importado!');
        }catch(e){
          alert('Não foi possível importar este backup. Verifique se o arquivo é JSON válido.');
        }
      };
      reader.readAsText(file);
    }

    function clearSavedData(){
      if(confirm('Deseja limpar os dados salvos neste navegador?')){
        localStorage.removeItem('novaSabaraCalculadoraState');
        showToast('Dados locais limpos!');
      }
    }

    function buildSmartAlerts(){
      const alerts = [];
      const calc = getCommercialCalc();
      const form = getFormationCalc();
      let precision = null;
      try{ precision = getPre
---
ateObjectURL(blob);
      const a = document.createElement('a');
      a.href = url;
      a.download = `backup-calculadora-nova-sabara-${new Date().toISOString().slice(0,10)}.json`;
      document.body.appendChild(a);
      a.click();
      a.remove();
      URL.revokeObjectURL(url);
      showToast('Backup exportado!');
    }

    function importBackup(file){
      if(!file) return;
      const reader = new FileReader();
      reader.onload = () => {
        try{
          const state = JSON.parse(reader.result);
          applyState(state);
          saveState();
          showToast('Backup importado!');
        }catch(e){
          alert('Não foi possível importar este backup. Verifique se o arquivo é JSON válido.');
        }
      };
      reader.readAsText(file);
    }

    function clearSavedData(){
      if(confirm('Deseja limpar os dados salvos neste navegador?')){
        localStorage.removeItem('novaSabaraCalculadoraState');
        showToast('Dados locais limpos!');
      }
    }

    function buildSmartAlerts(){
      const alerts = [];
      const calc = getCommercialCalc();
      const form = getFormationCalc();
      let precision = null;
      try{ precision = getPrecisionData(); }catch(e){}

      if(calc.mensal
---
remove();
      URL.revokeObjectURL(url);
      showToast('Backup exportado!');
    }

    function importBackup(file){
      if(!file) return;
      const reader = new FileReader();
      reader.onload = () => {
        try{
          const state = JSON.parse(reader.result);
          applyState(state);
          saveState();
          showToast('Backup importado!');
        }catch(e){
          alert('Não foi possível importar este backup. Verifique se o arquivo é JSON válido.');
        }
      };
      reader.readAsText(file);
    }

    function clearSavedData(){
      if(confirm('Deseja limpar os dados salvos neste navegador?')){
        localStorage.removeItem('novaSabaraCalculadoraState');
        showToast('Dados locais limpos!');
      }
    }

    function buildSmartAlerts(){
      const alerts = [];
      const calc = getCommercialCalc();
      const form = getFormationCalc();
      let precision = null;
      try{ precision = getPrecisionData(); }catch(e){}

      if(calc.mensalidade < form.minima){
        alerts.push({type:'danger', title:'Mensalidade abaixo do mínimo', text:'A simulação atual está abaixo do custo mínimo. Evite aprovar esta proposta sem ajuste.'});
      }else if(calc.mensalidade < form.segura){
   
---
   function importBackup(file){
      if(!file) return;
      const reader = new FileReader();
      reader.onload = () => {
        try{
          const state = JSON.parse(reader.result);
          applyState(state);
          saveState();
          showToast('Backup importado!');
        }catch(e){
          alert('Não foi possível importar este backup. Verifique se o arquivo é JSON válido.');
        }
      };
      reader.readAsText(file);
    }

    function clearSavedData(){
      if(confirm('Deseja limpar os dados salvos neste navegador?')){
        localStorage.removeItem('novaSabaraCalculadoraState');
        showToast('Dados locais limpos!');
      }
    }

    function buildSmartAlerts(){
      const alerts = [];
      const calc = getCommercialCalc();
      const form = getFormationCalc();
      let precision = null;
      try{ precision = getPrecisionData(); }catch(e){}

      if(calc.mensalidade < form.minima){
        alerts.push({type:'danger', title:'Mensalidade abaixo do mínimo', text:'A simulação atual está abaixo do custo mínimo. Evite aprovar esta proposta sem ajuste.'});
      }else if(calc.mensalidade < form.segura){
        alerts.push({type:'warn', title:'Mensalidade abaixo da segura', text:'A proposta co
---
ent.getElementById('resetPrecisionBtn');
    if(resetPrecisionBtn){
      resetPrecisionBtn.addEventListener('click', resetPrecision);
    }

    const addCustomActivityBtn = document.getElementById('addCustomActivityBtn');
    if(addCustomActivityBtn){
      addCustomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getElementById('importBackupInput');
    if(importBackupInput){
      importBackupInput.addEventListener('change', e => importBackup(e.target.files[0]));
    }

    const clearStorageBtn = document.getElementById('clearStorageBtn');
    if(clearStorageBtn){
      clearStorageBtn.addEventListener('click', clearSavedData);
    }

    const copyAlertsBtn = document.getElementById('copyAlertsBtn');
    if(copyAlertsBtn){
      copyAlertsBtn.addEventListener('click', copyAlerts);
    }

    const goPrecisionFromAlerts = document.getElementById('goPrecisionFromAlerts');
    if(goPrecisionFromAlerts){
      goPrecisionFromAlerts.addEventListener('click', () => {
        setActiveView('precisao');
        upd
---
  if(resetPrecisionBtn){
      resetPrecisionBtn.addEventListener('click', resetPrecision);
    }

    const addCustomActivityBtn = document.getElementById('addCustomActivityBtn');
    if(addCustomActivityBtn){
      addCustomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getElementById('importBackupInput');
    if(importBackupInput){
      importBackupInput.addEventListener('change', e => importBackup(e.target.files[0]));
    }

    const clearStorageBtn = document.getElementById('clearStorageBtn');
    if(clearStorageBtn){
      clearStorageBtn.addEventListener('click', clearSavedData);
    }

    const copyAlertsBtn = document.getElementById('copyAlertsBtn');
    if(copyAlertsBtn){
      copyAlertsBtn.addEventListener('click', copyAlerts);
    }

    const goPrecisionFromAlerts = document.getElementById('goPrecisionFromAlerts');
    if(goPrecisionFromAlerts){
      goPrecisionFromAlerts.addEventListener('click', () => {
        setActiveView('precisao');
        updateAll();
      });
    }

    const presen
---
     resetPrecisionBtn.addEventListener('click', resetPrecision);
    }

    const addCustomActivityBtn = document.getElementById('addCustomActivityBtn');
    if(addCustomActivityBtn){
      addCustomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getElementById('importBackupInput');
    if(importBackupInput){
      importBackupInput.addEventListener('change', e => importBackup(e.target.files[0]));
    }

    const clearStorageBtn = document.getElementById('clearStorageBtn');
    if(clearStorageBtn){
      clearStorageBtn.addEventListener('click', clearSavedData);
    }

    const copyAlertsBtn = document.getElementById('copyAlertsBtn');
    if(copyAlertsBtn){
      copyAlertsBtn.addEventListener('click', copyAlerts);
    }

    const goPrecisionFromAlerts = document.getElementById('goPrecisionFromAlerts');
    if(goPrecisionFromAlerts){
      goPrecisionFromAlerts.addEventListener('click', () => {
        setActiveView('precisao');
        updateAll();
      });
    }

    const presentationToBudget = document.
---
ddEventListener('click', resetPrecision);
    }

    const addCustomActivityBtn = document.getElementById('addCustomActivityBtn');
    if(addCustomActivityBtn){
      addCustomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getElementById('importBackupInput');
    if(importBackupInput){
      importBackupInput.addEventListener('change', e => importBackup(e.target.files[0]));
    }

    const clearStorageBtn = document.getElementById('clearStorageBtn');
    if(clearStorageBtn){
      clearStorageBtn.addEventListener('click', clearSavedData);
    }

    const copyAlertsBtn = document.getElementById('copyAlertsBtn');
    if(copyAlertsBtn){
      copyAlertsBtn.addEventListener('click', copyAlerts);
    }

    const goPrecisionFromAlerts = document.getElementById('goPrecisionFromAlerts');
    if(goPrecisionFromAlerts){
      goPrecisionFromAlerts.addEventListener('click', () => {
        setActiveView('precisao');
        updateAll();
      });
    }

    const presentationToBudget = document.getElementById('presenta
---
    }

    const addCustomActivityBtn = document.getElementById('addCustomActivityBtn');
    if(addCustomActivityBtn){
      addCustomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getElementById('importBackupInput');
    if(importBackupInput){
      importBackupInput.addEventListener('change', e => importBackup(e.target.files[0]));
    }

    const clearStorageBtn = document.getElementById('clearStorageBtn');
    if(clearStorageBtn){
      clearStorageBtn.addEventListener('click', clearSavedData);
    }

    const copyAlertsBtn = document.getElementById('copyAlertsBtn');
    if(copyAlertsBtn){
      copyAlertsBtn.addEventListener('click', copyAlerts);
    }

    const goPrecisionFromAlerts = document.getElementById('goPrecisionFromAlerts');
    if(goPrecisionFromAlerts){
      goPrecisionFromAlerts.addEventListener('click', () => {
        setActiveView('precisao');
        updateAll();
      });
    }

    const presentationToBudget = document.getElementById('presentationToBudget');
    if(presentationToBudge
---
tyBtn = document.getElementById('addCustomActivityBtn');
    if(addCustomActivityBtn){
      addCustomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getElementById('importBackupInput');
    if(importBackupInput){
      importBackupInput.addEventListener('change', e => importBackup(e.target.files[0]));
    }

    const clearStorageBtn = document.getElementById('clearStorageBtn');
    if(clearStorageBtn){
      clearStorageBtn.addEventListener('click', clearSavedData);
    }

    const copyAlertsBtn = document.getElementById('copyAlertsBtn');
    if(copyAlertsBtn){
      copyAlertsBtn.addEventListener('click', copyAlerts);
    }

    const goPrecisionFromAlerts = document.getElementById('goPrecisionFromAlerts');
    if(goPrecisionFromAlerts){
      goPrecisionFromAlerts.addEventListener('click', () => {
        setActiveView('precisao');
        updateAll();
      });
    }

    const presentationToBudget = document.getElementById('presentationToBudget');
    if(presentationToBudget){
      presentationToBudget.a
---
ivityBtn');
    if(addCustomActivityBtn){
      addCustomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getElementById('importBackupInput');
    if(importBackupInput){
      importBackupInput.addEventListener('change', e => importBackup(e.target.files[0]));
    }

    const clearStorageBtn = document.getElementById('clearStorageBtn');
    if(clearStorageBtn){
      clearStorageBtn.addEventListener('click', clearSavedData);
    }

    const copyAlertsBtn = document.getElementById('copyAlertsBtn');
    if(copyAlertsBtn){
      copyAlertsBtn.addEventListener('click', copyAlerts);
    }

    const goPrecisionFromAlerts = document.getElementById('goPrecisionFromAlerts');
    if(goPrecisionFromAlerts){
      goPrecisionFromAlerts.addEventListener('click', () => {
        setActiveView('precisao');
        updateAll();
      });
    }

    const presentationToBudget = document.getElementById('presentationToBudget');
    if(presentationToBudget){
      presentationToBudget.addEventListener('click', () => {
        setA
---
ActivityBtn){
      addCustomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getElementById('importBackupInput');
    if(importBackupInput){
      importBackupInput.addEventListener('change', e => importBackup(e.target.files[0]));
    }

    const clearStorageBtn = document.getElementById('clearStorageBtn');
    if(clearStorageBtn){
      clearStorageBtn.addEventListener('click', clearSavedData);
    }

    const copyAlertsBtn = document.getElementById('copyAlertsBtn');
    if(copyAlertsBtn){
      copyAlertsBtn.addEventListener('click', copyAlerts);
    }

    const goPrecisionFromAlerts = document.getElementById('goPrecisionFromAlerts');
    if(goPrecisionFromAlerts){
      goPrecisionFromAlerts.addEventListener('click', () => {
        setActiveView('precisao');
        updateAll();
      });
    }

    const presentationToBudget = document.getElementById('presentationToBudget');
    if(presentationToBudget){
      presentationToBudget.addEventListener('click', () => {
        setActiveView('relatorios');
   
---
tomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getElementById('importBackupInput');
    if(importBackupInput){
      importBackupInput.addEventListener('change', e => importBackup(e.target.files[0]));
    }

    const clearStorageBtn = document.getElementById('clearStorageBtn');
    if(clearStorageBtn){
      clearStorageBtn.addEventListener('click', clearSavedData);
    }

    const copyAlertsBtn = document.getElementById('copyAlertsBtn');
    if(copyAlertsBtn){
      copyAlertsBtn.addEventListener('click', copyAlerts);
    }

    const goPrecisionFromAlerts = document.getElementById('goPrecisionFromAlerts');
    if(goPrecisionFromAlerts){
      goPrecisionFromAlerts.addEventListener('click', () => {
        setActiveView('precisao');
        updateAll();
      });
    }

    const presentationToBudget = document.getElementById('presentationToBudget');
    if(presentationToBudget){
      presentationToBudget.addEventListener('click', () => {
        setActiveView('relatorios');
        updateAll();
      })
---
Activity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getElementById('importBackupInput');
    if(importBackupInput){
      importBackupInput.addEventListener('change', e => importBackup(e.target.files[0]));
    }

    const clearStorageBtn = document.getElementById('clearStorageBtn');
    if(clearStorageBtn){
      clearStorageBtn.addEventListener('click', clearSavedData);
    }

    const copyAlertsBtn = document.getElementById('copyAlertsBtn');
    if(copyAlertsBtn){
      copyAlertsBtn.addEventListener('click', copyAlerts);
    }

    const goPrecisionFromAlerts = document.getElementById('goPrecisionFromAlerts');
    if(goPrecisionFromAlerts){
      goPrecisionFromAlerts.addEventListener('click', () => {
        setActiveView('precisao');
        updateAll();
      });
    }

    const presentationToBudget = document.getElementById('presentationToBudget');
    if(presentationToBudget){
      presentationToBudget.addEventListener('click', () => {
        setActiveView('relatorios');
        updateAll();
      });
    }

    const presentationToCompare = documen
---

### restore

### import
html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="theme-color" content="#0757a8">
  <title>Nova Sabará Mensalidades</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Merriweather:wght@300;400;700;900&display=swap');
    :root{
      --bg:#eef8ff;
      --bg2:#fffdf6;
      --card:rgba(255,255,255,.72);
      --card2:rgba(255,255,255,.88);
      --text:#123047;
      --muted:#667789;
      --blue:#0757a8;
      --blue2:#58c3de;
      --green:#18a058;
      --red:#df3b3b;
      --amber:#ffc20a;
      --violet:#0b69bd;
      --border:rgba(7,87,168,.18);
      --shadow:0 20px 50px rgba(7,87,168,.13);
      --radius:18px;
    }

    *{
      box-sizing:border-box;
      font-family:"Merriweather", Georgia, serif;
    }

    button,
    input,
    select,
    textarea{
      font-family:"Merriweather", Georgia, serif;
    }

    body{
      margin:0;
      min-height:100vh;
      font-family: "Merriweather", Georgia, serif;
      color:var(--text);
      background:
        radial-gradient(circle at 10% 
---
   font-weight:900;
    }

    .line,
    .field input,
    .field select,
    .btn,
    .tab,
    .pill,
    .card-option,
    .hour-btn{
      font-size-adjust:.52;
    }

    .field input,
    .field select{
      line-height:1.35;
    }

    .report-table th,
    .report-table td{
      line-height:1.45;
    }
.hero{
        position:relative !important;
      }
    }


    /* Layout com sidebar e painel principal travados */
    html, body{
      height:100%;
    }

    body{
      margin:0;
      overflow:hidden;
    }

    .app{
      height:100vh;
      padding:16px;
      overflow:hidden;
    }

    .shell{
      display:grid;
      grid-template-columns:290px minmax(0,1fr);
      gap:18px;
      height:calc(100vh - 32px);
      overflow:hidden;
      align-items:start;
    }

    .sidebar{
      position:sticky;
      top:0;
      align-self:start;
      height:calc(100vh - 32px);
      max-height:calc(100vh - 32px);
      overflow-y:scroll;
      overflow-x:hidden;
      overscroll-behavior:contain;
      scrollbar-gutter:stable;
      scrollbar-width:thin;
      scrollbar-color:rgba(7,87,168,.55) rgba(7,87,168,.10);
      padding-right:8px;
    }

    .sidebar::-webkit-scrollbar{
      width:10px;
    }

    .sidebar::
---

        overflow:visible;
        padding-right:0;
      }

      .main-content{
        height:auto;
        overflow:visible;
        padding-right:0;
      }

      .hero{
        position:relative;
        top:auto;
      }
    }

    @media print{
      body,
      .app,
      .shell,
      .sidebar,
      .main-content{
        height:auto !important;
        max-height:none !important;
        overflow:visible !important;
      }

      .hero{
        position:relative !important;
      }
    }

    .dash-grid{
      display:grid;
      grid-template-columns:1.45fr .75fr;
      gap:18px;
      align-items:start;
    }

    .dash-hero{
      grid-row:span 2;
      min-height:520px;
      background:
        radial-gradient(circle at 100% 0%, rgba(255,194,10,.16), transparent 28%),
        radial-gradient(circle at 0% 100%, rgba(88,195,222,.16), transparent 28%),
        linear-gradient(145deg,rgba(255,255,255,.90),rgba(247,252,255,.72));
    }

    .dash-main{
      display:grid;
      grid-template-columns:1fr 210px;
      gap:18px;
      align-items:stretch;
      margin-top:12px;
    }

    .dash-main > div:first-child{
      border-radius:16px;
      padding:22px;
      background:linear-gradient(135deg,#0757a8,#58c3de)
---
adding-right:0;
      }

      .main-content{
        height:auto;
        overflow:visible;
        padding-right:0;
      }

      .hero{
        position:relative;
        top:auto;
      }
    }

    @media print{
      body,
      .app,
      .shell,
      .sidebar,
      .main-content{
        height:auto !important;
        max-height:none !important;
        overflow:visible !important;
      }

      .hero{
        position:relative !important;
      }
    }

    .dash-grid{
      display:grid;
      grid-template-columns:1.45fr .75fr;
      gap:18px;
      align-items:start;
    }

    .dash-hero{
      grid-row:span 2;
      min-height:520px;
      background:
        radial-gradient(circle at 100% 0%, rgba(255,194,10,.16), transparent 28%),
        radial-gradient(circle at 0% 100%, rgba(88,195,222,.16), transparent 28%),
        linear-gradient(145deg,rgba(255,255,255,.90),rgba(247,252,255,.72));
    }

    .dash-main{
      display:grid;
      grid-template-columns:1fr 210px;
      gap:18px;
      align-items:stretch;
      margin-top:12px;
    }

    .dash-main > div:first-child{
      border-radius:16px;
      padding:22px;
      background:linear-gradient(135deg,#0757a8,#58c3de);
      color:white;
      box-shado
---
content{
        height:auto;
        overflow:visible;
        padding-right:0;
      }

      .hero{
        position:relative;
        top:auto;
      }
    }

    @media print{
      body,
      .app,
      .shell,
      .sidebar,
      .main-content{
        height:auto !important;
        max-height:none !important;
        overflow:visible !important;
      }

      .hero{
        position:relative !important;
      }
    }

    .dash-grid{
      display:grid;
      grid-template-columns:1.45fr .75fr;
      gap:18px;
      align-items:start;
    }

    .dash-hero{
      grid-row:span 2;
      min-height:520px;
      background:
        radial-gradient(circle at 100% 0%, rgba(255,194,10,.16), transparent 28%),
        radial-gradient(circle at 0% 100%, rgba(88,195,222,.16), transparent 28%),
        linear-gradient(145deg,rgba(255,255,255,.90),rgba(247,252,255,.72));
    }

    .dash-main{
      display:grid;
      grid-template-columns:1fr 210px;
      gap:18px;
      align-items:stretch;
      margin-top:12px;
    }

    .dash-main > div:first-child{
      border-radius:16px;
      padding:22px;
      background:linear-gradient(135deg,#0757a8,#58c3de);
      color:white;
      box-shadow:0 18px 38px rgba(7,87,168,.20);
   
---
    padding-right:0;
      }

      .hero{
        position:relative;
        top:auto;
      }
    }

    @media print{
      body,
      .app,
      .shell,
      .sidebar,
      .main-content{
        height:auto !important;
        max-height:none !important;
        overflow:visible !important;
      }

      .hero{
        position:relative !important;
      }
    }

    .dash-grid{
      display:grid;
      grid-template-columns:1.45fr .75fr;
      gap:18px;
      align-items:start;
    }

    .dash-hero{
      grid-row:span 2;
      min-height:520px;
      background:
        radial-gradient(circle at 100% 0%, rgba(255,194,10,.16), transparent 28%),
        radial-gradient(circle at 0% 100%, rgba(88,195,222,.16), transparent 28%),
        linear-gradient(145deg,rgba(255,255,255,.90),rgba(247,252,255,.72));
    }

    .dash-main{
      display:grid;
      grid-template-columns:1fr 210px;
      gap:18px;
      align-items:stretch;
      margin-top:12px;
    }

    .dash-main > div:first-child{
      border-radius:16px;
      padding:22px;
      background:linear-gradient(135deg,#0757a8,#58c3de);
      color:white;
      box-shadow:0 18px 38px rgba(7,87,168,.20);
      position:relative;
      overflow:hidden;
    }

    .das
---
idth:1020px){
      .shell{
        grid-template-columns:1fr;
      }

      .sidebar{
        position:relative;
        top:auto;
      }
    }

    @media(max-width:720px){
      .sidebar{
        padding:14px;
      }

      .sidebar-nav{
        grid-template-columns:1fr;
      }
    }

    @media print{
      .sidebar{
        display:none !important;
      }
      .shell{
        display:block !important;
      }
    }

    .brand-logo{
      width:min(250px,100%);
      height:auto;
      display:block;
      filter:none;
      background:transparent;
    }

    .brand-card{
      display:flex;
      align-items:center;
      justify-content:center;
      border-radius:15px;
      padding:12px 14px;
      background:linear-gradient(145deg,rgba(255,255,255,.92),rgba(255,252,238,.66));
      border:1px solid rgba(255,194,10,.28);
      box-shadow:0 18px 42px rgba(7,87,168,.12);
      position:relative;
      overflow:hidden;
    }

    .brand-card:before{
      display:none;
      content:"";
      position:absolute;
    }

    .brand-mini{
      display:flex;
      align-items:center;
      gap:12px;
      margin-bottom:10px;
    }

    .brand-mini img{
      width:76px;
      height:auto;
      filter:none;
    }

    .br
---
s:1fr;
      }

      .sidebar{
        position:relative;
        top:auto;
      }
    }

    @media(max-width:720px){
      .sidebar{
        padding:14px;
      }

      .sidebar-nav{
        grid-template-columns:1fr;
      }
    }

    @media print{
      .sidebar{
        display:none !important;
      }
      .shell{
        display:block !important;
      }
    }

    .brand-logo{
      width:min(250px,100%);
      height:auto;
      display:block;
      filter:none;
      background:transparent;
    }

    .brand-card{
      display:flex;
      align-items:center;
      justify-content:center;
      border-radius:15px;
      padding:12px 14px;
      background:linear-gradient(145deg,rgba(255,255,255,.92),rgba(255,252,238,.66));
      border:1px solid rgba(255,194,10,.28);
      box-shadow:0 18px 42px rgba(7,87,168,.12);
      position:relative;
      overflow:hidden;
    }

    .brand-card:before{
      display:none;
      content:"";
      position:absolute;
    }

    .brand-mini{
      display:flex;
      align-items:center;
      gap:12px;
      margin-bottom:10px;
    }

    .brand-mini img{
      width:76px;
      height:auto;
      filter:none;
    }

    .brand-mini div{
      display:flex;
      flex-direction:c
---
ear-gradient(135deg,#0757a8,#58c3de);
    }

    .pill{
      border-color:rgba(255,194,10,.28);
      background:rgba(255,252,238,.82);
    }

    .total-box{
      border:1px solid rgba(255,194,10,.24);
    }

    .hero-badge{
      background:linear-gradient(135deg,#0757a8,#58c3de);
    }

    @media print{
      body{
        background:white !important;
      }
      .hero,
      .tabs,
      .report-toolbar,
      .view:not(.active),
      .no-print{
        display:none !important;
      }
      .app{
        width:100%;
        padding:0;
        margin:0;
      }
      .panel,
      .print-sheet{
        box-shadow:none !important;
        border:0 !important;
        background:white !important;
        backdrop-filter:none !important;
      }
      .print-sheet{
        page-break-after:always;
        border-radius:0 !important;
      }
      .print-sheet:last-child{
        page-break-after:auto;
      }
      .print-sheet:before{
        display:none !important;
      }
      .print-inner{
        padding:18mm 14mm;
      }
      .report-table{
        border:1px solid #ccd7df;
      }
      .report-table th{
        background:#eef5fb !important;
        color:#123047 !important;
      }
      .report-table th,
    
---
2);
    }

    .total-box{
      border:1px solid rgba(255,194,10,.24);
    }

    .hero-badge{
      background:linear-gradient(135deg,#0757a8,#58c3de);
    }

    @media print{
      body{
        background:white !important;
      }
      .hero,
      .tabs,
      .report-toolbar,
      .view:not(.active),
      .no-print{
        display:none !important;
      }
      .app{
        width:100%;
        padding:0;
        margin:0;
      }
      .panel,
      .print-sheet{
        box-shadow:none !important;
        border:0 !important;
        background:white !important;
        backdrop-filter:none !important;
      }
      .print-sheet{
        page-break-after:always;
        border-radius:0 !important;
      }
      .print-sheet:last-child{
        page-break-after:auto;
      }
      .print-sheet:before{
        display:none !important;
      }
      .print-inner{
        padding:18mm 14mm;
      }
      .report-table{
        border:1px solid #ccd7df;
      }
      .report-table th{
        background:#eef5fb !important;
        color:#123047 !important;
      }
      .report-table th,
      .report-table td{
        border-color:#dce5eb !important;
        color:#123047 !important;
      }
      .budget-box,
      .prin
---
   }

    @media print{
      body{
        background:white !important;
      }
      .hero,
      .tabs,
      .report-toolbar,
      .view:not(.active),
      .no-print{
        display:none !important;
      }
      .app{
        width:100%;
        padding:0;
        margin:0;
      }
      .panel,
      .print-sheet{
        box-shadow:none !important;
        border:0 !important;
        background:white !important;
        backdrop-filter:none !important;
      }
      .print-sheet{
        page-break-after:always;
        border-radius:0 !important;
      }
      .print-sheet:last-child{
        page-break-after:auto;
      }
      .print-sheet:before{
        display:none !important;
      }
      .print-inner{
        padding:18mm 14mm;
      }
      .report-table{
        border:1px solid #ccd7df;
      }
      .report-table th{
        background:#eef5fb !important;
        color:#123047 !important;
      }
      .report-table th,
      .report-table td{
        border-color:#dce5eb !important;
        color:#123047 !important;
      }
      .budget-box,
      .print-note{
        background:#f4f8fb !important;
        border:1px solid #dce5eb !important;
      }
    }


    .panel,
    .kpi,
    .print-sheet,
    .ca
---
 body{
        background:white !important;
      }
      .hero,
      .tabs,
      .report-toolbar,
      .view:not(.active),
      .no-print{
        display:none !important;
      }
      .app{
        width:100%;
        padding:0;
        margin:0;
      }
      .panel,
      .print-sheet{
        box-shadow:none !important;
        border:0 !important;
        background:white !important;
        backdrop-filter:none !important;
      }
      .print-sheet{
        page-break-after:always;
        border-radius:0 !important;
      }
      .print-sheet:last-child{
        page-break-after:auto;
      }
      .print-sheet:before{
        display:none !important;
      }
      .print-inner{
        padding:18mm 14mm;
      }
      .report-table{
        border:1px solid #ccd7df;
      }
      .report-table th{
        background:#eef5fb !important;
        color:#123047 !important;
      }
      .report-table th,
      .report-table td{
        border-color:#dce5eb !important;
        color:#123047 !important;
      }
      .budget-box,
      .print-note{
        background:#f4f8fb !important;
        border:1px solid #dce5eb !important;
      }
    }


    .panel,
    .kpi,
    .print-sheet,
    .card-option,
    .hour-btn,
   
---
rtant;
      }
      .hero,
      .tabs,
      .report-toolbar,
      .view:not(.active),
      .no-print{
        display:none !important;
      }
      .app{
        width:100%;
        padding:0;
        margin:0;
      }
      .panel,
      .print-sheet{
        box-shadow:none !important;
        border:0 !important;
        background:white !important;
        backdrop-filter:none !important;
      }
      .print-sheet{
        page-break-after:always;
        border-radius:0 !important;
      }
      .print-sheet:last-child{
        page-break-after:auto;
      }
      .print-sheet:before{
        display:none !important;
      }
      .print-inner{
        padding:18mm 14mm;
      }
      .report-table{
        border:1px solid #ccd7df;
      }
      .report-table th{
        background:#eef5fb !important;
        color:#123047 !important;
      }
      .report-table th,
      .report-table td{
        border-color:#dce5eb !important;
        color:#123047 !important;
      }
      .budget-box,
      .print-note{
        background:#f4f8fb !important;
        border:1px solid #dce5eb !important;
      }
    }


    .panel,
    .kpi,
    .print-sheet,
    .card-option,
    .hour-btn,
    .extra,
    .weight-row,
    .price-
---
      .report-toolbar,
      .view:not(.active),
      .no-print{
        display:none !important;
      }
      .app{
        width:100%;
        padding:0;
        margin:0;
      }
      .panel,
      .print-sheet{
        box-shadow:none !important;
        border:0 !important;
        background:white !important;
        backdrop-filter:none !important;
      }
      .print-sheet{
        page-break-after:always;
        border-radius:0 !important;
      }
      .print-sheet:last-child{
        page-break-after:auto;
      }
      .print-sheet:before{
        display:none !important;
      }
      .print-inner{
        padding:18mm 14mm;
      }
      .report-table{
        border:1px solid #ccd7df;
      }
      .report-table th{
        background:#eef5fb !important;
        color:#123047 !important;
      }
      .report-table th,
      .report-table td{
        border-color:#dce5eb !important;
        color:#123047 !important;
      }
      .budget-box,
      .print-note{
        background:#f4f8fb !important;
        border:1px solid #dce5eb !important;
      }
    }


    .panel,
    .kpi,
    .print-sheet,
    .card-option,
    .hour-btn,
    .extra,
    .weight-row,
    .price-cell,
    .budget-box{
      position:rel
---
;
      }
      .app{
        width:100%;
        padding:0;
        margin:0;
      }
      .panel,
      .print-sheet{
        box-shadow:none !important;
        border:0 !important;
        background:white !important;
        backdrop-filter:none !important;
      }
      .print-sheet{
        page-break-after:always;
        border-radius:0 !important;
      }
      .print-sheet:last-child{
        page-break-after:auto;
      }
      .print-sheet:before{
        display:none !important;
      }
      .print-inner{
        padding:18mm 14mm;
      }
      .report-table{
        border:1px solid #ccd7df;
      }
      .report-table th{
        background:#eef5fb !important;
        color:#123047 !important;
      }
      .report-table th,
      .report-table td{
        border-color:#dce5eb !important;
        color:#123047 !important;
      }
      .budget-box,
      .print-note{
        background:#f4f8fb !important;
        border:1px solid #dce5eb !important;
      }
    }


    .panel,
    .kpi,
    .print-sheet,
    .card-option,
    .hour-btn,
    .extra,
    .weight-row,
    .price-cell,
    .budget-box{
      position:relative;
      overflow:hidden;
    }

    .panel:after,
    .kpi:after,
    .print-sheet:after{
  
---
w:none !important;
        border:0 !important;
        background:white !important;
        backdrop-filter:none !important;
      }
      .print-sheet{
        page-break-after:always;
        border-radius:0 !important;
      }
      .print-sheet:last-child{
        page-break-after:auto;
      }
      .print-sheet:before{
        display:none !important;
      }
      .print-inner{
        padding:18mm 14mm;
      }
      .report-table{
        border:1px solid #ccd7df;
      }
      .report-table th{
        background:#eef5fb !important;
        color:#123047 !important;
      }
      .report-table th,
      .report-table td{
        border-color:#dce5eb !important;
        color:#123047 !important;
      }
      .budget-box,
      .print-note{
        background:#f4f8fb !important;
        border:1px solid #dce5eb !important;
      }
    }


    .panel,
    .kpi,
    .print-sheet,
    .card-option,
    .hour-btn,
    .extra,
    .weight-row,
    .price-cell,
    .budget-box{
      position:relative;
      overflow:hidden;
    }

    .panel:after,
    .kpi:after,
    .print-sheet:after{
      content:"";
      position:absolute;
      left:0;
      right:0;
      top:0;
      height:3px;
      background:linear-gradient(90de
---
      border-radius:0 !important;
      }
      .print-sheet:last-child{
        page-break-after:auto;
      }
      .print-sheet:before{
        display:none !important;
      }
      .print-inner{
        padding:18mm 14mm;
      }
      .report-table{
        border:1px solid #ccd7df;
      }
      .report-table th{
        background:#eef5fb !important;
        color:#123047 !important;
      }
      .report-table th,
      .report-table td{
        border-color:#dce5eb !important;
        color:#123047 !important;
      }
      .budget-box,
      .print-note{
        background:#f4f8fb !important;
        border:1px solid #dce5eb !important;
      }
    }


    .panel,
    .kpi,
    .print-sheet,
    .card-option,
    .hour-btn,
    .extra,
    .weight-row,
    .price-cell,
    .budget-box{
      position:relative;
      overflow:hidden;
    }

    .panel:after,
    .kpi:after,
    .print-sheet:after{
      content:"";
      position:absolute;
      left:0;
      right:0;
      top:0;
      height:3px;
      background:linear-gradient(90deg,#0757a8,#58c3de,#ffc20a);
      opacity:.95;
      pointer-events:none;
    }

    .card-option{
      border-width:1.5px;
    }

    .card-option[data-turma="bercario"]{
      border-col
---
      }
      .print-sheet:last-child{
        page-break-after:auto;
      }
      .print-sheet:before{
        display:none !important;
      }
      .print-inner{
        padding:18mm 14mm;
      }
      .report-table{
        border:1px solid #ccd7df;
      }
      .report-table th{
        background:#eef5fb !important;
        color:#123047 !important;
      }
      .report-table th,
      .report-table td{
        border-color:#dce5eb !important;
        color:#123047 !important;
      }
      .budget-box,
      .print-note{
        background:#f4f8fb !important;
        border:1px solid #dce5eb !important;
      }
    }


    .panel,
    .kpi,
    .print-sheet,
    .card-option,
    .hour-btn,
    .extra,
    .weight-row,
    .price-cell,
    .budget-box{
      position:relative;
      overflow:hidden;
    }

    .panel:after,
    .kpi:after,
    .print-sheet:after{
      content:"";
      position:absolute;
      left:0;
      right:0;
      top:0;
      height:3px;
      background:linear-gradient(90deg,#0757a8,#58c3de,#ffc20a);
      opacity:.95;
      pointer-events:none;
    }

    .card-option{
      border-width:1.5px;
    }

    .card-option[data-turma="bercario"]{
      border-color:rgba(255,194,10,.75);
      box
---
before{
        display:none !important;
      }
      .print-inner{
        padding:18mm 14mm;
      }
      .report-table{
        border:1px solid #ccd7df;
      }
      .report-table th{
        background:#eef5fb !important;
        color:#123047 !important;
      }
      .report-table th,
      .report-table td{
        border-color:#dce5eb !important;
        color:#123047 !important;
      }
      .budget-box,
      .print-note{
        background:#f4f8fb !important;
        border:1px solid #dce5eb !important;
      }
    }


    .panel,
    .kpi,
    .print-sheet,
    .card-option,
    .hour-btn,
    .extra,
    .weight-row,
    .price-cell,
    .budget-box{
      position:relative;
      overflow:hidden;
    }

    .panel:after,
    .kpi:after,
    .print-sheet:after{
      content:"";
      position:absolute;
      left:0;
      right:0;
      top:0;
      height:3px;
      background:linear-gradient(90deg,#0757a8,#58c3de,#ffc20a);
      opacity:.95;
      pointer-events:none;
    }

    .card-option{
      border-width:1.5px;
    }

    .card-option[data-turma="bercario"]{
      border-color:rgba(255,194,10,.75);
      box-shadow:0 12px 28px rgba(255,194,10,.10), 0 10px 24px rgba(7,87,168,.06);
    }

    .card-option
---
rtant;
      }
      .print-inner{
        padding:18mm 14mm;
      }
      .report-table{
        border:1px solid #ccd7df;
      }
      .report-table th{
        background:#eef5fb !important;
        color:#123047 !important;
      }
      .report-table th,
      .report-table td{
        border-color:#dce5eb !important;
        color:#123047 !important;
      }
      .budget-box,
      .print-note{
        background:#f4f8fb !important;
        border:1px solid #dce5eb !important;
      }
    }


    .panel,
    .kpi,
    .print-sheet,
    .card-option,
    .hour-btn,
    .extra,
    .weight-row,
    .price-cell,
    .budget-box{
      position:relative;
      overflow:hidden;
    }

    .panel:after,
    .kpi:after,
    .print-sheet:after{
      content:"";
      position:absolute;
      left:0;
      right:0;
      top:0;
      height:3px;
      background:linear-gradient(90deg,#0757a8,#58c3de,#ffc20a);
      opacity:.95;
      pointer-events:none;
    }

    .card-option{
      border-width:1.5px;
    }

    .card-option[data-turma="bercario"]{
      border-color:rgba(255,194,10,.75);
      box-shadow:0 12px 28px rgba(255,194,10,.10), 0 10px 24px rgba(7,87,168,.06);
    }

    .card-option[data-turma="maternal1"]{
      bo
---
able{
        border:1px solid #ccd7df;
      }
      .report-table th{
        background:#eef5fb !important;
        color:#123047 !important;
      }
      .report-table th,
      .report-table td{
        border-color:#dce5eb !important;
        color:#123047 !important;
      }
      .budget-box,
      .print-note{
        background:#f4f8fb !important;
        border:1px solid #dce5eb !important;
      }
    }


    .panel,
    .kpi,
    .print-sheet,
    .card-option,
    .hour-btn,
    .extra,
    .weight-row,
    .price-cell,
    .budget-box{
      position:relative;
      overflow:hidden;
    }

    .panel:after,
    .kpi:after,
    .print-sheet:after{
      content:"";
      position:absolute;
      left:0;
      right:0;
      top:0;
      height:3px;
      background:linear-gradient(90deg,#0757a8,#58c3de,#ffc20a);
      opacity:.95;
      pointer-events:none;
    }

    .card-option{
      border-width:1.5px;
    }

    .card-option[data-turma="bercario"]{
      border-color:rgba(255,194,10,.75);
      box-shadow:0 12px 28px rgba(255,194,10,.10), 0 10px 24px rgba(7,87,168,.06);
    }

    .card-option[data-turma="maternal1"]{
      border-color:rgba(88,195,222,.75);
      box-shadow:0 12px 28px rgba(88,195,222,.10), 0
---
 }
      .report-table th{
        background:#eef5fb !important;
        color:#123047 !important;
      }
      .report-table th,
      .report-table td{
        border-color:#dce5eb !important;
        color:#123047 !important;
      }
      .budget-box,
      .print-note{
        background:#f4f8fb !important;
        border:1px solid #dce5eb !important;
      }
    }


    .panel,
    .kpi,
    .print-sheet,
    .card-option,
    .hour-btn,
    .extra,
    .weight-row,
    .price-cell,
    .budget-box{
      position:relative;
      overflow:hidden;
    }

    .panel:after,
    .kpi:after,
    .print-sheet:after{
      content:"";
      position:absolute;
      left:0;
      right:0;
      top:0;
      height:3px;
      background:linear-gradient(90deg,#0757a8,#58c3de,#ffc20a);
      opacity:.95;
      pointer-events:none;
    }

    .card-option{
      border-width:1.5px;
    }

    .card-option[data-turma="bercario"]{
      border-color:rgba(255,194,10,.75);
      box-shadow:0 12px 28px rgba(255,194,10,.10), 0 10px 24px rgba(7,87,168,.06);
    }

    .card-option[data-turma="maternal1"]{
      border-color:rgba(88,195,222,.75);
      box-shadow:0 12px 28px rgba(88,195,222,.10), 0 10px 24px rgba(7,87,168,.06);
    }

    .ca
---
  }


    .hero{
      max-width:1120px;
      margin-inline:auto;
    }

    .hero .eyebrow{
      padding:6px 10px;
      font-size:11px;
    }

    .hero h1{
      max-width:760px;
    }

    .hero p{
      max-width:680px;
    }

    .brand-card{
      min-height:auto;
    }

    @media(max-width:640px){
      .hero{
        padding:14px 14px !important;
      }
      .brand-logo{
        width:min(190px,100%);
      }
      .hero h1{
        font-size:24px !important;
      }
    }


    .nutrition-grid{
      display:grid;
      grid-template-columns:repeat(2,minmax(0,1fr));
      gap:12px;
      margin-top:14px;
    }

    .nutrition-row{
      display:grid;
      grid-template-columns:1fr 96px;
      gap:12px;
      align-items:center;
      border:1px solid rgba(7,87,168,.14);
      border-left:4px solid rgba(88,195,222,.82);
      border-radius:14px;
      padding:13px;
      background:rgba(255,255,255,.66);
    }

    .nutrition-row:nth-child(odd){
      border-left-color:rgba(255,194,10,.86);
    }

    .nutrition-row b{
      display:block;
      color:#123047;
      font-size:14px;
      margin-bottom:3px;
    }

    .nutrition-row small{
      display:block;
      color:#667789;
      font-size:12px;
      font-wei
---

      font-size:11px;
    }

    .hero h1{
      max-width:760px;
    }

    .hero p{
      max-width:680px;
    }

    .brand-card{
      min-height:auto;
    }

    @media(max-width:640px){
      .hero{
        padding:14px 14px !important;
      }
      .brand-logo{
        width:min(190px,100%);
      }
      .hero h1{
        font-size:24px !important;
      }
    }


    .nutrition-grid{
      display:grid;
      grid-template-columns:repeat(2,minmax(0,1fr));
      gap:12px;
      margin-top:14px;
    }

    .nutrition-row{
      display:grid;
      grid-template-columns:1fr 96px;
      gap:12px;
      align-items:center;
      border:1px solid rgba(7,87,168,.14);
      border-left:4px solid rgba(88,195,222,.82);
      border-radius:14px;
      padding:13px;
      background:rgba(255,255,255,.66);
    }

    .nutrition-row:nth-child(odd){
      border-left-color:rgba(255,194,10,.86);
    }

    .nutrition-row b{
      display:block;
      color:#123047;
      font-size:14px;
      margin-bottom:3px;
    }

    .nutrition-row small{
      display:block;
      color:#667789;
      font-size:12px;
      font-weight:800;
    }

    .nutrition-row input{
      width:100%;
      border:none;
      outline:none;
      border-radiu
---
der-radius:26px}
      .turmas,
      .extras,
      .fields{
        grid-template-columns:1fr;
      }
      .hours,
      .price-grid{
        grid-template-columns:repeat(2,1fr);
      }
      .panel{padding:16px}
      .hero-badge strong{font-size:24px}
    }
  
    /* Força rolagem interna no sidebar */
    .sidebar{
      overflow-y:scroll !important;
      overflow-x:hidden !important;
      height:calc(100vh - 32px) !important;
      max-height:calc(100vh - 32px) !important;
    }

    .main-content{
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
    }

    @media(max-width:1020px){
      .sidebar,
      .main-content{
        height:auto !important;
        max-height:none !important;
        overflow:visible !important;
      }
    }


    /* Correção final: painel principal/topo realmente travado */
    .main-content{
      position:relative !important;
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
      scroll-padding-top:150px;
    }

    .main-content > .hero{
      position:sticky !important;
      top:0 !important;
      z-index:999 !important;
      margin-top:0 !important;
      
---
  .extras,
      .fields{
        grid-template-columns:1fr;
      }
      .hours,
      .price-grid{
        grid-template-columns:repeat(2,1fr);
      }
      .panel{padding:16px}
      .hero-badge strong{font-size:24px}
    }
  
    /* Força rolagem interna no sidebar */
    .sidebar{
      overflow-y:scroll !important;
      overflow-x:hidden !important;
      height:calc(100vh - 32px) !important;
      max-height:calc(100vh - 32px) !important;
    }

    .main-content{
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
    }

    @media(max-width:1020px){
      .sidebar,
      .main-content{
        height:auto !important;
        max-height:none !important;
        overflow:visible !important;
      }
    }


    /* Correção final: painel principal/topo realmente travado */
    .main-content{
      position:relative !important;
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
      scroll-padding-top:150px;
    }

    .main-content > .hero{
      position:sticky !important;
      top:0 !important;
      z-index:999 !important;
      margin-top:0 !important;
      margin-bottom:14px !important;
     
---
ate-columns:1fr;
      }
      .hours,
      .price-grid{
        grid-template-columns:repeat(2,1fr);
      }
      .panel{padding:16px}
      .hero-badge strong{font-size:24px}
    }
  
    /* Força rolagem interna no sidebar */
    .sidebar{
      overflow-y:scroll !important;
      overflow-x:hidden !important;
      height:calc(100vh - 32px) !important;
      max-height:calc(100vh - 32px) !important;
    }

    .main-content{
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
    }

    @media(max-width:1020px){
      .sidebar,
      .main-content{
        height:auto !important;
        max-height:none !important;
        overflow:visible !important;
      }
    }


    /* Correção final: painel principal/topo realmente travado */
    .main-content{
      position:relative !important;
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
      scroll-padding-top:150px;
    }

    .main-content > .hero{
      position:sticky !important;
      top:0 !important;
      z-index:999 !important;
      margin-top:0 !important;
      margin-bottom:14px !important;
      background:linear-gradient(135deg,rgba(255,
---
ice-grid{
        grid-template-columns:repeat(2,1fr);
      }
      .panel{padding:16px}
      .hero-badge strong{font-size:24px}
    }
  
    /* Força rolagem interna no sidebar */
    .sidebar{
      overflow-y:scroll !important;
      overflow-x:hidden !important;
      height:calc(100vh - 32px) !important;
      max-height:calc(100vh - 32px) !important;
    }

    .main-content{
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
    }

    @media(max-width:1020px){
      .sidebar,
      .main-content{
        height:auto !important;
        max-height:none !important;
        overflow:visible !important;
      }
    }


    /* Correção final: painel principal/topo realmente travado */
    .main-content{
      position:relative !important;
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
      scroll-padding-top:150px;
    }

    .main-content > .hero{
      position:sticky !important;
      top:0 !important;
      z-index:999 !important;
      margin-top:0 !important;
      margin-bottom:14px !important;
      background:linear-gradient(135deg,rgba(255,255,255,.94),rgba(255,255,255,.78)) !important;

---
panel{padding:16px}
      .hero-badge strong{font-size:24px}
    }
  
    /* Força rolagem interna no sidebar */
    .sidebar{
      overflow-y:scroll !important;
      overflow-x:hidden !important;
      height:calc(100vh - 32px) !important;
      max-height:calc(100vh - 32px) !important;
    }

    .main-content{
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
    }

    @media(max-width:1020px){
      .sidebar,
      .main-content{
        height:auto !important;
        max-height:none !important;
        overflow:visible !important;
      }
    }


    /* Correção final: painel principal/topo realmente travado */
    .main-content{
      position:relative !important;
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
      scroll-padding-top:150px;
    }

    .main-content > .hero{
      position:sticky !important;
      top:0 !important;
      z-index:999 !important;
      margin-top:0 !important;
      margin-bottom:14px !important;
      background:linear-gradient(135deg,rgba(255,255,255,.94),rgba(255,255,255,.78)) !important;
      backdrop-filter:blur(24px) saturate(175%) !important;
      -web
---
dge strong{font-size:24px}
    }
  
    /* Força rolagem interna no sidebar */
    .sidebar{
      overflow-y:scroll !important;
      overflow-x:hidden !important;
      height:calc(100vh - 32px) !important;
      max-height:calc(100vh - 32px) !important;
    }

    .main-content{
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
    }

    @media(max-width:1020px){
      .sidebar,
      .main-content{
        height:auto !important;
        max-height:none !important;
        overflow:visible !important;
      }
    }


    /* Correção final: painel principal/topo realmente travado */
    .main-content{
      position:relative !important;
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
      scroll-padding-top:150px;
    }

    .main-content > .hero{
      position:sticky !important;
      top:0 !important;
      z-index:999 !important;
      margin-top:0 !important;
      margin-bottom:14px !important;
      background:linear-gradient(135deg,rgba(255,255,255,.94),rgba(255,255,255,.78)) !important;
      backdrop-filter:blur(24px) saturate(175%) !important;
      -webkit-backdrop-filter:blur(24px) sat
---
    /* Força rolagem interna no sidebar */
    .sidebar{
      overflow-y:scroll !important;
      overflow-x:hidden !important;
      height:calc(100vh - 32px) !important;
      max-height:calc(100vh - 32px) !important;
    }

    .main-content{
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
    }

    @media(max-width:1020px){
      .sidebar,
      .main-content{
        height:auto !important;
        max-height:none !important;
        overflow:visible !important;
      }
    }


    /* Correção final: painel principal/topo realmente travado */
    .main-content{
      position:relative !important;
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
      scroll-padding-top:150px;
    }

    .main-content > .hero{
      position:sticky !important;
      top:0 !important;
      z-index:999 !important;
      margin-top:0 !important;
      margin-bottom:14px !important;
      background:linear-gradient(135deg,rgba(255,255,255,.94),rgba(255,255,255,.78)) !important;
      backdrop-filter:blur(24px) saturate(175%) !important;
      -webkit-backdrop-filter:blur(24px) saturate(175%) !important;
      box-sh
---
w-x:hidden !important;
      height:calc(100vh - 32px) !important;
      max-height:calc(100vh - 32px) !important;
    }

    .main-content{
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
    }

    @media(max-width:1020px){
      .sidebar,
      .main-content{
        height:auto !important;
        max-height:none !important;
        overflow:visible !important;
      }
    }


    /* Correção final: painel principal/topo realmente travado */
    .main-content{
      position:relative !important;
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
      scroll-padding-top:150px;
    }

    .main-content > .hero{
      position:sticky !important;
      top:0 !important;
      z-index:999 !important;
      margin-top:0 !important;
      margin-bottom:14px !important;
      background:linear-gradient(135deg,rgba(255,255,255,.94),rgba(255,255,255,.78)) !important;
      backdrop-filter:blur(24px) saturate(175%) !important;
      -webkit-backdrop-filter:blur(24px) saturate(175%) !important;
      box-shadow:0 16px 34px rgba(7,87,168,.16) !important;
    }

    .main-content > .hero:before{
      z-index:0;

---
calc(100vh - 32px) !important;
      max-height:calc(100vh - 32px) !important;
    }

    .main-content{
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
    }

    @media(max-width:1020px){
      .sidebar,
      .main-content{
        height:auto !important;
        max-height:none !important;
        overflow:visible !important;
      }
    }


    /* Correção final: painel principal/topo realmente travado */
    .main-content{
      position:relative !important;
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
      scroll-padding-top:150px;
    }

    .main-content > .hero{
      position:sticky !important;
      top:0 !important;
      z-index:999 !important;
      margin-top:0 !important;
      margin-bottom:14px !important;
      background:linear-gradient(135deg,rgba(255,255,255,.94),rgba(255,255,255,.78)) !important;
      backdrop-filter:blur(24px) saturate(175%) !important;
      -webkit-backdrop-filter:blur(24px) saturate(175%) !important;
      box-shadow:0 16px 34px rgba(7,87,168,.16) !important;
    }

    .main-content > .hero:before{
      z-index:0;
    }

    .main-content > .hero .he
---
max-height:calc(100vh - 32px) !important;
    }

    .main-content{
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
    }

    @media(max-width:1020px){
      .sidebar,
      .main-content{
        height:auto !important;
        max-height:none !important;
        overflow:visible !important;
      }
    }


    /* Correção final: painel principal/topo realmente travado */
    .main-content{
      position:relative !important;
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
      scroll-padding-top:150px;
    }

    .main-content > .hero{
      position:sticky !important;
      top:0 !important;
      z-index:999 !important;
      margin-top:0 !important;
      margin-bottom:14px !important;
      background:linear-gradient(135deg,rgba(255,255,255,.94),rgba(255,255,255,.78)) !important;
      backdrop-filter:blur(24px) saturate(175%) !important;
      -webkit-backdrop-filter:blur(24px) saturate(175%) !important;
      box-shadow:0 16px 34px rgba(7,87,168,.16) !important;
    }

    .main-content > .hero:before{
      z-index:0;
    }

    .main-content > .hero .hero-content{
      position:relative;

---
portant;
      overflow-x:hidden !important;
    }

    @media(max-width:1020px){
      .sidebar,
      .main-content{
        height:auto !important;
        max-height:none !important;
        overflow:visible !important;
      }
    }


    /* Correção final: painel principal/topo realmente travado */
    .main-content{
      position:relative !important;
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
      scroll-padding-top:150px;
    }

    .main-content > .hero{
      position:sticky !important;
      top:0 !important;
      z-index:999 !important;
      margin-top:0 !important;
      margin-bottom:14px !important;
      background:linear-gradient(135deg,rgba(255,255,255,.94),rgba(255,255,255,.78)) !important;
      backdrop-filter:blur(24px) saturate(175%) !important;
      -webkit-backdrop-filter:blur(24px) saturate(175%) !important;
      box-shadow:0 16px 34px rgba(7,87,168,.16) !important;
    }

    .main-content > .hero:before{
      z-index:0;
    }

    .main-content > .hero .hero-content{
      position:relative;
      z-index:2;
    }

    @media(max-width:1020px){
      .main-content{
        height:auto !important;
        overflow:visible !impo
---

    }

    @media(max-width:1020px){
      .sidebar,
      .main-content{
        height:auto !important;
        max-height:none !important;
        overflow:visible !important;
      }
    }


    /* Correção final: painel principal/topo realmente travado */
    .main-content{
      position:relative !important;
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
      scroll-padding-top:150px;
    }

    .main-content > .hero{
      position:sticky !important;
      top:0 !important;
      z-index:999 !important;
      margin-top:0 !important;
      margin-bottom:14px !important;
      background:linear-gradient(135deg,rgba(255,255,255,.94),rgba(255,255,255,.78)) !important;
      backdrop-filter:blur(24px) saturate(175%) !important;
      -webkit-backdrop-filter:blur(24px) saturate(175%) !important;
      box-shadow:0 16px 34px rgba(7,87,168,.16) !important;
    }

    .main-content > .hero:before{
      z-index:0;
    }

    .main-content > .hero .hero-content{
      position:relative;
      z-index:2;
    }

    @media(max-width:1020px){
      .main-content{
        height:auto !important;
        overflow:visible !important;
        scroll-padding-top:0;
      }
---
x){
      .sidebar,
      .main-content{
        height:auto !important;
        max-height:none !important;
        overflow:visible !important;
      }
    }


    /* Correção final: painel principal/topo realmente travado */
    .main-content{
      position:relative !important;
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
      scroll-padding-top:150px;
    }

    .main-content > .hero{
      position:sticky !important;
      top:0 !important;
      z-index:999 !important;
      margin-top:0 !important;
      margin-bottom:14px !important;
      background:linear-gradient(135deg,rgba(255,255,255,.94),rgba(255,255,255,.78)) !important;
      backdrop-filter:blur(24px) saturate(175%) !important;
      -webkit-backdrop-filter:blur(24px) saturate(175%) !important;
      box-shadow:0 16px 34px rgba(7,87,168,.16) !important;
    }

    .main-content > .hero:before{
      z-index:0;
    }

    .main-content > .hero .hero-content{
      position:relative;
      z-index:2;
    }

    @media(max-width:1020px){
      .main-content{
        height:auto !important;
        overflow:visible !important;
        scroll-padding-top:0;
      }

      .main-content > .hero{
   
---
ent{
        height:auto !important;
        max-height:none !important;
        overflow:visible !important;
      }
    }


    /* Correção final: painel principal/topo realmente travado */
    .main-content{
      position:relative !important;
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
      scroll-padding-top:150px;
    }

    .main-content > .hero{
      position:sticky !important;
      top:0 !important;
      z-index:999 !important;
      margin-top:0 !important;
      margin-bottom:14px !important;
      background:linear-gradient(135deg,rgba(255,255,255,.94),rgba(255,255,255,.78)) !important;
      backdrop-filter:blur(24px) saturate(175%) !important;
      -webkit-backdrop-filter:blur(24px) saturate(175%) !important;
      box-shadow:0 16px 34px rgba(7,87,168,.16) !important;
    }

    .main-content > .hero:before{
      z-index:0;
    }

    .main-content > .hero .hero-content{
      position:relative;
      z-index:2;
    }

    @media(max-width:1020px){
      .main-content{
        height:auto !important;
        overflow:visible !important;
        scroll-padding-top:0;
      }

      .main-content > .hero{
        position:relative !important;
 
---
mportant;
      }
    }


    /* Correção final: painel principal/topo realmente travado */
    .main-content{
      position:relative !important;
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
      scroll-padding-top:150px;
    }

    .main-content > .hero{
      position:sticky !important;
      top:0 !important;
      z-index:999 !important;
      margin-top:0 !important;
      margin-bottom:14px !important;
      background:linear-gradient(135deg,rgba(255,255,255,.94),rgba(255,255,255,.78)) !important;
      backdrop-filter:blur(24px) saturate(175%) !important;
      -webkit-backdrop-filter:blur(24px) saturate(175%) !important;
      box-shadow:0 16px 34px rgba(7,87,168,.16) !important;
    }

    .main-content > .hero:before{
      z-index:0;
    }

    .main-content > .hero .hero-content{
      position:relative;
      z-index:2;
    }

    @media(max-width:1020px){
      .main-content{
        height:auto !important;
        overflow:visible !important;
        scroll-padding-top:0;
      }

      .main-content > .hero{
        position:relative !important;
        top:auto !important;
        z-index:1 !important;
      }
    }

    @media print{
      .ma
---


    /* Correção final: painel principal/topo realmente travado */
    .main-content{
      position:relative !important;
      height:calc(100vh - 32px) !important;
      overflow-y:auto !important;
      overflow-x:hidden !important;
      scroll-padding-top:150px;
    }

    .main-content > .hero{
      position:sticky !important;
      top:0 !important;
      z-index:999 !important;
      margin-top:0 !important;
      margin-bottom:14px !important;
      background:linear-gradient(135deg,rgba(255,255,255,.94),rgba(255,255,255,.78)) !important;
      backdrop-filter:blur(24px) saturate(175%) !important;
      -webkit-backdrop-filter:blur(24px) saturate(175%) !important;
      box-shadow:0 16px 34px rgba(7,87,168,.16) !important;
    }

    .main-content > .hero:before{
      z-index:0;
    }

    .main-content > .hero .hero-content{
      position:relative;
      z-index:2;
    }

    @media(max-width:1020px){
      .main-content{
        height:auto !important;
        overflow:visible !important;
        scroll-padding-top:0;
      }

      .main-content > .hero{
        position:relative !important;
        top:auto !important;
        z-index:1 !important;
      }
    }

    @media print{
      .main-content > .hero{
    
---

### export

            <div class="total" id="avgGap">R$ 0,00</div>
          </div>

          <div class="summary" id="compareSummary"></div>

          <div class="insight" id="compareInsight"></div>

          <div class="actions">
            <button class="btn" id="copyCompareBtn">Copiar diagnóstico</button>
          <button class="btn secondary" id="exportCsvBtn" style="margin-top:10px">Exportar tabela como CSV</button>
            <button class="btn secondary" id="resetCurrentPricesBtn">Preencher exemplo atual</button>
          </div>
        </aside>
      </div>

      <div class="panel" style="margin-top:18px">
        <div class="section-title">
          <h2>Tabela comparativa</h2>
          <span class="pill">Atual x recomendado</span>
        </div>
        <div id="comparisonTable"></div>
      </div>
    </section>


    <section id="precisao" class="view">
      <div class="kpis">
        <div class="kpi">
          <small>Ponto de equilíbrio</small>
          <strong id="breakEvenStudents">0 alunos</strong>
          <em>pagantes para empatar</em>
        </div>
        <div class="kpi">
          <small>Ticket médio atual</small>
          <strong id="avgTicketCurrent">R$ 0,00</strong>
          <em>com preços praticad
---
gGap">R$ 0,00</div>
          </div>

          <div class="summary" id="compareSummary"></div>

          <div class="insight" id="compareInsight"></div>

          <div class="actions">
            <button class="btn" id="copyCompareBtn">Copiar diagnóstico</button>
          <button class="btn secondary" id="exportCsvBtn" style="margin-top:10px">Exportar tabela como CSV</button>
            <button class="btn secondary" id="resetCurrentPricesBtn">Preencher exemplo atual</button>
          </div>
        </aside>
      </div>

      <div class="panel" style="margin-top:18px">
        <div class="section-title">
          <h2>Tabela comparativa</h2>
          <span class="pill">Atual x recomendado</span>
        </div>
        <div id="comparisonTable"></div>
      </div>
    </section>


    <section id="precisao" class="view">
      <div class="kpis">
        <div class="kpi">
          <small>Ponto de equilíbrio</small>
          <strong id="breakEvenStudents">0 alunos</strong>
          <em>pagantes para empatar</em>
        </div>
        <div class="kpi">
          <small>Ticket médio atual</small>
          <strong id="avgTicketCurrent">R$ 0,00</strong>
          <em>com preços praticados</em>
        </div>
        <div cl
---
utomaticamente neste navegador para evitar retrabalho.</p>
              <div class="settings-status" id="saveStatus">Aguardando alterações...</div>
              <button class="btn secondary" id="clearStorageBtn">Limpar dados salvos</button>
            </div>

            <div class="settings-card">
              <h3>Backup</h3>
              <p>Exporte um arquivo JSON com todos os campos da calculadora e restaure quando precisar.</p>
              <div class="actions">
                <button class="btn" id="exportBackupBtn">Exportar backup</button>
                <label class="import-label">
                  Importar backup
                  <input id="importBackupInput" type="file" accept=".json,application/json">
                </label>
              </div>
            </div>

            <div class="settings-card">
              <h3>Instalar como app</h3>
              <p>Esta versão está preparada para virar PWA. Ao finalizar o design, gere o pacote com manifest, service worker e ícones.</p>
              <div class="app-checklist">
                <span>✅ HTML único funcional</span>
                <span>✅ Dados locais</span>
                <span>✅ Backup/importação</span>
                <span>⏳ Manifest + Service Wo
---
ton class="btn secondary" id="clearStorageBtn">Limpar dados salvos</button>
            </div>

            <div class="settings-card">
              <h3>Backup</h3>
              <p>Exporte um arquivo JSON com todos os campos da calculadora e restaure quando precisar.</p>
              <div class="actions">
                <button class="btn" id="exportBackupBtn">Exportar backup</button>
                <label class="import-label">
                  Importar backup
                  <input id="importBackupInput" type="file" accept=".json,application/json">
                </label>
              </div>
            </div>

            <div class="settings-card">
              <h3>Instalar como app</h3>
              <p>Esta versão está preparada para virar PWA. Ao finalizar o design, gere o pacote com manifest, service worker e ícones.</p>
              <div class="app-checklist">
                <span>✅ HTML único funcional</span>
                <span>✅ Dados locais</span>
                <span>✅ Backup/importação</span>
                <span>⏳ Manifest + Service Worker no pacote final</span>
              </div>
            </div>

            <div class="settings-card">
              <h3>Proposta para família</h3>
             
---
condary" id="clearStorageBtn">Limpar dados salvos</button>
            </div>

            <div class="settings-card">
              <h3>Backup</h3>
              <p>Exporte um arquivo JSON com todos os campos da calculadora e restaure quando precisar.</p>
              <div class="actions">
                <button class="btn" id="exportBackupBtn">Exportar backup</button>
                <label class="import-label">
                  Importar backup
                  <input id="importBackupInput" type="file" accept=".json,application/json">
                </label>
              </div>
            </div>

            <div class="settings-card">
              <h3>Instalar como app</h3>
              <p>Esta versão está preparada para virar PWA. Ao finalizar o design, gere o pacote com manifest, service worker e ícones.</p>
              <div class="app-checklist">
                <span>✅ HTML único funcional</span>
                <span>✅ Dados locais</span>
                <span>✅ Backup/importação</span>
                <span>⏳ Manifest + Service Worker no pacote final</span>
              </div>
            </div>

            <div class="settings-card">
              <h3>Proposta para família</h3>
              <p>Use o modo ap
---
ass="panel result-card">
          <div class="section-title">
            <h2>Status do sistema</h2>
            <span class="pill">Resumo</span>
          </div>
          <div class="summary" id="settingsSummary"></div>
          <div class="insight">
            <strong>Recomendação:</strong><br>
            Depois de validar os valores reais, exporte um backup e guarde como referência oficial da tabela da escola.
          </div>
        </aside>
      </div>
    </section>

    <section id="relatorios" class="view">
      <div class="panel">
        <div class="section-title">
          <h2>Relatórios e orçamentos para impressão</h2>
          <span class="pill">PDF / Impressora</span>
        </div>

        <div class="report-toolbar">
          <button class="btn" id="printAllBtn">Imprimir relatório completo</button>
          <button class="btn secondary" id="printBudgetBtn">Imprimir orçamento da simulação atual</button>
          <button class="btn secondary" id="copyBudgetBtn">Copiar orçamento</button>
        </div>

        <div class="fields no-print" style="margin-bottom:18px">
          <div class="field">
            <label>Nome da escola</label>
            <input id="schoolName" type="text" value="Escola de Edu
---
     clearTimeout(saveTimer);
      saveTimer = setTimeout(saveState, 250);
    }

    function loadState(){
      try{
        const raw = localStorage.getItem('novaSabaraCalculadoraState');
        if(raw) applyState(JSON.parse(raw));
      }catch(e){
        console.warn('Não foi possível carregar dados salvos:', e);
      }
    }

    function exportBackup(){
      const blob = new Blob([JSON.stringify(collectState(), null, 2)], {type:'application/json'});
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url;
      a.download = `backup-calculadora-nova-sabara-${new Date().toISOString().slice(0,10)}.json`;
      document.body.appendChild(a);
      a.click();
      a.remove();
      URL.revokeObjectURL(url);
      showToast('Backup exportado!');
    }

    function importBackup(file){
      if(!file) return;
      const reader = new FileReader();
      reader.onload = () => {
        try{
          const state = JSON.parse(reader.result);
          applyState(state);
          saveState();
          showToast('Backup importado!');
        }catch(e){
          alert('Não foi possível importar este backup. Verifique se o arquivo é JSON válido.');
        }
      };
      read
---
ation/json'});
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url;
      a.download = `backup-calculadora-nova-sabara-${new Date().toISOString().slice(0,10)}.json`;
      document.body.appendChild(a);
      a.click();
      a.remove();
      URL.revokeObjectURL(url);
      showToast('Backup exportado!');
    }

    function importBackup(file){
      if(!file) return;
      const reader = new FileReader();
      reader.onload = () => {
        try{
          const state = JSON.parse(reader.result);
          applyState(state);
          saveState();
          showToast('Backup importado!');
        }catch(e){
          alert('Não foi possível importar este backup. Verifique se o arquivo é JSON válido.');
        }
      };
      reader.readAsText(file);
    }

    function clearSavedData(){
      if(confirm('Deseja limpar os dados salvos neste navegador?')){
        localStorage.removeItem('novaSabaraCalculadoraState');
        showToast('Dados locais limpos!');
      }
    }

    function buildSmartAlerts(){
      const alerts = [];
      const calc = getCommercialCalc();
      const form = getFormationCalc();
      let precision = null;
      try{ precision = getPrecisionD
---
 document.getElementById('resetPrecisionBtn');
    if(resetPrecisionBtn){
      resetPrecisionBtn.addEventListener('click', resetPrecision);
    }

    const addCustomActivityBtn = document.getElementById('addCustomActivityBtn');
    if(addCustomActivityBtn){
      addCustomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getElementById('importBackupInput');
    if(importBackupInput){
      importBackupInput.addEventListener('change', e => importBackup(e.target.files[0]));
    }

    const clearStorageBtn = document.getElementById('clearStorageBtn');
    if(clearStorageBtn){
      clearStorageBtn.addEventListener('click', clearSavedData);
    }

    const copyAlertsBtn = document.getElementById('copyAlertsBtn');
    if(copyAlertsBtn){
      copyAlertsBtn.addEventListener('click', copyAlerts);
    }

    const goPrecisionFromAlerts = document.getElementById('goPrecisionFromAlerts');
    if(goPrecisionFromAlerts){
      goPrecisionFromAlerts.addEventListener('click', () => {
        setActiveView('precisao');
     
---
');
    if(resetPrecisionBtn){
      resetPrecisionBtn.addEventListener('click', resetPrecision);
    }

    const addCustomActivityBtn = document.getElementById('addCustomActivityBtn');
    if(addCustomActivityBtn){
      addCustomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getElementById('importBackupInput');
    if(importBackupInput){
      importBackupInput.addEventListener('change', e => importBackup(e.target.files[0]));
    }

    const clearStorageBtn = document.getElementById('clearStorageBtn');
    if(clearStorageBtn){
      clearStorageBtn.addEventListener('click', clearSavedData);
    }

    const copyAlertsBtn = document.getElementById('copyAlertsBtn');
    if(copyAlertsBtn){
      copyAlertsBtn.addEventListener('click', copyAlerts);
    }

    const goPrecisionFromAlerts = document.getElementById('goPrecisionFromAlerts');
    if(goPrecisionFromAlerts){
      goPrecisionFromAlerts.addEventListener('click', () => {
        setActiveView('precisao');
        updateAll();
      });
    }

    const 
---
tn){
      resetPrecisionBtn.addEventListener('click', resetPrecision);
    }

    const addCustomActivityBtn = document.getElementById('addCustomActivityBtn');
    if(addCustomActivityBtn){
      addCustomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getElementById('importBackupInput');
    if(importBackupInput){
      importBackupInput.addEventListener('change', e => importBackup(e.target.files[0]));
    }

    const clearStorageBtn = document.getElementById('clearStorageBtn');
    if(clearStorageBtn){
      clearStorageBtn.addEventListener('click', clearSavedData);
    }

    const copyAlertsBtn = document.getElementById('copyAlertsBtn');
    if(copyAlertsBtn){
      copyAlertsBtn.addEventListener('click', copyAlerts);
    }

    const goPrecisionFromAlerts = document.getElementById('goPrecisionFromAlerts');
    if(goPrecisionFromAlerts){
      goPrecisionFromAlerts.addEventListener('click', () => {
        setActiveView('precisao');
        updateAll();
      });
    }

    const presentationToBudget = doc
---
nBtn.addEventListener('click', resetPrecision);
    }

    const addCustomActivityBtn = document.getElementById('addCustomActivityBtn');
    if(addCustomActivityBtn){
      addCustomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getElementById('importBackupInput');
    if(importBackupInput){
      importBackupInput.addEventListener('change', e => importBackup(e.target.files[0]));
    }

    const clearStorageBtn = document.getElementById('clearStorageBtn');
    if(clearStorageBtn){
      clearStorageBtn.addEventListener('click', clearSavedData);
    }

    const copyAlertsBtn = document.getElementById('copyAlertsBtn');
    if(copyAlertsBtn){
      copyAlertsBtn.addEventListener('click', copyAlerts);
    }

    const goPrecisionFromAlerts = document.getElementById('goPrecisionFromAlerts');
    if(goPrecisionFromAlerts){
      goPrecisionFromAlerts.addEventListener('click', () => {
        setActiveView('precisao');
        updateAll();
      });
    }

    const presentationToBudget = document.getElementById('pr
---
ion);
    }

    const addCustomActivityBtn = document.getElementById('addCustomActivityBtn');
    if(addCustomActivityBtn){
      addCustomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getElementById('importBackupInput');
    if(importBackupInput){
      importBackupInput.addEventListener('change', e => importBackup(e.target.files[0]));
    }

    const clearStorageBtn = document.getElementById('clearStorageBtn');
    if(clearStorageBtn){
      clearStorageBtn.addEventListener('click', clearSavedData);
    }

    const copyAlertsBtn = document.getElementById('copyAlertsBtn');
    if(copyAlertsBtn){
      copyAlertsBtn.addEventListener('click', copyAlerts);
    }

    const goPrecisionFromAlerts = document.getElementById('goPrecisionFromAlerts');
    if(goPrecisionFromAlerts){
      goPrecisionFromAlerts.addEventListener('click', () => {
        setActiveView('precisao');
        updateAll();
      });
    }

    const presentationToBudget = document.getElementById('presentationToBudget');
    if(presentationT
---

      cancelSnapshotBtn2.addEventListener('click', ()=>{ if(snapshotModal) snapshotModal.style.display='none'; });
    }
    if(clearHistoryBtn2){
      clearHistoryBtn2.addEventListener('click', ()=>{
        if(confirm('Deseja limpar todo o histórico?')){ saveHistory([]); renderHistory(); showToast('Histórico limpo!'); }
      });
    }

    // Export CSV
    function exportCSV(){
      let rows = [];
      try{ rows = getComparisonData(); }catch(e){ showToast('Configure os dados primeiro!'); return; }
      const ipca = getIPCARate();
      const headers = ['Turma','Idade','Horario','Preco Atual','Recomendado','Diferenca','Diferenca %','Status','Ref IPCA'];
      const lines = [headers.join(';')];
      rows.forEach(r => {
        const ipcaRef = (r.current*(1+ipca)).toFixed(2).replace('.',',');
        lines.push([r.turma,r.idade,r.hour+'h',
          r.current.toFixed(2).replace('.',','),
          r.recommended.toFixed(2).replace('.',','),
          r.diff.toFixed(2).replace('.',','),
          r.diffPercent.toFixed(1).replace('.',',')+'%',
          r.status.label, ipcaRef].join(';'));
      });
      const blob = new Blob(['\uFEFF'+lines.join('\n')],{type:'text/csv;charset=utf-8;'});
      const url = URL.createObjectURL(
---
2.addEventListener('click', ()=>{ if(snapshotModal) snapshotModal.style.display='none'; });
    }
    if(clearHistoryBtn2){
      clearHistoryBtn2.addEventListener('click', ()=>{
        if(confirm('Deseja limpar todo o histórico?')){ saveHistory([]); renderHistory(); showToast('Histórico limpo!'); }
      });
    }

    // Export CSV
    function exportCSV(){
      let rows = [];
      try{ rows = getComparisonData(); }catch(e){ showToast('Configure os dados primeiro!'); return; }
      const ipca = getIPCARate();
      const headers = ['Turma','Idade','Horario','Preco Atual','Recomendado','Diferenca','Diferenca %','Status','Ref IPCA'];
      const lines = [headers.join(';')];
      rows.forEach(r => {
        const ipcaRef = (r.current*(1+ipca)).toFixed(2).replace('.',',');
        lines.push([r.turma,r.idade,r.hour+'h',
          r.current.toFixed(2).replace('.',','),
          r.recommended.toFixed(2).replace('.',','),
          r.diff.toFixed(2).replace('.',','),
          r.diffPercent.toFixed(1).replace('.',',')+'%',
          r.status.label, ipcaRef].join(';'));
      });
      const blob = new Blob(['\uFEFF'+lines.join('\n')],{type:'text/csv;charset=utf-8;'});
      const url = URL.createObjectURL(blob);
      const a = d
---
EFF'+lines.join('\n')],{type:'text/csv;charset=utf-8;'});
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href=url; a.download='mensalidades-'+new Date().toISOString().slice(0,10)+'.csv';
      document.body.appendChild(a); a.click(); a.remove();
      URL.revokeObjectURL(url);
      showToast('CSV exportado!');
    }

    const exportCsvBtn2 = document.getElementById('exportCsvBtn');
    if(exportCsvBtn2){ exportCsvBtn2.addEventListener('click', exportCSV); }

    // Hook into side navigation for new views
    document.querySelectorAll('.side-link').forEach(link => {
      link.addEventListener('click', () => {
        const view = link.dataset.view;
        if(view === 'historico') setTimeout(renderHistory, 50);
        if(view === 'dashboard') setTimeout(updateAnnualProjection, 50);
      });
    });

    // Initial calls (deferred to after DOM + data ready)
    setTimeout(()=>{ renderHistory(); updateAnnualProjection(); updateIPCABanner(); }, 300);

</script>

<script>
  // Registro do Service Worker: necessário para instalação como app/PWA
  if ('serviceWorker' in navigator) {
    window.addEventListener('load', function () {
      navigator.serviceWorker.register('./sw.js', {
---
ext/csv;charset=utf-8;'});
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href=url; a.download='mensalidades-'+new Date().toISOString().slice(0,10)+'.csv';
      document.body.appendChild(a); a.click(); a.remove();
      URL.revokeObjectURL(url);
      showToast('CSV exportado!');
    }

    const exportCsvBtn2 = document.getElementById('exportCsvBtn');
    if(exportCsvBtn2){ exportCsvBtn2.addEventListener('click', exportCSV); }

    // Hook into side navigation for new views
    document.querySelectorAll('.side-link').forEach(link => {
      link.addEventListener('click', () => {
        const view = link.dataset.view;
        if(view === 'historico') setTimeout(renderHistory, 50);
        if(view === 'dashboard') setTimeout(updateAnnualProjection, 50);
      });
    });

    // Initial calls (deferred to after DOM + data ready)
    setTimeout(()=>{ renderHistory(); updateAnnualProjection(); updateIPCABanner(); }, 300);

</script>

<script>
  // Registro do Service Worker: necessário para instalação como app/PWA
  if ('serviceWorker' in navigator) {
    window.addEventListener('load', function () {
      navigator.serviceWorker.register('./sw.js', { scope: './' })
        .then(f
---
l = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href=url; a.download='mensalidades-'+new Date().toISOString().slice(0,10)+'.csv';
      document.body.appendChild(a); a.click(); a.remove();
      URL.revokeObjectURL(url);
      showToast('CSV exportado!');
    }

    const exportCsvBtn2 = document.getElementById('exportCsvBtn');
    if(exportCsvBtn2){ exportCsvBtn2.addEventListener('click', exportCSV); }

    // Hook into side navigation for new views
    document.querySelectorAll('.side-link').forEach(link => {
      link.addEventListener('click', () => {
        const view = link.dataset.view;
        if(view === 'historico') setTimeout(renderHistory, 50);
        if(view === 'dashboard') setTimeout(updateAnnualProjection, 50);
      });
    });

    // Initial calls (deferred to after DOM + data ready)
    setTimeout(()=>{ renderHistory(); updateAnnualProjection(); updateIPCABanner(); }, 300);

</script>

<script>
  // Registro do Service Worker: necessário para instalação como app/PWA
  if ('serviceWorker' in navigator) {
    window.addEventListener('load', function () {
      navigator.serviceWorker.register('./sw.js', { scope: './' })
        .then(function () { console.log('[NovaSabara] Se
---
(blob);
      const a = document.createElement('a');
      a.href=url; a.download='mensalidades-'+new Date().toISOString().slice(0,10)+'.csv';
      document.body.appendChild(a); a.click(); a.remove();
      URL.revokeObjectURL(url);
      showToast('CSV exportado!');
    }

    const exportCsvBtn2 = document.getElementById('exportCsvBtn');
    if(exportCsvBtn2){ exportCsvBtn2.addEventListener('click', exportCSV); }

    // Hook into side navigation for new views
    document.querySelectorAll('.side-link').forEach(link => {
      link.addEventListener('click', () => {
        const view = link.dataset.view;
        if(view === 'historico') setTimeout(renderHistory, 50);
        if(view === 'dashboard') setTimeout(updateAnnualProjection, 50);
      });
    });

    // Initial calls (deferred to after DOM + data ready)
    setTimeout(()=>{ renderHistory(); updateAnnualProjection(); updateIPCABanner(); }, 300);

</script>

<script>
  // Registro do Service Worker: necessário para instalação como app/PWA
  if ('serviceWorker' in navigator) {
    window.addEventListener('load', function () {
      navigator.serviceWorker.register('./sw.js', { scope: './' })
        .then(function () { console.log('[NovaSabara] Service Worker registrado
---
nst a = document.createElement('a');
      a.href=url; a.download='mensalidades-'+new Date().toISOString().slice(0,10)+'.csv';
      document.body.appendChild(a); a.click(); a.remove();
      URL.revokeObjectURL(url);
      showToast('CSV exportado!');
    }

    const exportCsvBtn2 = document.getElementById('exportCsvBtn');
    if(exportCsvBtn2){ exportCsvBtn2.addEventListener('click', exportCSV); }

    // Hook into side navigation for new views
    document.querySelectorAll('.side-link').forEach(link => {
      link.addEventListener('click', () => {
        const view = link.dataset.view;
        if(view === 'historico') setTimeout(renderHistory, 50);
        if(view === 'dashboard') setTimeout(updateAnnualProjection, 50);
      });
    });

    // Initial calls (deferred to after DOM + data ready)
    setTimeout(()=>{ renderHistory(); updateAnnualProjection(); updateIPCABanner(); }, 300);

</script>

<script>
  // Registro do Service Worker: necessário para instalação como app/PWA
  if ('serviceWorker' in navigator) {
    window.addEventListener('load', function () {
      navigator.serviceWorker.register('./sw.js', { scope: './' })
        .then(function () { console.log('[NovaSabara] Service Worker registrado.'); })
        
---
   a.href=url; a.download='mensalidades-'+new Date().toISOString().slice(0,10)+'.csv';
      document.body.appendChild(a); a.click(); a.remove();
      URL.revokeObjectURL(url);
      showToast('CSV exportado!');
    }

    const exportCsvBtn2 = document.getElementById('exportCsvBtn');
    if(exportCsvBtn2){ exportCsvBtn2.addEventListener('click', exportCSV); }

    // Hook into side navigation for new views
    document.querySelectorAll('.side-link').forEach(link => {
      link.addEventListener('click', () => {
        const view = link.dataset.view;
        if(view === 'historico') setTimeout(renderHistory, 50);
        if(view === 'dashboard') setTimeout(updateAnnualProjection, 50);
      });
    });

    // Initial calls (deferred to after DOM + data ready)
    setTimeout(()=>{ renderHistory(); updateAnnualProjection(); updateIPCABanner(); }, 300);

</script>

<script>
  // Registro do Service Worker: necessário para instalação como app/PWA
  if ('serviceWorker' in navigator) {
    window.addEventListener('load', function () {
      navigator.serviceWorker.register('./sw.js', { scope: './' })
        .then(function () { console.log('[NovaSabara] Service Worker registrado.'); })
        .catch(function (err) { console.warn('[N
---

### reset
/small>
            <div class="total" id="total">R$ 0,00</div>
          </div>

          <div class="summary" id="summary"></div>

          <div class="proposal" id="proposal"></div>

          <div class="actions">
            <button class="btn" id="copyBtn">Copiar proposta para WhatsApp</button>
            <button class="btn secondary" id="resetBtn">Limpar simulação</button>
          </div>
        </aside>
      </div>
    </section>

    <section id="formacao" class="view">
      <div class="kpis">
        <div class="kpi">
          <small>Mensalidade mínima</small>
          <strong id="kpiMinima">R$ 0,00</strong>
          <em>Valor para cobrir custos</em>
        </div>
        <div class="kpi">
          <small>Mensalidade segura</small>
          <strong id="kpiSegura">R$ 0,00</strong>
          <em>Com inadimplência e reserva</em>
        </div>
        <div class="kpi">
          <small>Mensalidade ideal</small>
          <strong id="kpiIdeal">R$ 0,00</strong>
          <em>Com margem de lucro</em>
        </div>
      </div>

      <div class="grid">
        <div>
          <div class="grid-2">
            <div class="panel">
              <div class="section-title">
                <h2>Custos fixos mensais</h2
---
         <div class="total" id="seguraMain">R$ 0,00</div>
          </div>

          <div class="summary" id="formacaoSummary"></div>

          <div class="insight" id="insight"></div>

          <div class="actions">
            <button class="btn" id="copyGestaoBtn">Copiar resumo gerencial</button>
            <button class="btn secondary" id="resetGestaoBtn">Restaurar exemplo</button>
          </div>
        </aside>
      </div>
    </section>

    <section id="tabela" class="view">
      <div class="grid">
        <div class="panel">
          <div class="section-title">
            <h2>Tabela recomendada por turma e horário</h2>
            <span class="pill">Baseada na formação de preço</span>
          </div>
          <div id="tablePrices"></div>
        </div>

        <aside class="panel result-card">
          <div class="section-title">
            <h2>Como interpretar</h2>
            <span class="pill">Uso prático</span>
          </div>
          <div class="total-box ideal">
            <small>Referência principal</small>
            <div class="total" id="idealMain">R$ 0,00</div>
          </div>
          <div class="proposal">
            <strong>Leitura recomendada:</strong><br>
            Use a mensalidad
---
Summary"></div>

          <div class="insight" id="compareInsight"></div>

          <div class="actions">
            <button class="btn" id="copyCompareBtn">Copiar diagnóstico</button>
          <button class="btn secondary" id="exportCsvBtn" style="margin-top:10px">Exportar tabela como CSV</button>
            <button class="btn secondary" id="resetCurrentPricesBtn">Preencher exemplo atual</button>
          </div>
        </aside>
      </div>

      <div class="panel" style="margin-top:18px">
        <div class="section-title">
          <h2>Tabela comparativa</h2>
          <span class="pill">Atual x recomendado</span>
        </div>
        <div id="comparisonTable"></div>
      </div>
    </section>


    <section id="precisao" class="view">
      <div class="kpis">
        <div class="kpi">
          <small>Ponto de equilíbrio</small>
          <strong id="breakEvenStudents">0 alunos</strong>
          <em>pagantes para empatar</em>
        </div>
        <div class="kpi">
          <small>Ticket médio atual</small>
          <strong id="avgTicketCurrent">R$ 0,00</strong>
          <em>com preços praticados</em>
        </div>
        <div class="kpi">
          <small>Ocupação geral</small>
          <strong id="overall
---
="precisionMonthlyResult">R$ 0,00</div>
          </div>

          <div class="summary" id="precisionSummary"></div>

          <div class="insight" id="precisionInsight"></div>

          <div class="actions">
            <button class="btn" id="copyPrecisionBtn">Copiar diagnóstico gerencial</button>
            <button class="btn secondary" id="resetPrecisionBtn">Restaurar exemplo</button>
          </div>
        </aside>
      </div>

      <div class="panel" style="margin-top:18px">
        <div class="section-title">
          <h2>Impacto do reajuste por turma</h2>
          <span class="pill">Atual x novo</span>
        </div>
        <div id="adjustmentTable"></div>
      </div>
    </section>


    <section id="alertas" class="view">
      <div class="grid">
        <div class="panel">
          <div class="section-title">
            <h2>Alertas inteligentes</h2>
            <span class="pill">Leitura automática</span>
          </div>
          <p class="helper-text">
            O sistema cruza mensalidade, custos, descontos, ocupação, equipe, alimentação, preços praticados e ponto de equilíbrio para destacar riscos e oportunidades.
          </p>
          <div class="smart-alert-list" id="smartAlertList"></div>
    
---
idade mínima: ${money.format(c.minima)}
Mensalidade segura: ${money.format(c.segura)}
Mensalidade ideal: ${money.format(c.ideal)}

Receita ideal estimada: ${money.format(c.receitaIdeal)}
Resultado ideal estimado: ${money.format(c.lucroIdeal)}`;

      navigator.clipboard.writeText(text).then(() => showToast('Resumo copiado!'));
    }

    function resetCommercial(){
      selectedTurma = 'bercario';
      selectedHour = 6;
      document.getElementById('alimentacao').checked = true;
      document.getElementById('material').checked = false;
      document.getElementById('transporte').checked = false;
      document.getElementById('estendido').checked = false;
      document.getElementById('matricula').value = 0;
      document.getElementById('desconto').value = 0;
      document.getElementById('materialInicial').value = 180;
      document.getElementById('uniformeInicial').value = 0;
      document.getElementById('agendaApp').value = 50;
      document.getElementById('eventosProjetos').value = 0;
      customActivities = [];
      renderCustomActivities();
      document.getElementById('diasLetivos').value = 22;
      document.getElementById('alimPerdas').value = 8;
      document.getElementById('alim_bercario').value = 11;
      
---
ument.getElementById('act_judo_matricula').value = 40;
      document.getElementById('act_bale_mensal').value = 160;
      document.getElementById('act_bale_matricula').value = 45;
      document.getElementById('act_teatro_mensal').value = 140;
      document.getElementById('act_teatro_matricula').value = 35;
      updateAll();
    }

    function resetFormation(){
      const defaults = {
        folha:28000, impostos:6500, aluguel:7000, utilidades:3200, manutencao:2500, administrativo:2200, marketing:1200, outros:1800
      };
      Object.entries(defaults).forEach(([k,v]) => document.querySelector(`[data-cost="${k}"]`).value = v);

      const vars = { alimentacao:180, material:90, higiene:55, atividades:45 };
      Object.entries(vars).forEach(([k,v]) => document.querySelector(`[data-var="${k}"]`).value = v);

      document.getElementById('alunos').value = 60;
      document.getElementById('pagantes').value = 57;
      document.getElementById('inadimplencia').value = 5;
      document.getElementById('reserva').value = 5;
      document.getElementById('margem').value = 20;
      document.getElementById('arredondar').value = 50;

      Object.entries(commercialTable).forEach(([key, t]) => {
        const el = document.querySele
---
gth === 0 ? 'A tabela atual está saudável em relação à mensalidade segura.' : belowRows.length <= 5 ? 'Há ajustes pontuais recomendados por turma/horário.' : 'Há defasagem relevante. Recomenda-se revisar a tabela com transição gradual.'}`;

      navigator.clipboard.writeText(text).then(() => showToast('Comparativo copiado!'));
    }

    function resetCurrentPrices(){
      Object.entries(currentPriceDefaults).forEach(([key, values]) => {
        [6,8,10,12].forEach(hour => {
          const el = document.querySelector(`[data-current-price="${key}_${hour}"]`);
          if(el) el.value = values[hour];
        });
      });
      updateAll();
    }


    const precisionDefaults = {
      bercario:{capacity:12,enrolled:8,payers:7,discount:8,teacher:1,assistant:2,teacherSalary:3200,assistantSalary:1900,charges:45},
      maternal1:{capacity:16,enrolled:13,payers:12,discount:7,teacher:1,assistant:1,teacherSalary:3100,assistantSalary:1850,charges:45},
      maternal2:{capacity:18,enrolled:15,payers:14,discount:6,teacher:1,assistant:1,teacherSalary:3000,assistantSalary:1800,charges:45},
      jardim1:{capacity:20,enrolled:17,payers:16,discount:5,teacher:1,assistant:1,teacherSalary:3000,assistantSalary:1750,charges:45},
      jardim2:{c
---
estimada no vermelho. Priorizar reajuste, ocupação e revisão de descontos.' : d.occupancy < .75 ? 'Ocupação abaixo do ideal. Trabalhar captação e rematrícula.' : 'Operação em faixa mais saudável. Corrigir defasagens pontuais.'}`;

      navigator.clipboard.writeText(text).then(() => showToast('Diagnóstico gerencial copiado!'));
    }

    function resetPrecision(){
      Object.entries(precisionDefaults).forEach(([key, d]) => {
        Object.entries(d).forEach(([field, value]) => {
          const el = document.querySelector(`[data-precision="${key}_${field}"]`);
          if(el) el.value = value;
        });
      });
      const adj = document.getElementById('adjustPercent');
      const mode = document.getElementById('adjustMode');
      const profit = document.getElementById('profitGoal');
      const ticketMode = document.getElementById('goalTicketMode');
      if(adj) adj.value = 8;
      if(mode) mode.value = 'all';
      if(profit) profit.value = 10000;
      if(ticketMode) ticketMode.value = 'current';
      updateAll();
    }


    let customActivities = [];
    let isRestoringState = false;
    let saveTimer = null;

    function getInitialCosts(){
      return n('materialInicial') + n('uniformeInicial') + n('agendaApp
---
orts');
    if(dashGoReportsBtn){
      dashGoReportsBtn.addEventListener('click', () => {
        setActiveView('relatorios');
        updateAll();
      });
    }

    const copyCompareBtn = document.getElementById('copyCompareBtn');
    if(copyCompareBtn){
      copyCompareBtn.addEventListener('click', copyComparisonDiagnosis);
    }

    const resetCurrentPricesBtn = document.getElementById('resetCurrentPricesBtn');
    if(resetCurrentPricesBtn){
      resetCurrentPricesBtn.addEventListener('click', resetCurrentPrices);
    }

    const copyPrecisionBtn = document.getElementById('copyPrecisionBtn');
    if(copyPrecisionBtn){
      copyPrecisionBtn.addEventListener('click', copyPrecisionDiagnosis);
    }

    const resetPrecisionBtn = document.getElementById('resetPrecisionBtn');
    if(resetPrecisionBtn){
      resetPrecisionBtn.addEventListener('click', resetPrecision);
    }

    const addCustomActivityBtn = document.getElementById('addCustomActivityBtn');
    if(addCustomActivityBtn){
      addCustomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
   
---
ortsBtn.addEventListener('click', () => {
        setActiveView('relatorios');
        updateAll();
      });
    }

    const copyCompareBtn = document.getElementById('copyCompareBtn');
    if(copyCompareBtn){
      copyCompareBtn.addEventListener('click', copyComparisonDiagnosis);
    }

    const resetCurrentPricesBtn = document.getElementById('resetCurrentPricesBtn');
    if(resetCurrentPricesBtn){
      resetCurrentPricesBtn.addEventListener('click', resetCurrentPrices);
    }

    const copyPrecisionBtn = document.getElementById('copyPrecisionBtn');
    if(copyPrecisionBtn){
      copyPrecisionBtn.addEventListener('click', copyPrecisionDiagnosis);
    }

    const resetPrecisionBtn = document.getElementById('resetPrecisionBtn');
    if(resetPrecisionBtn){
      resetPrecisionBtn.addEventListener('click', resetPrecision);
    }

    const addCustomActivityBtn = document.getElementById('addCustomActivityBtn');
    if(addCustomActivityBtn){
      addCustomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getEle
---
, () => {
        setActiveView('relatorios');
        updateAll();
      });
    }

    const copyCompareBtn = document.getElementById('copyCompareBtn');
    if(copyCompareBtn){
      copyCompareBtn.addEventListener('click', copyComparisonDiagnosis);
    }

    const resetCurrentPricesBtn = document.getElementById('resetCurrentPricesBtn');
    if(resetCurrentPricesBtn){
      resetCurrentPricesBtn.addEventListener('click', resetCurrentPrices);
    }

    const copyPrecisionBtn = document.getElementById('copyPrecisionBtn');
    if(copyPrecisionBtn){
      copyPrecisionBtn.addEventListener('click', copyPrecisionDiagnosis);
    }

    const resetPrecisionBtn = document.getElementById('resetPrecisionBtn');
    if(resetPrecisionBtn){
      resetPrecisionBtn.addEventListener('click', resetPrecision);
    }

    const addCustomActivityBtn = document.getElementById('addCustomActivityBtn');
    if(addCustomActivityBtn){
      addCustomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getElementById('importBackupInput');
 
---
w('relatorios');
        updateAll();
      });
    }

    const copyCompareBtn = document.getElementById('copyCompareBtn');
    if(copyCompareBtn){
      copyCompareBtn.addEventListener('click', copyComparisonDiagnosis);
    }

    const resetCurrentPricesBtn = document.getElementById('resetCurrentPricesBtn');
    if(resetCurrentPricesBtn){
      resetCurrentPricesBtn.addEventListener('click', resetCurrentPrices);
    }

    const copyPrecisionBtn = document.getElementById('copyPrecisionBtn');
    if(copyPrecisionBtn){
      copyPrecisionBtn.addEventListener('click', copyPrecisionDiagnosis);
    }

    const resetPrecisionBtn = document.getElementById('resetPrecisionBtn');
    if(resetPrecisionBtn){
      resetPrecisionBtn.addEventListener('click', resetPrecision);
    }

    const addCustomActivityBtn = document.getElementById('addCustomActivityBtn');
    if(addCustomActivityBtn){
      addCustomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getElementById('importBackupInput');
    if(importBackupInput){
    
---
    }

    const copyCompareBtn = document.getElementById('copyCompareBtn');
    if(copyCompareBtn){
      copyCompareBtn.addEventListener('click', copyComparisonDiagnosis);
    }

    const resetCurrentPricesBtn = document.getElementById('resetCurrentPricesBtn');
    if(resetCurrentPricesBtn){
      resetCurrentPricesBtn.addEventListener('click', resetCurrentPrices);
    }

    const copyPrecisionBtn = document.getElementById('copyPrecisionBtn');
    if(copyPrecisionBtn){
      copyPrecisionBtn.addEventListener('click', copyPrecisionDiagnosis);
    }

    const resetPrecisionBtn = document.getElementById('resetPrecisionBtn');
    if(resetPrecisionBtn){
      resetPrecisionBtn.addEventListener('click', resetPrecision);
    }

    const addCustomActivityBtn = document.getElementById('addCustomActivityBtn');
    if(addCustomActivityBtn){
      addCustomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getElementById('importBackupInput');
    if(importBackupInput){
      importBackupInput.addEventListener('change', e
---
ment.getElementById('resetCurrentPricesBtn');
    if(resetCurrentPricesBtn){
      resetCurrentPricesBtn.addEventListener('click', resetCurrentPrices);
    }

    const copyPrecisionBtn = document.getElementById('copyPrecisionBtn');
    if(copyPrecisionBtn){
      copyPrecisionBtn.addEventListener('click', copyPrecisionDiagnosis);
    }

    const resetPrecisionBtn = document.getElementById('resetPrecisionBtn');
    if(resetPrecisionBtn){
      resetPrecisionBtn.addEventListener('click', resetPrecision);
    }

    const addCustomActivityBtn = document.getElementById('addCustomActivityBtn');
    if(addCustomActivityBtn){
      addCustomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getElementById('importBackupInput');
    if(importBackupInput){
      importBackupInput.addEventListener('change', e => importBackup(e.target.files[0]));
    }

    const clearStorageBtn = document.getElementById('clearStorageBtn');
    if(clearStorageBtn){
      clearStorageBtn.addEventListener('click', clearSavedData);
    }

    c
---

    if(resetCurrentPricesBtn){
      resetCurrentPricesBtn.addEventListener('click', resetCurrentPrices);
    }

    const copyPrecisionBtn = document.getElementById('copyPrecisionBtn');
    if(copyPrecisionBtn){
      copyPrecisionBtn.addEventListener('click', copyPrecisionDiagnosis);
    }

    const resetPrecisionBtn = document.getElementById('resetPrecisionBtn');
    if(resetPrecisionBtn){
      resetPrecisionBtn.addEventListener('click', resetPrecision);
    }

    const addCustomActivityBtn = document.getElementById('addCustomActivityBtn');
    if(addCustomActivityBtn){
      addCustomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getElementById('importBackupInput');
    if(importBackupInput){
      importBackupInput.addEventListener('change', e => importBackup(e.target.files[0]));
    }

    const clearStorageBtn = document.getElementById('clearStorageBtn');
    if(clearStorageBtn){
      clearStorageBtn.addEventListener('click', clearSavedData);
    }

    const copyAlertsBtn = document.getElementById(
---
n){
      resetCurrentPricesBtn.addEventListener('click', resetCurrentPrices);
    }

    const copyPrecisionBtn = document.getElementById('copyPrecisionBtn');
    if(copyPrecisionBtn){
      copyPrecisionBtn.addEventListener('click', copyPrecisionDiagnosis);
    }

    const resetPrecisionBtn = document.getElementById('resetPrecisionBtn');
    if(resetPrecisionBtn){
      resetPrecisionBtn.addEventListener('click', resetPrecision);
    }

    const addCustomActivityBtn = document.getElementById('addCustomActivityBtn');
    if(addCustomActivityBtn){
      addCustomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getElementById('importBackupInput');
    if(importBackupInput){
      importBackupInput.addEventListener('change', e => importBackup(e.target.files[0]));
    }

    const clearStorageBtn = document.getElementById('clearStorageBtn');
    if(clearStorageBtn){
      clearStorageBtn.addEventListener('click', clearSavedData);
    }

    const copyAlertsBtn = document.getElementById('copyAlertsBtn');
    if(cop
---
esBtn.addEventListener('click', resetCurrentPrices);
    }

    const copyPrecisionBtn = document.getElementById('copyPrecisionBtn');
    if(copyPrecisionBtn){
      copyPrecisionBtn.addEventListener('click', copyPrecisionDiagnosis);
    }

    const resetPrecisionBtn = document.getElementById('resetPrecisionBtn');
    if(resetPrecisionBtn){
      resetPrecisionBtn.addEventListener('click', resetPrecision);
    }

    const addCustomActivityBtn = document.getElementById('addCustomActivityBtn');
    if(addCustomActivityBtn){
      addCustomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getElementById('importBackupInput');
    if(importBackupInput){
      importBackupInput.addEventListener('change', e => importBackup(e.target.files[0]));
    }

    const clearStorageBtn = document.getElementById('clearStorageBtn');
    if(clearStorageBtn){
      clearStorageBtn.addEventListener('click', clearSavedData);
    }

    const copyAlertsBtn = document.getElementById('copyAlertsBtn');
    if(copyAlertsBtn){
      copyAle
---
Prices);
    }

    const copyPrecisionBtn = document.getElementById('copyPrecisionBtn');
    if(copyPrecisionBtn){
      copyPrecisionBtn.addEventListener('click', copyPrecisionDiagnosis);
    }

    const resetPrecisionBtn = document.getElementById('resetPrecisionBtn');
    if(resetPrecisionBtn){
      resetPrecisionBtn.addEventListener('click', resetPrecision);
    }

    const addCustomActivityBtn = document.getElementById('addCustomActivityBtn');
    if(addCustomActivityBtn){
      addCustomActivityBtn.addEventListener('click', addCustomActivity);
    }

    const exportBackupBtn = document.getElementById('exportBackupBtn');
    if(exportBackupBtn){
      exportBackupBtn.addEventListener('click', exportBackup);
    }

    const importBackupInput = document.getElementById('importBackupInput');
    if(importBackupInput){
      importBackupInput.addEventListener('change', e => importBackup(e.target.files[0]));
    }

    const clearStorageBtn = document.getElementById('clearStorageBtn');
    if(clearStorageBtn){
      clearStorageBtn.addEventListener('click', clearSavedData);
    }

    const copyAlertsBtn = document.getElementById('copyAlertsBtn');
    if(copyAlertsBtn){
      copyAlertsBtn.addEventListener('click', copyAlerts)
---
istener('input', updateAll);
      profitGoalEl.addEventListener('change', updateAll);
    }

    const goalTicketModeEl = document.getElementById('goalTicketMode');
    if(goalTicketModeEl){
      goalTicketModeEl.addEventListener('input', updateAll);
      goalTicketModeEl.addEventListener('change', updateAll);
    }
    document.getElementById('resetBtn').addEventListener('click', resetCommercial);
    document.getElementById('copyGestaoBtn').addEventListener('click', copyManagement);
    document.getElementById('resetGestaoBtn').addEventListener('click', resetFormation);
    document.getElementById('printAllBtn').addEventListener('click', () => printReportOnly('all'));
    document.getElementById('printBudgetBtn').addEventListener('click', () => printReportOnly('budget'));
    document.getElementById('copyBudgetBtn').addEventListener('click', copyBudgetText);

    renderWeights();
    renderCurrentPriceInputs();
    renderPrecisionInputs();
    renderCustomActivities();
    loadState();
    setActiveView('dashboard');
    updateAll();
  
    // Splash + instalação PWA
    let deferredPrompt = null;

    window.addEventListener('load', () => {
      const splash = document.getElementById('appSplash');
      setTimeout(() => {
 
---
ofitGoalEl.addEventListener('change', updateAll);
    }

    const goalTicketModeEl = document.getElementById('goalTicketMode');
    if(goalTicketModeEl){
      goalTicketModeEl.addEventListener('input', updateAll);
      goalTicketModeEl.addEventListener('change', updateAll);
    }
    document.getElementById('resetBtn').addEventListener('click', resetCommercial);
    document.getElementById('copyGestaoBtn').addEventListener('click', copyManagement);
    document.getElementById('resetGestaoBtn').addEventListener('click', resetFormation);
    document.getElementById('printAllBtn').addEventListener('click', () => printReportOnly('all'));
    document.getElementById('printBudgetBtn').addEventListener('click', () => printReportOnly('budget'));
    document.getElementById('copyBudgetBtn').addEventListener('click', copyBudgetText);

    renderWeights();
    renderCurrentPriceInputs();
    renderPrecisionInputs();
    renderCustomActivities();
    loadState();
    setActiveView('dashboard');
    updateAll();
  
    // Splash + instalação PWA
    let deferredPrompt = null;

    window.addEventListener('load', () => {
      const splash = document.getElementById('appSplash');
      setTimeout(() => {
        if(splash) splash.classList.ad
---
(goalTicketModeEl){
      goalTicketModeEl.addEventListener('input', updateAll);
      goalTicketModeEl.addEventListener('change', updateAll);
    }
    document.getElementById('resetBtn').addEventListener('click', resetCommercial);
    document.getElementById('copyGestaoBtn').addEventListener('click', copyManagement);
    document.getElementById('resetGestaoBtn').addEventListener('click', resetFormation);
    document.getElementById('printAllBtn').addEventListener('click', () => printReportOnly('all'));
    document.getElementById('printBudgetBtn').addEventListener('click', () => printReportOnly('budget'));
    document.getElementById('copyBudgetBtn').addEventListener('click', copyBudgetText);

    renderWeights();
    renderCurrentPriceInputs();
    renderPrecisionInputs();
    renderCustomActivities();
    loadState();
    setActiveView('dashboard');
    updateAll();
  
    // Splash + instalação PWA
    let deferredPrompt = null;

    window.addEventListener('load', () => {
      const splash = document.getElementById('appSplash');
      setTimeout(() => {
        if(splash) splash.classList.add('hide');
      }, 1200);

      // Service Worker desabilitado (requer sw.js externo)
    });

    window.addEventListener('beforeins
---
addEventListener('input', updateAll);
      goalTicketModeEl.addEventListener('change', updateAll);
    }
    document.getElementById('resetBtn').addEventListener('click', resetCommercial);
    document.getElementById('copyGestaoBtn').addEventListener('click', copyManagement);
    document.getElementById('resetGestaoBtn').addEventListener('click', resetFormation);
    document.getElementById('printAllBtn').addEventListener('click', () => printReportOnly('all'));
    document.getElementById('printBudgetBtn').addEventListener('click', () => printReportOnly('budget'));
    document.getElementById('copyBudgetBtn').addEventListener('click', copyBudgetText);

    renderWeights();
    renderCurrentPriceInputs();
    renderPrecisionInputs();
    renderCustomActivities();
    loadState();
    setActiveView('dashboard');
    updateAll();
  
    // Splash + instalação PWA
    let deferredPrompt = null;

    window.addEventListener('load', () => {
      const splash = document.getElementById('appSplash');
      setTimeout(() => {
        if(splash) splash.classList.add('hide');
      }, 1200);

      // Service Worker desabilitado (requer sw.js externo)
    });

    window.addEventListener('beforeinstallprompt', (e) => {
      e.preventDefaul
---

### removeItem
 saveState();
          showToast('Backup importado!');
        }catch(e){
          alert('Não foi possível importar este backup. Verifique se o arquivo é JSON válido.');
        }
      };
      reader.readAsText(file);
    }

    function clearSavedData(){
      if(confirm('Deseja limpar os dados salvos neste navegador?')){
        localStorage.removeItem('novaSabaraCalculadoraState');
        showToast('Dados locais limpos!');
      }
    }

    function buildSmartAlerts(){
      const alerts = [];
      const calc = getCommercialCalc();
      const form = getFormationCalc();
      let precision = null;
      try{ precision = getPrecisionData(); }catch(e){}

      if(calc.mensalidade < form.minima){
        alerts.push({type:'danger', title:'Mensalidade abaixo do mínimo', text:'A simulação atual está abaixo do custo mínimo. Evite aprovar esta proposta sem ajuste.'});
      }else if(calc.mensalidade < form.segura){
        alerts.push({type:'warn', title:'Mensalidade abaixo da segura', text:'A proposta cobre o mínimo, mas não protege suficientemente contra inadimplência e reserva.'});
      }else{
        alerts.push({type:'ok', title:'Mensalidade comercial saudável', text:'A simulação atual está acima da referência segura.'});
---

### toISOString
vel carregar dados salvos:', e);
      }
    }

    function exportBackup(){
      const blob = new Blob([JSON.stringify(collectState(), null, 2)], {type:'application/json'});
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url;
      a.download = `backup-calculadora-nova-sabara-${new Date().toISOString().slice(0,10)}.json`;
      document.body.appendChild(a);
      a.click();
      a.remove();
      URL.revokeObjectURL(url);
      showToast('Backup exportado!');
    }

    function importBackup(file){
      if(!file) return;
      const reader = new FileReader();
      reader.onload = () => {
        try{
          const state = JSON.parse(reader.result);
          applyState(state);
          saveState();
          showToast('Backup importado!');
        }catch(e){
          alert('Não foi possível importar este backup. Verifique se o arquivo é JSON válido.');
        }
      };
      reader.readAsText(file);
    }

    function clearSavedData(){
      if(confirm('Deseja limpar os dados salvos neste navegador?')){
        localStorage.removeItem('novaSabaraCalculadoraState');
        showToast('Dados locais limpos!');
      }
    }

    function buildSmartAlerts(){
      c
---
         r.diffPercent.toFixed(1).replace('.',',')+'%',
          r.status.label, ipcaRef].join(';'));
      });
      const blob = new Blob(['\uFEFF'+lines.join('\n')],{type:'text/csv;charset=utf-8;'});
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href=url; a.download='mensalidades-'+new Date().toISOString().slice(0,10)+'.csv';
      document.body.appendChild(a); a.click(); a.remove();
      URL.revokeObjectURL(url);
      showToast('CSV exportado!');
    }

    const exportCsvBtn2 = document.getElementById('exportCsvBtn');
    if(exportCsvBtn2){ exportCsvBtn2.addEventListener('click', exportCSV); }

    // Hook into side navigation for new views
    document.querySelectorAll('.side-link').forEach(link => {
      link.addEventListener('click', () => {
        const view = link.dataset.view;
        if(view === 'historico') setTimeout(renderHistory, 50);
        if(view === 'dashboard') setTimeout(updateAnnualProjection, 50);
      });
    });

    // Initial calls (deferred to after DOM + data ready)
    setTimeout(()=>{ renderHistory(); updateAnnualProjection(); updateIPCABanner(); }, 300);

</script>

<script>
  // Registro do Service Worker: necessário para instalação como a
---

### mensalidade
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="theme-color" content="#0757a8">
  <title>Nova Sabará Mensalidades</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Merriweather:wght@300;400;700;900&display=swap');
    :root{
      --bg:#eef8ff;
      --bg2:#fffdf6;
      --card:rgba(255,255,255,.72);
      --card2:rgba(255,255,255,.88);
      --text:#123047;
      --muted:#667789;
      --blue:#0757a8;
      --blue2:#58c3de;
      --green:#18a058;
      --red:#df3b3b;
      --amber:#ffc20a;
      --violet:#0b69bd;
      --border:rgba(7,87,168,.18);
      --shadow:0 20px 50px rgba(7,87,168,.13);
      --radius:18px;
    }

    *{
      box-sizing:border-box;
      font-family:"Merriweather", Georgia, serif;
    }

    button,
    input,
    select,
    textarea{
      font-family:"Merriweather", Georgia, serif;
    }

    body{
      margin:0;
      min-height:100vh;
      font-family: "Merriweather", Georgia, serif;
      color:var(--text);
      background:
  
---
s:16px !important; }
      .brand-card{ padding:8px !important; }
      .brand-logo{ width:min(150px,100%) !important; }
      .hours,
      .price-grid,
      .current-price-grid{
        grid-template-columns:1fr 1fr !important;
      }
      .panel{ padding:14px !important; }
    }

</style>

  <meta name="application-name" content="Nova Sabará Mensalidades">
  <meta name="apple-mobile-web-app-title" content="Nova Sabará">
  <meta name="theme-color" content="#0757a8">
  <!-- manifest.json requer hospedagem externa -->


  <!-- PWA / instalação como aplicativo -->
  <link rel="manifest" href="./manifest.webmanifest">
  <link rel="icon" type="image/png" sizes="32x32" href="./icon-32.png">
  <link rel="icon" type="image/png" sizes="192x192" href="./icon-192.png">
  <link rel="apple-touch-icon" sizes="180x180" href="./apple-touch-icon.png">
  <meta name="application-name" content="Nova Sabará Mensalidades">
  <meta name="apple-mobile-web-app-title" content="Nova Sabará">
  <meta name="apple-mobile-web-app-status-bar-style" content="default">

</head>
<body>

  <div id="appSplash" aria-hidden="true">
    <div class="splash-card">
<h1 class="splash-app-name">Nova Sabará Mensalidades</h1>
      <p class="splash-sub">Calculadora premiu
---
mo aplicativo -->
  <link rel="manifest" href="./manifest.webmanifest">
  <link rel="icon" type="image/png" sizes="32x32" href="./icon-32.png">
  <link rel="icon" type="image/png" sizes="192x192" href="./icon-192.png">
  <link rel="apple-touch-icon" sizes="180x180" href="./apple-touch-icon.png">
  <meta name="application-name" content="Nova Sabará Mensalidades">
  <meta name="apple-mobile-web-app-title" content="Nova Sabará">
  <meta name="apple-mobile-web-app-status-bar-style" content="default">

</head>
<body>

  <div id="appSplash" aria-hidden="true">
    <div class="splash-card">
<h1 class="splash-app-name">Nova Sabará Mensalidades</h1>
      <p class="splash-sub">Calculadora premium de mensalidades, formação de preço, comparativos e gestão escolar.</p>
      <div class="splash-loading"><i></i></div>
      <div class="splash-hint">Carregando ambiente da escola</div>
      <div class="install-banner" id="installBanner" style="display:none">
        <button id="installAppBtn" type="button">Instalar app</button>
        <button id="dismissInstallBtn" class="muted" type="button">Agora não</button>
      </div>
    </div>
  </div>

  <main class="app">
    <div class="shell">
      <aside class="sidebar">
        <div class="sideba
---
ouch-icon.png">
  <meta name="application-name" content="Nova Sabará Mensalidades">
  <meta name="apple-mobile-web-app-title" content="Nova Sabará">
  <meta name="apple-mobile-web-app-status-bar-style" content="default">

</head>
<body>

  <div id="appSplash" aria-hidden="true">
    <div class="splash-card">
<h1 class="splash-app-name">Nova Sabará Mensalidades</h1>
      <p class="splash-sub">Calculadora premium de mensalidades, formação de preço, comparativos e gestão escolar.</p>
      <div class="splash-loading"><i></i></div>
      <div class="splash-hint">Carregando ambiente da escola</div>
      <div class="install-banner" id="installBanner" style="display:none">
        <button id="installAppBtn" type="button">Instalar app</button>
        <button id="dismissInstallBtn" class="muted" type="button">Agora não</button>
      </div>
    </div>
  </div>

  <main class="app">
    <div class="shell">
      <aside class="sidebar">
        <div class="sidebar-top">
          <div class="sidebar-brand">
            <div class="sidebar-brand-title">Nova Sabará</div>
            <div class="sidebar-brand-sub">Mensalidades Premium</div>
          </div>
          <button class="mobile-menu-toggle" id="mobileMenuToggle" type="button" aria
---
Mensalidades">
  <meta name="apple-mobile-web-app-title" content="Nova Sabará">
  <meta name="apple-mobile-web-app-status-bar-style" content="default">

</head>
<body>

  <div id="appSplash" aria-hidden="true">
    <div class="splash-card">
<h1 class="splash-app-name">Nova Sabará Mensalidades</h1>
      <p class="splash-sub">Calculadora premium de mensalidades, formação de preço, comparativos e gestão escolar.</p>
      <div class="splash-loading"><i></i></div>
      <div class="splash-hint">Carregando ambiente da escola</div>
      <div class="install-banner" id="installBanner" style="display:none">
        <button id="installAppBtn" type="button">Instalar app</button>
        <button id="dismissInstallBtn" class="muted" type="button">Agora não</button>
      </div>
    </div>
  </div>

  <main class="app">
    <div class="shell">
      <aside class="sidebar">
        <div class="sidebar-top">
          <div class="sidebar-brand">
            <div class="sidebar-brand-title">Nova Sabará</div>
            <div class="sidebar-brand-sub">Mensalidades Premium</div>
          </div>
          <button class="mobile-menu-toggle" id="mobileMenuToggle" type="button" aria-expanded="false">
            <span>☰</span>
            <b>Menu</b>
---
missInstallBtn" class="muted" type="button">Agora não</button>
      </div>
    </div>
  </div>

  <main class="app">
    <div class="shell">
      <aside class="sidebar">
        <div class="sidebar-top">
          <div class="sidebar-brand">
            <div class="sidebar-brand-title">Nova Sabará</div>
            <div class="sidebar-brand-sub">Mensalidades Premium</div>
          </div>
          <button class="mobile-menu-toggle" id="mobileMenuToggle" type="button" aria-expanded="false">
            <span>☰</span>
            <b>Menu</b>
          </button>
        </div>

        <nav class="sidebar-nav">
          <button class="side-link active" data-view="dashboard" aria-label="Ir para Dashboard">
            <span class="side-emoji">📌</span>
            <span>
              <strong>Dashboard</strong>
              <small>Visão geral da mensalidade</small>
            </span>
          </button>

          <button class="side-link" data-view="comercial" aria-label="Ir para Simulação Comercial">
            <span class="side-emoji">🏫</span>
            <span>
              <strong>Simulação Comercial</strong>
              <small>Orçamento rápido para famílias</small>
            </span>
          </button>

          <but
---
  <span>☰</span>
            <b>Menu</b>
          </button>
        </div>

        <nav class="sidebar-nav">
          <button class="side-link active" data-view="dashboard" aria-label="Ir para Dashboard">
            <span class="side-emoji">📌</span>
            <span>
              <strong>Dashboard</strong>
              <small>Visão geral da mensalidade</small>
            </span>
          </button>

          <button class="side-link" data-view="comercial" aria-label="Ir para Simulação Comercial">
            <span class="side-emoji">🏫</span>
            <span>
              <strong>Simulação Comercial</strong>
              <small>Orçamento rápido para famílias</small>
            </span>
          </button>

          <button class="side-link" data-view="formacao" aria-label="Ir para Formação de Preço">
            <span class="side-emoji">📊</span>
            <span>
              <strong>Formação de Preço</strong>
              <small>Custos, margem e segurança</small>
            </span>
          </button>

          <button class="side-link" data-view="tabela" aria-label="Ir para Tabela por Turma">
            <span class="side-emoji">🧾</span>
            <span>
              <strong>Tabela por Turma</strong>
       
---
Atalhos</small>
          <button class="sidebar-mini-btn" id="goBudgetBtn">Ir para orçamento</button>
          <button class="sidebar-mini-btn" id="goPrintBtn">Ir para relatórios</button>
        </div>

        <div class="sidebar-box quick-view-mobile">
          <small>Visão rápida</small>
          <div class="sidebar-kpi">
            <span>Mensalidade atual</span>
            <strong id="sideCurrentMonthly">R$ 0,00</strong>
          </div>
          <div class="sidebar-kpi">
            <span>Total inicial</span>
            <strong id="sideInitialTotal">R$ 0,00</strong>
          </div>
        </div>
      </aside>

      <div class="main-content">
    <section class="hero">
      <div class="hero-content">
        <div>
          <div class="eyebrow">Nova Sabará · Calculadora Premium</div>
          <h1>Calculadora de Mensalidade Nova Sabará</h1>
          <p>Simule mensalidades, forme preços reais e gere orçamentos com a identidade visual da Escola de Educação Infantil Nova Sabará.</p>
        </div>
        <div class="brand-card">
          <img class="brand-logo" src="data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAjMAAAGRCAIAAAAfDKXtAAAAAXNSR0IArs4c6QAAAARnQU1BAACxjwv8YQUAAAAJcEhZcwAADsMAAA7DAcdvqGQAALfkSURBVHhe7f0
---
         <span>Total inicial</span>
            <strong id="sideInitialTotal">R$ 0,00</strong>
          </div>
        </div>
      </aside>

      <div class="main-content">
    <section class="hero">
      <div class="hero-content">
        <div>
          <div class="eyebrow">Nova Sabará · Calculadora Premium</div>
          <h1>Calculadora de Mensalidade Nova Sabará</h1>
          <p>Simule mensalidades, forme preços reais e gere orçamentos com a identidade visual da Escola de Educação Infantil Nova Sabará.</p>
        </div>
        <div class="brand-card">
          <img class="brand-logo" src="data:image/png;base64,
---
strong id="sideInitialTotal">R$ 0,00</strong>
          </div>
        </div>
      </aside>

      <div class="main-content">
    <section class="hero">
      <div class="hero-content">
        <div>
          <div class="eyebrow">Nova Sabará · Calculadora Premium</div>
          <h1>Calculadora de Mensalidade Nova Sabará</h1>
          <p>Simule mensalidades, forme preços reais e gere orçamentos com a identidade visual da Escola de Educação Infantil Nova Sabará.</p>
        </div>
        <div class="brand-card">
          <img class="brand-logo" src="data:image/png;base64,
---
EIskupDJJJBKJJLuQyiSRSCSS7EIqk0QikUiyiX/84/8D7SGTXnJipAUAAAAASUVORK5CYII=" alt="Escola de Educação Infantil Nova Sabará">
        </div>
      </div>
    </section>

    <section id="dashboard" class="view active">
      <div class="dash-grid">
        <div class="dash-hero panel">
          <div class="section-title">
            <h2>Dashboard da Mensalidade</h2>
            <span class="pill">Visão executiva</span>
          </div>

          <div class="dash-main">
            <div>
              <small>Mensalidade final simulada</small>
              <strong id="dashMonthly">R$ 0,00</strong>
              <span id="dashSubtitle">Carregando simulação...</span>
            </div>
            <div class="dash-status" id="dashStatus">
              <b>Status</b>
              <span>Calculando...</span>
            </div>
          </div>

          <div class="dash-bars">
            <div class="dash-bar-row">
              <div>
                <b>Comparação com mensalidade mínima</b>
                <small id="dashMinText">R$ 0,00</small>
              </div>
              <div class="dash-track"><i id="dashMinBar"></i></div>
            </div>

            <div class="dash-bar-row">
              <div>
                <b>Compar
---
n>

    <section id="dashboard" class="view active">
      <div class="dash-grid">
        <div class="dash-hero panel">
          <div class="section-title">
            <h2>Dashboard da Mensalidade</h2>
            <span class="pill">Visão executiva</span>
          </div>

          <div class="dash-main">
            <div>
              <small>Mensalidade final simulada</small>
              <strong id="dashMonthly">R$ 0,00</strong>
              <span id="dashSubtitle">Carregando simulação...</span>
            </div>
            <div class="dash-status" id="dashStatus">
              <b>Status</b>
              <span>Calculando...</span>
            </div>
          </div>

          <div class="dash-bars">
            <div class="dash-bar-row">
              <div>
                <b>Comparação com mensalidade mínima</b>
                <small id="dashMinText">R$ 0,00</small>
              </div>
              <div class="dash-track"><i id="dashMinBar"></i></div>
            </div>

            <div class="dash-bar-row">
              <div>
                <b>Comparação com mensalidade segura</b>
                <small id="dashSafeText">R$ 0,00</small>
              </div>
              <div class="dash-track"><i id="dashSaf
---
ashSubtitle">Carregando simulação...</span>
            </div>
            <div class="dash-status" id="dashStatus">
              <b>Status</b>
              <span>Calculando...</span>
            </div>
          </div>

          <div class="dash-bars">
            <div class="dash-bar-row">
              <div>
                <b>Comparação com mensalidade mínima</b>
                <small id="dashMinText">R$ 0,00</small>
              </div>
              <div class="dash-track"><i id="dashMinBar"></i></div>
            </div>

            <div class="dash-bar-row">
              <div>
                <b>Comparação com mensalidade segura</b>
                <small id="dashSafeText">R$ 0,00</small>
              </div>
              <div class="dash-track"><i id="dashSafeBar"></i></div>
            </div>

            <div class="dash-bar-row">
              <div>
                <b>Comparação com mensalidade ideal</b>
                <small id="dashIdealText">R$ 0,00</small>
              </div>
              <div class="dash-track"><i id="dashIdealBar"></i></div>
            </div>
          </div>
        </div>

        <div class="panel dash-panel">
          <div class="section-title">
            <h2>Resumo financeiro</h
---
dash-bar-row">
              <div>
                <b>Comparação com mensalidade mínima</b>
                <small id="dashMinText">R$ 0,00</small>
              </div>
              <div class="dash-track"><i id="dashMinBar"></i></div>
            </div>

            <div class="dash-bar-row">
              <div>
                <b>Comparação com mensalidade segura</b>
                <small id="dashSafeText">R$ 0,00</small>
              </div>
              <div class="dash-track"><i id="dashSafeBar"></i></div>
            </div>

            <div class="dash-bar-row">
              <div>
                <b>Comparação com mensalidade ideal</b>
                <small id="dashIdealText">R$ 0,00</small>
              </div>
              <div class="dash-track"><i id="dashIdealBar"></i></div>
            </div>
          </div>
        </div>

        <div class="panel dash-panel">
          <div class="section-title">
            <h2>Resumo financeiro</h2>
            <span class="pill">Atual</span>
          </div>
          <div class="dash-kpi-list">
            <div class="dash-kpi">
              <small>Mensalidade cheia</small>
              <strong id="dashFull">R$ 0,00</strong>
            </div>
            <div class="d
---
sh-bar-row">
              <div>
                <b>Comparação com mensalidade segura</b>
                <small id="dashSafeText">R$ 0,00</small>
              </div>
              <div class="dash-track"><i id="dashSafeBar"></i></div>
            </div>

            <div class="dash-bar-row">
              <div>
                <b>Comparação com mensalidade ideal</b>
                <small id="dashIdealText">R$ 0,00</small>
              </div>
              <div class="dash-track"><i id="dashIdealBar"></i></div>
            </div>
          </div>
        </div>

        <div class="panel dash-panel">
          <div class="section-title">
            <h2>Resumo financeiro</h2>
            <span class="pill">Atual</span>
          </div>
          <div class="dash-kpi-list">
            <div class="dash-kpi">
              <small>Mensalidade cheia</small>
              <strong id="dashFull">R$ 0,00</strong>
            </div>
            <div class="dash-kpi">
              <small>Desconto aplicado</small>
              <strong id="dashDiscount">R$ 0,00</strong>
            </div>
            <div class="dash-kpi">
              <small>Total inicial</small>
              <strong id="dashInitial">R$ 0,00</strong>
            </di
---
="dashIdealBar"></i></div>
            </div>
          </div>
        </div>

        <div class="panel dash-panel">
          <div class="section-title">
            <h2>Resumo financeiro</h2>
            <span class="pill">Atual</span>
          </div>
          <div class="dash-kpi-list">
            <div class="dash-kpi">
              <small>Mensalidade cheia</small>
              <strong id="dashFull">R$ 0,00</strong>
            </div>
            <div class="dash-kpi">
              <small>Desconto aplicado</small>
              <strong id="dashDiscount">R$ 0,00</strong>
            </div>
            <div class="dash-kpi">
              <small>Total inicial</small>
              <strong id="dashInitial">R$ 0,00</strong>
            </div>
            <div class="dash-kpi">
              <small>Atividades extras</small>
              <strong id="dashActivities">R$ 0,00</strong>
            </div>
          </div>
        </div>

        <div class="panel dash-panel">
          <div class="section-title">
            <h2>Composição</h2>
            <span class="pill">Detalhes</span>
          </div>
          <div class="dash-donut-wrap">
            <div class="dash-donut" id="dashDonut"></div>
            <div class="das
---
nput type="checkbox" id="act_natacao"></div>
              <div class="activity-content">
                <div class="activity-head">
                  <b>Natação</b>
                  <span>Opcional</span>
                </div>
                <div class="activity-fields">
                  <div class="activity-field">
                    <label>Mensalidade</label>
                    <input type="number" id="act_natacao_mensal" value="180" min="0" step="10">
                  </div>
                  <div class="activity-field">
                    <label>Matrícula</label>
                    <input type="number" id="act_natacao_matricula" value="50" min="0" step="10">
                  </div>
                </div>
              </div>
            </div>

            <div class="activity-card">
              <div class="activity-toggle"><input type="checkbox" id="act_judo"></div>
              <div class="activity-content">
                <div class="activity-head">
                  <b>Judô</b>
                  <span>Opcional</span>
                </div>
                <div class="activity-fields">
                  <div class="activity-field">
                    <label>Mensalidade</label>
                    <input type
---
le"><input type="checkbox" id="act_judo"></div>
              <div class="activity-content">
                <div class="activity-head">
                  <b>Judô</b>
                  <span>Opcional</span>
                </div>
                <div class="activity-fields">
                  <div class="activity-field">
                    <label>Mensalidade</label>
                    <input type="number" id="act_judo_mensal" value="150" min="0" step="10">
                  </div>
                  <div class="activity-field">
                    <label>Matrícula</label>
                    <input type="number" id="act_judo_matricula" value="40" min="0" step="10">
                  </div>
                </div>
              </div>
            </div>

            <div class="activity-card">
              <div class="activity-toggle"><input type="checkbox" id="act_bale"></div>
              <div class="activity-content">
                <div class="activity-head">
                  <b>Balé</b>
                  <span>Opcional</span>
                </div>
                <div class="activity-fields">
                  <div class="activity-field">
                    <label>Mensalidade</label>
                    <input type="numb
---
le"><input type="checkbox" id="act_bale"></div>
              <div class="activity-content">
                <div class="activity-head">
                  <b>Balé</b>
                  <span>Opcional</span>
                </div>
                <div class="activity-fields">
                  <div class="activity-field">
                    <label>Mensalidade</label>
                    <input type="number" id="act_bale_mensal" value="160" min="0" step="10">
                  </div>
                  <div class="activity-field">
                    <label>Matrícula</label>
                    <input type="number" id="act_bale_matricula" value="45" min="0" step="10">
                  </div>
                </div>
              </div>
            </div>

            <div class="activity-card">
              <div class="activity-toggle"><input type="checkbox" id="act_teatro"></div>
              <div class="activity-content">
                <div class="activity-head">
                  <b>Teatro</b>
                  <span>Opcional</span>
                </div>
                <div class="activity-fields">
                  <div class="activity-field">
                    <label>Mensalidade</label>
                    <input type="
---
<input type="checkbox" id="act_teatro"></div>
              <div class="activity-content">
                <div class="activity-head">
                  <b>Teatro</b>
                  <span>Opcional</span>
                </div>
                <div class="activity-fields">
                  <div class="activity-field">
                    <label>Mensalidade</label>
                    <input type="number" id="act_teatro_mensal" value="140" min="0" step="10">
                  </div>
                  <div class="activity-field">
                    <label>Matrícula</label>
                    <input type="number" id="act_teatro_matricula" value="35" min="0" step="10">
                  </div>
                </div>
              </div>
            </div>
          </div>

        </div>

        <aside class="panel result-card">
          <div class="section-title">
            <h2>Resultado comercial</h2>
            <span class="pill" id="selectedLabel">Berçário · 6h</span>
          </div>

          <div class="total-box">
            <small>Mensalidade estimada</small>
            <div class="total" id="total">R$ 0,00</div>
          </div>

          <div class="summary" id="summary"></div>

          <div class="proposal"
---
      </div>
              </div>
            </div>
          </div>

        </div>

        <aside class="panel result-card">
          <div class="section-title">
            <h2>Resultado comercial</h2>
            <span class="pill" id="selectedLabel">Berçário · 6h</span>
          </div>

          <div class="total-box">
            <small>Mensalidade estimada</small>
            <div class="total" id="total">R$ 0,00</div>
          </div>

          <div class="summary" id="summary"></div>

          <div class="proposal" id="proposal"></div>

          <div class="actions">
            <button class="btn" id="copyBtn">Copiar proposta para WhatsApp</button>
            <button class="btn secondary" id="resetBtn">Limpar simulação</button>
          </div>
        </aside>
      </div>
    </section>

    <section id="formacao" class="view">
      <div class="kpis">
        <div class="kpi">
          <small>Mensalidade mínima</small>
          <strong id="kpiMinima">R$ 0,00</strong>
          <em>Valor para cobrir custos</em>
        </div>
        <div class="kpi">
          <small>Mensalidade segura</small>
          <strong id="kpiSegura">R$ 0,00</strong>
          <em>Com inadimplência e reserva</em>
        </div>
   
---
="actions">
            <button class="btn" id="copyBtn">Copiar proposta para WhatsApp</button>
            <button class="btn secondary" id="resetBtn">Limpar simulação</button>
          </div>
        </aside>
      </div>
    </section>

    <section id="formacao" class="view">
      <div class="kpis">
        <div class="kpi">
          <small>Mensalidade mínima</small>
          <strong id="kpiMinima">R$ 0,00</strong>
          <em>Valor para cobrir custos</em>
        </div>
        <div class="kpi">
          <small>Mensalidade segura</small>
          <strong id="kpiSegura">R$ 0,00</strong>
          <em>Com inadimplência e reserva</em>
        </div>
        <div class="kpi">
          <small>Mensalidade ideal</small>
          <strong id="kpiIdeal">R$ 0,00</strong>
          <em>Com margem de lucro</em>
        </div>
      </div>

      <div class="grid">
        <div>
          <div class="grid-2">
            <div class="panel">
              <div class="section-title">
                <h2>Custos fixos mensais</h2>
                <span class="pill">Escola</span>
              </div>

              <div class="fields">
                <div class="field">
                  <label>Folha de pagamento</label>
            
---
         </div>
        </aside>
      </div>
    </section>

    <section id="formacao" class="view">
      <div class="kpis">
        <div class="kpi">
          <small>Mensalidade mínima</small>
          <strong id="kpiMinima">R$ 0,00</strong>
          <em>Valor para cobrir custos</em>
        </div>
        <div class="kpi">
          <small>Mensalidade segura</small>
          <strong id="kpiSegura">R$ 0,00</strong>
          <em>Com inadimplência e reserva</em>
        </div>
        <div class="kpi">
          <small>Mensalidade ideal</small>
          <strong id="kpiIdeal">R$ 0,00</strong>
          <em>Com margem de lucro</em>
        </div>
      </div>

      <div class="grid">
        <div>
          <div class="grid-2">
            <div class="panel">
              <div class="section-title">
                <h2>Custos fixos mensais</h2>
                <span class="pill">Escola</span>
              </div>

              <div class="fields">
                <div class="field">
                  <label>Folha de pagamento</label>
                  <input data-cost="folha" type="number" value="28000" min="0" step="100">
                </div>
                <div class="field">
                  <label>Impostos / encar
---
 mínima</small>
          <strong id="kpiMinima">R$ 0,00</strong>
          <em>Valor para cobrir custos</em>
        </div>
        <div class="kpi">
          <small>Mensalidade segura</small>
          <strong id="kpiSegura">R$ 0,00</strong>
          <em>Com inadimplência e reserva</em>
        </div>
        <div class="kpi">
          <small>Mensalidade ideal</small>
          <strong id="kpiIdeal">R$ 0,00</strong>
          <em>Com margem de lucro</em>
        </div>
      </div>

      <div class="grid">
        <div>
          <div class="grid-2">
            <div class="panel">
              <div class="section-title">
                <h2>Custos fixos mensais</h2>
                <span class="pill">Escola</span>
              </div>

              <div class="fields">
                <div class="field">
                  <label>Folha de pagamento</label>
                  <input data-cost="folha" type="number" value="28000" min="0" step="100">
                </div>
                <div class="field">
                  <label>Impostos / encargos</label>
                  <input data-cost="impostos" type="number" value="6500" min="0" step="100">
                </div>
                <div class="field">
                  
---
="checkPwaBtn" type="button">Verificar instalação</button>
            </div>

          </div>
        </div>

        <aside class="panel result-card">
          <div class="section-title">
            <h2>Diagnóstico</h2>
            <span class="pill">Preço real</span>
          </div>

          <div class="total-box safe">
            <small>Mensalidade segura sugerida</small>
            <div class="total" id="seguraMain">R$ 0,00</div>
          </div>

          <div class="summary" id="formacaoSummary"></div>

          <div class="insight" id="insight"></div>

          <div class="actions">
            <button class="btn" id="copyGestaoBtn">Copiar resumo gerencial</button>
            <button class="btn secondary" id="resetGestaoBtn">Restaurar exemplo</button>
          </div>
        </aside>
      </div>
    </section>

    <section id="tabela" class="view">
      <div class="grid">
        <div class="panel">
          <div class="section-title">
            <h2>Tabela recomendada por turma e horário</h2>
            <span class="pill">Baseada na formação de preço</span>
          </div>
          <div id="tablePrices"></div>
        </div>

        <aside class="panel result-card">
          <div class="section-titl
---
rpretar</h2>
            <span class="pill">Uso prático</span>
          </div>
          <div class="total-box ideal">
            <small>Referência principal</small>
            <div class="total" id="idealMain">R$ 0,00</div>
          </div>
          <div class="proposal">
            <strong>Leitura recomendada:</strong><br>
            Use a mensalidade mínima apenas como piso técnico. A mensalidade segura é a melhor referência para não trabalhar no limite. A ideal permite investimento, reserva, melhorias e crescimento.
          </div>
          <div class="summary" id="tableSummary"></div>
        </aside>
      </div>
    </section>


    <section id="comparativo" class="view">
      <div class="grid">
        <div class="panel">
          <div class="section-title">
            <h2>Preços praticados atualmente</h2>
            <span class="pill">Comparar com recomendado</span>
          </div>

          <p class="helper-text">
            Preencha os valores que a escola cobra hoje. A calculadora compara com a tabela recomendada pela formação de preço e mostra onde há defasagem, equilíbrio ou folga.
          </p>

          <div class="current-price-list" id="currentPriceInputs"></div>
        </div>

        <aside cl
---
 prático</span>
          </div>
          <div class="total-box ideal">
            <small>Referência principal</small>
            <div class="total" id="idealMain">R$ 0,00</div>
          </div>
          <div class="proposal">
            <strong>Leitura recomendada:</strong><br>
            Use a mensalidade mínima apenas como piso técnico. A mensalidade segura é a melhor referência para não trabalhar no limite. A ideal permite investimento, reserva, melhorias e crescimento.
          </div>
          <div class="summary" id="tableSummary"></div>
        </aside>
      </div>
    </section>


    <section id="comparativo" class="view">
      <div class="grid">
        <div class="panel">
          <div class="section-title">
            <h2>Preços praticados atualmente</h2>
            <span class="pill">Comparar com recomendado</span>
          </div>

          <p class="helper-text">
            Preencha os valores que a escola cobra hoje. A calculadora compara com a tabela recomendada pela formação de preço e mostra onde há defasagem, equilíbrio ou folga.
          </p>

          <div class="current-price-list" id="currentPriceInputs"></div>
        </div>

        <aside class="panel result-card">
          <div class="
---
  </div>
              <div class="field">
                <label for="goalTicketMode">Ticket usado na meta</label>
                <select id="goalTicketMode">
                  <option value="current">Ticket médio atual</option>
                  <option value="recommended">Ticket médio recomendado</option>
                  <option value="safe">Mensalidade segura base</option>
                </select>
              </div>
            </div>

            <div class="scenario-grid">
              <div class="scenario-card">
                <small>Vagas disponíveis</small>
                <strong id="openSeatsTotal">0</strong>
                <span>capacidade menos matriculados</span>
              </div>
              <div class="scenario-card">
                <small>Alunos para empatar</small>
                <strong id="studentsToBreakEven">0</strong>
                <span>pagantes adicionais estimados</span>
              </div>
              <div class="scenario-card">
                <small>Alunos para lucro</small>
                <strong id="studentsToProfit">0</strong>
                <span>pagantes adicionais estimados</span>
              </div>
            </div>

            <div id="enrollmentOpportunityTable" styl
---
le"></div>
      </div>
    </section>


    <section id="alertas" class="view">
      <div class="grid">
        <div class="panel">
          <div class="section-title">
            <h2>Alertas inteligentes</h2>
            <span class="pill">Leitura automática</span>
          </div>
          <p class="helper-text">
            O sistema cruza mensalidade, custos, descontos, ocupação, equipe, alimentação, preços praticados e ponto de equilíbrio para destacar riscos e oportunidades.
          </p>
          <div class="smart-alert-list" id="smartAlertList"></div>
        </div>

        <aside class="panel result-card">
          <div class="section-title">
            <h2>Resumo de risco</h2>
            <span class="pill">Semáforo</span>
          </div>
          <div class="total-box" id="riskBox">
            <small>Nível geral</small>
            <div class="total" id="riskLevel">Calculando</div>
          </div>
          <div class="summary" id="riskSummary"></div>
          <div class="actions">
            <button class="btn" id="copyAlertsBtn">Copiar alertas</button>
            <button class="btn secondary" id="goPrecisionFromAlerts">Ver precisão gerencial</button>
          </div>
        </aside>
      </div>
    
---
isão gerencial</button>
          </div>
        </aside>
      </div>
    </section>

    <section id="apresentacao" class="view">
      <div class="presentation-stage">
        <div class="presentation-card">
          <div class="presentation-header">
            <div>
              <small>Nova Sabará · visão executiva</small>
              <h2>Mensalidade, custos e decisão</h2>
            </div>
            <span id="presentationDate"></span>
          </div>

          <div class="presentation-grid">
            <div class="presentation-metric primary">
              <small>Mensalidade simulada</small>
              <strong id="presMonthly">R$ 0,00</strong>
              <span id="presClass">Turma · horário</span>
            </div>
            <div class="presentation-metric">
              <small>Mensalidade segura</small>
              <strong id="presSafe">R$ 0,00</strong>
              <span>referência gerencial</span>
            </div>
            <div class="presentation-metric">
              <small>Resultado mensal</small>
              <strong id="presResult">R$ 0,00</strong>
              <span>estimativa atual</span>
            </div>
            <div class="presentation-metric">
              <small>Ponto de e
---
entation-header">
            <div>
              <small>Nova Sabará · visão executiva</small>
              <h2>Mensalidade, custos e decisão</h2>
            </div>
            <span id="presentationDate"></span>
          </div>

          <div class="presentation-grid">
            <div class="presentation-metric primary">
              <small>Mensalidade simulada</small>
              <strong id="presMonthly">R$ 0,00</strong>
              <span id="presClass">Turma · horário</span>
            </div>
            <div class="presentation-metric">
              <small>Mensalidade segura</small>
              <strong id="presSafe">R$ 0,00</strong>
              <span>referência gerencial</span>
            </div>
            <div class="presentation-metric">
              <small>Resultado mensal</small>
              <strong id="presResult">R$ 0,00</strong>
              <span>estimativa atual</span>
            </div>
            <div class="presentation-metric">
              <small>Ponto de equilíbrio</small>
              <strong id="presBreakEven">0</strong>
              <span>alunos pagantes</span>
            </div>
          </div>

          <div class="presentation-insight" id="presentationInsight"></div>

          
---
v>

          <div class="presentation-grid">
            <div class="presentation-metric primary">
              <small>Mensalidade simulada</small>
              <strong id="presMonthly">R$ 0,00</strong>
              <span id="presClass">Turma · horário</span>
            </div>
            <div class="presentation-metric">
              <small>Mensalidade segura</small>
              <strong id="presSafe">R$ 0,00</strong>
              <span>referência gerencial</span>
            </div>
            <div class="presentation-metric">
              <small>Resultado mensal</small>
              <strong id="presResult">R$ 0,00</strong>
              <span>estimativa atual</span>
            </div>
            <div class="presentation-metric">
              <small>Ponto de equilíbrio</small>
              <strong id="presBreakEven">0</strong>
              <span>alunos pagantes</span>
            </div>
          </div>

          <div class="presentation-insight" id="presentationInsight"></div>

          <div class="presentation-actions">
            <button class="btn" id="presentationToBudget">Gerar orçamento</button>
            <button class="btn secondary" id="presentationToCompare">Ver comparativo</button>
          </div>

---
</span>
                <span>✅ Backup/importação</span>
                <span>⏳ Manifest + Service Worker no pacote final</span>
              </div>
            </div>

            <div class="settings-card">
              <h3>Proposta para família</h3>
              <p>Use o modo apresentação e os relatórios para atendimento. O orçamento inclui mensalidade, alimentação, atividades, matrícula e material inicial.</p>
              <button class="btn secondary" id="settingsGoPresentation">Abrir modo apresentação</button>
            </div>
          </div>
        </div>

        <aside class="panel result-card">
          <div class="section-title">
            <h2>Status do sistema</h2>
            <span class="pill">Resumo</span>
          </div>
          <div class="summary" id="settingsSummary"></div>
          <div class="insight">
            <strong>Recomendação:</strong><br>
            Depois de validar os valores reais, exporte um backup e guarde como referência oficial da tabela da escola.
          </div>
        </aside>
      </div>
    </section>

    <section id="relatorios" class="view">
      <div class="panel">
        <div class="section-title">
          <h2>Relatórios e orçamentos para impressão</h2>
      
---
adesMatricula = activitiesChosen.reduce((acc, a) => acc + a.matricula, 0);
      const alimentacaoCalculada = document.getElementById('alimentacao')?.checked ? getNutritionCost(selectedTurma, selectedHour) : 0;
      const custosIniciais = getInitialCosts();

      const matricula = n('matricula');
      const desconto = n('desconto');
      const mensalidadeCheia = Math.max(0, base + extraTotal + atividadesMensal);
      const mensalidade = Math.max(0, mensalidadeCheia - desconto);
      const totalInicial = mensalidade + matricula + atividadesMatricula + custosIniciais;

      return {
        turma,
        base,
        extraTotal,
        extraLines,
        activitiesChosen,
        atividadesMensal,
        atividadesMatricula,
        alimentacaoCalculada,
        custosIniciais,
        matricula,
        desconto,
        mensalidadeCheia,
        mensalidade,
        totalInicial
      };
    }

    function updateCommercial(){
      renderTurmas();
      renderHours();

      const calc = getCommercialCalc();
      document.getElementById('selectedLabel').textContent = `${calc.turma.nome} · ${selectedHour}h`;
      document.getElementById('total').textContent = money.format(calc.mensalidade);

      const extraHtml = c
---
onst alimentacaoCalculada = document.getElementById('alimentacao')?.checked ? getNutritionCost(selectedTurma, selectedHour) : 0;
      const custosIniciais = getInitialCosts();

      const matricula = n('matricula');
      const desconto = n('desconto');
      const mensalidadeCheia = Math.max(0, base + extraTotal + atividadesMensal);
      const mensalidade = Math.max(0, mensalidadeCheia - desconto);
      const totalInicial = mensalidade + matricula + atividadesMatricula + custosIniciais;

      return {
        turma,
        base,
        extraTotal,
        extraLines,
        activitiesChosen,
        atividadesMensal,
        atividadesMatricula,
        alimentacaoCalculada,
        custosIniciais,
        matricula,
        desconto,
        mensalidadeCheia,
        mensalidade,
        totalInicial
      };
    }

    function updateCommercial(){
      renderTurmas();
      renderHours();

      const calc = getCommercialCalc();
      document.getElementById('selectedLabel').textContent = `${calc.turma.nome} · ${selectedHour}h`;
      document.getElementById('total').textContent = money.format(calc.mensalidade);

      const extraHtml = calc.extraLines.length
        ? calc.extraLines.map(line => `
          <div class
---
= document.getElementById('alimentacao')?.checked ? getNutritionCost(selectedTurma, selectedHour) : 0;
      const custosIniciais = getInitialCosts();

      const matricula = n('matricula');
      const desconto = n('desconto');
      const mensalidadeCheia = Math.max(0, base + extraTotal + atividadesMensal);
      const mensalidade = Math.max(0, mensalidadeCheia - desconto);
      const totalInicial = mensalidade + matricula + atividadesMatricula + custosIniciais;

      return {
        turma,
        base,
        extraTotal,
        extraLines,
        activitiesChosen,
        atividadesMensal,
        atividadesMatricula,
        alimentacaoCalculada,
        custosIniciais,
        matricula,
        desconto,
        mensalidadeCheia,
        mensalidade,
        totalInicial
      };
    }

    function updateCommercial(){
      renderTurmas();
      renderHours();

      const calc = getCommercialCalc();
      document.getElementById('selectedLabel').textContent = `${calc.turma.nome} · ${selectedHour}h`;
      document.getElementById('total').textContent = money.format(calc.mensalidade);

      const extraHtml = calc.extraLines.length
        ? calc.extraLines.map(line => `
          <div class="line positive"><span>${l
---
tritionCost(selectedTurma, selectedHour) : 0;
      const custosIniciais = getInitialCosts();

      const matricula = n('matricula');
      const desconto = n('desconto');
      const mensalidadeCheia = Math.max(0, base + extraTotal + atividadesMensal);
      const mensalidade = Math.max(0, mensalidadeCheia - desconto);
      const totalInicial = mensalidade + matricula + atividadesMatricula + custosIniciais;

      return {
        turma,
        base,
        extraTotal,
        extraLines,
        activitiesChosen,
        atividadesMensal,
        atividadesMatricula,
        alimentacaoCalculada,
        custosIniciais,
        matricula,
        desconto,
        mensalidadeCheia,
        mensalidade,
        totalInicial
      };
    }

    function updateCommercial(){
      renderTurmas();
      renderHours();

      const calc = getCommercialCalc();
      document.getElementById('selectedLabel').textContent = `${calc.turma.nome} · ${selectedHour}h`;
      document.getElementById('total').textContent = money.format(calc.mensalidade);

      const extraHtml = calc.extraLines.length
        ? calc.extraLines.map(line => `
          <div class="line positive"><span>${line.nome}</span><strong>+ ${money.format(line.valor)}</st
---
const totalInicial = mensalidade + matricula + atividadesMatricula + custosIniciais;

      return {
        turma,
        base,
        extraTotal,
        extraLines,
        activitiesChosen,
        atividadesMensal,
        atividadesMatricula,
        alimentacaoCalculada,
        custosIniciais,
        matricula,
        desconto,
        mensalidadeCheia,
        mensalidade,
        totalInicial
      };
    }

    function updateCommercial(){
      renderTurmas();
      renderHours();

      const calc = getCommercialCalc();
      document.getElementById('selectedLabel').textContent = `${calc.turma.nome} · ${selectedHour}h`;
      document.getElementById('total').textContent = money.format(calc.mensalidade);

      const extraHtml = calc.extraLines.length
        ? calc.extraLines.map(line => `
          <div class="line positive"><span>${line.nome}</span><strong>+ ${money.format(line.valor)}</strong></div>
        `).join('')
        : `<div class="line"><span>Extras da escola</span><strong>Não há</strong></div>`;

      const atividadeHtml = calc.activitiesChosen.length
        ? calc.activitiesChosen.map(line => `
          <div class="line positive"><span>${line.nome}</span><strong>+ ${money.format(line.mensal)}</s
---
lidade + matricula + atividadesMatricula + custosIniciais;

      return {
        turma,
        base,
        extraTotal,
        extraLines,
        activitiesChosen,
        atividadesMensal,
        atividadesMatricula,
        alimentacaoCalculada,
        custosIniciais,
        matricula,
        desconto,
        mensalidadeCheia,
        mensalidade,
        totalInicial
      };
    }

    function updateCommercial(){
      renderTurmas();
      renderHours();

      const calc = getCommercialCalc();
      document.getElementById('selectedLabel').textContent = `${calc.turma.nome} · ${selectedHour}h`;
      document.getElementById('total').textContent = money.format(calc.mensalidade);

      const extraHtml = calc.extraLines.length
        ? calc.extraLines.map(line => `
          <div class="line positive"><span>${line.nome}</span><strong>+ ${money.format(line.valor)}</strong></div>
        `).join('')
        : `<div class="line"><span>Extras da escola</span><strong>Não há</strong></div>`;

      const atividadeHtml = calc.activitiesChosen.length
        ? calc.activitiesChosen.map(line => `
          <div class="line positive"><span>${line.nome}</span><strong>+ ${money.format(line.mensal)}</strong></div>
        `).jo
---
,
        mensalidade,
        totalInicial
      };
    }

    function updateCommercial(){
      renderTurmas();
      renderHours();

      const calc = getCommercialCalc();
      document.getElementById('selectedLabel').textContent = `${calc.turma.nome} · ${selectedHour}h`;
      document.getElementById('total').textContent = money.format(calc.mensalidade);

      const extraHtml = calc.extraLines.length
        ? calc.extraLines.map(line => `
          <div class="line positive"><span>${line.nome}</span><strong>+ ${money.format(line.valor)}</strong></div>
        `).join('')
        : `<div class="line"><span>Extras da escola</span><strong>Não há</strong></div>`;

      const atividadeHtml = calc.activitiesChosen.length
        ? calc.activitiesChosen.map(line => `
          <div class="line positive"><span>${line.nome}</span><strong>+ ${money.format(line.mensal)}</strong></div>
        `).join('')
        : `<div class="line"><span>Atividades extras</span><strong>Não há</strong></div>`;

      document.getElementById('summary').innerHTML = `
        <div class="line"><span>Turma</span><strong>${calc.turma.nome}</strong></div>
        <div class="line"><span>Idade</span><strong>${calc.turma.idade}</strong></div>
        <div 
---

### pagamento
="grid">
        <div>
          <div class="grid-2">
            <div class="panel">
              <div class="section-title">
                <h2>Custos fixos mensais</h2>
                <span class="pill">Escola</span>
              </div>

              <div class="fields">
                <div class="field">
                  <label>Folha de pagamento</label>
                  <input data-cost="folha" type="number" value="28000" min="0" step="100">
                </div>
                <div class="field">
                  <label>Impostos / encargos</label>
                  <input data-cost="impostos" type="number" value="6500" min="0" step="100">
                </div>
                <div class="field">
                  <label>Aluguel / imóvel</label>
                  <input data-cost="aluguel" type="number" value="7000" min="0" step="100">
                </div>
                <div class="field">
                  <label>Água, energia e internet</label>
                  <input data-cost="utilidades" type="number" value="3200" min="0" step="100">
                </div>
                <div class="field">
                  <label>Limpeza e manutenção</label>
                  <input data-cost="manutencao" type="number
---

### pago

### vencimento

### multa

### juros

### desconto
>
            <h2>Resumo financeiro</h2>
            <span class="pill">Atual</span>
          </div>
          <div class="dash-kpi-list">
            <div class="dash-kpi">
              <small>Mensalidade cheia</small>
              <strong id="dashFull">R$ 0,00</strong>
            </div>
            <div class="dash-kpi">
              <small>Desconto aplicado</small>
              <strong id="dashDiscount">R$ 0,00</strong>
            </div>
            <div class="dash-kpi">
              <small>Total inicial</small>
              <strong id="dashInitial">R$ 0,00</strong>
            </div>
            <div class="dash-kpi">
              <small>Atividades extras</small>
              <strong id="dashActivities">R$ 0,00</strong>
            </div>
          </div>
        </div>

        <div class="panel dash-panel">
          <div class="section-title">
            <h2>Composição</h2>
            <span class="pill">Detalhes</span>
          </div>
          <div class="dash-donut-wrap">
            <div class="dash-donut" id="dashDonut"></div>
            <div class="dash-legend" id="dashLegend"></div>
          </div>
        </div>

        <div class="panel dash-panel">
          <div class="section-title">
           
---
lue="12.5" min="0" step=".5">
            </div>
          </div>


          <div class="fields">
            <div class="field">
              <label for="matricula">Taxa de matrícula</label>
              <input id="matricula" type="number" value="0" min="0" step="10">
            </div>
            <div class="field">
              <label for="desconto">Desconto mensal</label>
              <input id="desconto" type="number" value="0" min="0" step="10">
            </div>
          </div>

          <div class="section-title" style="margin-top:24px">
            <h2>Material e taxas iniciais</h2>
            <span class="pill">Entrada</span>
          </div>

          <div class="fields">
            <div class="field">
              <label for="materialInicial">Material pedagógico inicial</label>
              <input id="materialInicial" type="number" value="180" min="0" step="10">
            </div>
            <div class="field">
              <label for="uniformeInicial">Uniforme / kit inicial</label>
              <input id="uniformeInicial" type="number" value="0" min="0" step="10">
            </div>
            <div class="field">
              <label for="agendaApp">Agenda / app / cadastro</label>
              <inpu
---
 min="0" step=".5">
            </div>
          </div>


          <div class="fields">
            <div class="field">
              <label for="matricula">Taxa de matrícula</label>
              <input id="matricula" type="number" value="0" min="0" step="10">
            </div>
            <div class="field">
              <label for="desconto">Desconto mensal</label>
              <input id="desconto" type="number" value="0" min="0" step="10">
            </div>
          </div>

          <div class="section-title" style="margin-top:24px">
            <h2>Material e taxas iniciais</h2>
            <span class="pill">Entrada</span>
          </div>

          <div class="fields">
            <div class="field">
              <label for="materialInicial">Material pedagógico inicial</label>
              <input id="materialInicial" type="number" value="180" min="0" step="10">
            </div>
            <div class="field">
              <label for="uniformeInicial">Uniforme / kit inicial</label>
              <input id="uniformeInicial" type="number" value="0" min="0" step="10">
            </div>
            <div class="field">
              <label for="agendaApp">Agenda / app / cadastro</label>
              <input id="agen
---
</div>


          <div class="fields">
            <div class="field">
              <label for="matricula">Taxa de matrícula</label>
              <input id="matricula" type="number" value="0" min="0" step="10">
            </div>
            <div class="field">
              <label for="desconto">Desconto mensal</label>
              <input id="desconto" type="number" value="0" min="0" step="10">
            </div>
          </div>

          <div class="section-title" style="margin-top:24px">
            <h2>Material e taxas iniciais</h2>
            <span class="pill">Entrada</span>
          </div>

          <div class="fields">
            <div class="field">
              <label for="materialInicial">Material pedagógico inicial</label>
              <input id="materialInicial" type="number" value="180" min="0" step="10">
            </div>
            <div class="field">
              <label for="uniformeInicial">Uniforme / kit inicial</label>
              <input id="uniformeInicial" type="number" value="0" min="0" step="10">
            </div>
            <div class="field">
              <label for="agendaApp">Agenda / app / cadastro</label>
              <input id="agendaApp" type="number" value="50" min="0" step="10"
---
iv class="grid">
        <div>
          <div class="panel">
            <div class="section-title">
              <h2>Capacidade, alunos e bolsas por turma</h2>
              <span class="pill">Precisão por sala</span>
            </div>
            <p class="helper-text">
              Informe a capacidade, alunos matriculados, alunos pagantes e desconto médio real. Isso melhora a leitura de ocupação, ticket médio e risco de defasagem.
            </p>
            <div id="classPrecisionInputs" class="precision-list"></div>
          </div>

          <div class="panel" style="margin-top:18px">
            <div class="section-title">
              <h2>Equipe por turma</h2>
              <span class="pill">Custo pedagógico</span>
            </div>
            <p class="helper-text">
              Aqui você dimensiona professor, auxiliar e encargos por turma. O berçário normalmente tem custo por aluno maior pela proporção adulto/criança.
            </p>
            <div id="staffPrecisionInputs" class="precision-list"></div>
          </div>

          <div class="panel" style="margin-top:18px">
            <div class="section-title">
              <h2>Meta de lucro e captação</h2>
              <span class="pill">Quantos alunos
---
v>
    </section>


    <section id="alertas" class="view">
      <div class="grid">
        <div class="panel">
          <div class="section-title">
            <h2>Alertas inteligentes</h2>
            <span class="pill">Leitura automática</span>
          </div>
          <p class="helper-text">
            O sistema cruza mensalidade, custos, descontos, ocupação, equipe, alimentação, preços praticados e ponto de equilíbrio para destacar riscos e oportunidades.
          </p>
          <div class="smart-alert-list" id="smartAlertList"></div>
        </div>

        <aside class="panel result-card">
          <div class="section-title">
            <h2>Resumo de risco</h2>
            <span class="pill">Semáforo</span>
          </div>
          <div class="total-box" id="riskBox">
            <small>Nível geral</small>
            <div class="total" id="riskLevel">Calculando</div>
          </div>
          <div class="summary" id="riskSummary"></div>
          <div class="actions">
            <button class="btn" id="copyAlertsBtn">Copiar alertas</button>
            <button class="btn secondary" id="goPrecisionFromAlerts">Ver precisão gerencial</button>
          </div>
        </aside>
      </div>
    </section>

    <sect
---
acc + a.mensal, 0);
      const atividadesMatricula = activitiesChosen.reduce((acc, a) => acc + a.matricula, 0);
      const alimentacaoCalculada = document.getElementById('alimentacao')?.checked ? getNutritionCost(selectedTurma, selectedHour) : 0;
      const custosIniciais = getInitialCosts();

      const matricula = n('matricula');
      const desconto = n('desconto');
      const mensalidadeCheia = Math.max(0, base + extraTotal + atividadesMensal);
      const mensalidade = Math.max(0, mensalidadeCheia - desconto);
      const totalInicial = mensalidade + matricula + atividadesMatricula + custosIniciais;

      return {
        turma,
        base,
        extraTotal,
        extraLines,
        activitiesChosen,
        atividadesMensal,
        atividadesMatricula,
        alimentacaoCalculada,
        custosIniciais,
        matricula,
        desconto,
        mensalidadeCheia,
        mensalidade,
        totalInicial
      };
    }

    function updateCommercial(){
      renderTurmas();
      renderHours();

      const calc = getCommercialCalc();
      document.getElementById('selectedLabel').textContent = `${calc.turma.nome} · ${selectedHour}h`;
      document.getElementById('total').textContent = money.format(calc.me
---
, 0);
      const atividadesMatricula = activitiesChosen.reduce((acc, a) => acc + a.matricula, 0);
      const alimentacaoCalculada = document.getElementById('alimentacao')?.checked ? getNutritionCost(selectedTurma, selectedHour) : 0;
      const custosIniciais = getInitialCosts();

      const matricula = n('matricula');
      const desconto = n('desconto');
      const mensalidadeCheia = Math.max(0, base + extraTotal + atividadesMensal);
      const mensalidade = Math.max(0, mensalidadeCheia - desconto);
      const totalInicial = mensalidade + matricula + atividadesMatricula + custosIniciais;

      return {
        turma,
        base,
        extraTotal,
        extraLines,
        activitiesChosen,
        atividadesMensal,
        atividadesMatricula,
        alimentacaoCalculada,
        custosIniciais,
        matricula,
        desconto,
        mensalidadeCheia,
        mensalidade,
        totalInicial
      };
    }

    function updateCommercial(){
      renderTurmas();
      renderHours();

      const calc = getCommercialCalc();
      document.getElementById('selectedLabel').textContent = `${calc.turma.nome} · ${selectedHour}h`;
      document.getElementById('total').textContent = money.format(calc.mensalidade);

 
---
ntById('alimentacao')?.checked ? getNutritionCost(selectedTurma, selectedHour) : 0;
      const custosIniciais = getInitialCosts();

      const matricula = n('matricula');
      const desconto = n('desconto');
      const mensalidadeCheia = Math.max(0, base + extraTotal + atividadesMensal);
      const mensalidade = Math.max(0, mensalidadeCheia - desconto);
      const totalInicial = mensalidade + matricula + atividadesMatricula + custosIniciais;

      return {
        turma,
        base,
        extraTotal,
        extraLines,
        activitiesChosen,
        atividadesMensal,
        atividadesMatricula,
        alimentacaoCalculada,
        custosIniciais,
        matricula,
        desconto,
        mensalidadeCheia,
        mensalidade,
        totalInicial
      };
    }

    function updateCommercial(){
      renderTurmas();
      renderHours();

      const calc = getCommercialCalc();
      document.getElementById('selectedLabel').textContent = `${calc.turma.nome} · ${selectedHour}h`;
      document.getElementById('total').textContent = money.format(calc.mensalidade);

      const extraHtml = calc.extraLines.length
        ? calc.extraLines.map(line => `
          <div class="line positive"><span>${line.nome}</span><st
---
 desconto);
      const totalInicial = mensalidade + matricula + atividadesMatricula + custosIniciais;

      return {
        turma,
        base,
        extraTotal,
        extraLines,
        activitiesChosen,
        atividadesMensal,
        atividadesMatricula,
        alimentacaoCalculada,
        custosIniciais,
        matricula,
        desconto,
        mensalidadeCheia,
        mensalidade,
        totalInicial
      };
    }

    function updateCommercial(){
      renderTurmas();
      renderHours();

      const calc = getCommercialCalc();
      document.getElementById('selectedLabel').textContent = `${calc.turma.nome} · ${selectedHour}h`;
      document.getElementById('total').textContent = money.format(calc.mensalidade);

      const extraHtml = calc.extraLines.length
        ? calc.extraLines.map(line => `
          <div class="line positive"><span>${line.nome}</span><strong>+ ${money.format(line.valor)}</strong></div>
        `).join('')
        : `<div class="line"><span>Extras da escola</span><strong>Não há</strong></div>`;

      const atividadeHtml = calc.activitiesChosen.length
        ? calc.activitiesChosen.map(line => `
          <div class="line positive"><span>${line.nome}</span><strong>+ ${money.forma
---
e)}</strong></div>
        <div class="line"><span>Alimentação calculada</span><strong>${money.format(calc.alimentacaoCalculada)}</strong></div>
        ${extraHtml}
        ${atividadeHtml}
        <div class="line"><span>Mensalidade cheia</span><strong>${money.format(calc.mensalidadeCheia)}</strong></div>
        <div class="line negative"><span>Desconto mensal</span><strong>- ${money.format(calc.desconto)}</strong></div>
        <div class="line"><span>Matrícula escolar</span><strong>${money.format(calc.matricula)}</strong></div>
        <div class="line"><span>Matrícula das atividades</span><strong>${money.format(calc.atividadesMatricula)}</strong></div>
        <div class="line"><span>Material/taxas iniciais</span><strong>${money.format(calc.custosIniciais)}</strong></div>
        <div class="line final"><span>Total inicial estimado</span><strong>${money.format(calc.totalInicial)}</strong></div>
      `;

      const extrasText = calc.extraLines.length
        ? calc.extraLines.map(x => x.nome.toLowerCase()).join(', ')
        : 'sem extras escolares adicionais';

      const atividadesText = calc.activitiesChosen.length
        ? calc.activitiesChosen.map(x => x.nome.toLowerCase()).join(', ')
        : 'sem atividades extras
---
limentação calculada</span><strong>${money.format(calc.alimentacaoCalculada)}</strong></div>
        ${extraHtml}
        ${atividadeHtml}
        <div class="line"><span>Mensalidade cheia</span><strong>${money.format(calc.mensalidadeCheia)}</strong></div>
        <div class="line negative"><span>Desconto mensal</span><strong>- ${money.format(calc.desconto)}</strong></div>
        <div class="line"><span>Matrícula escolar</span><strong>${money.format(calc.matricula)}</strong></div>
        <div class="line"><span>Matrícula das atividades</span><strong>${money.format(calc.atividadesMatricula)}</strong></div>
        <div class="line"><span>Material/taxas iniciais</span><strong>${money.format(calc.custosIniciais)}</strong></div>
        <div class="line final"><span>Total inicial estimado</span><strong>${money.format(calc.totalInicial)}</strong></div>
      `;

      const extrasText = calc.extraLines.length
        ? calc.extraLines.map(x => x.nome.toLowerCase()).join(', ')
        : 'sem extras escolares adicionais';

      const atividadesText = calc.activitiesChosen.length
        ? calc.activitiesChosen.map(x => x.nome.toLowerCase()).join(', ')
        : 'sem atividades extras';

      document.getElementById('proposal').innerH
---
imulação de mensalidade:

Turma: ${calc.turma.nome}
Faixa etária: ${calc.turma.idade}
Carga horária: ${selectedHour}h
Valor base: ${money.format(calc.base)}
Alimentação calculada: ${money.format(calc.alimentacaoCalculada)}
Extras da escola: ${extrasText}
Atividades extras: ${atividadesText}
Mensalidade cheia: ${money.format(calc.mensalidadeCheia)}
Desconto mensal: ${money.format(calc.desconto)}
Mensalidade final: ${money.format(calc.mensalidade)}
Matrícula escolar: ${money.format(calc.matricula)}
Matrícula das atividades: ${money.format(calc.atividadesMatricula)}
Material/taxas iniciais: ${money.format(calc.custosIniciais)}
Total inicial estimado: ${money.format(calc.totalInicial)}

Estamos à disposição para apresentar a escola e esclarecer qualquer dúvida.`;

      navigator.clipboard.writeText(text).then(() => showToast('Proposta copiada!'));
    }

    function copyManagement(){
      const c = getFormationCalc();
      const text = `Resumo gerencial da formação de mensalidade:

Custos fixos mensais: ${money.format(c.fixed)}
Custo variável por aluno: ${money.format(c.variable)}
Alunos matriculados: ${c.alunos}
Alunos pagantes estimados: ${c.pagantes}
Inadimplência estimada: ${(c.inad*100).toFixed(1)}%
Reserva de segurança: ${(c
---
lc.turma.nome}
Faixa etária: ${calc.turma.idade}
Carga horária: ${selectedHour}h
Valor base: ${money.format(calc.base)}
Alimentação calculada: ${money.format(calc.alimentacaoCalculada)}
Extras da escola: ${extrasText}
Atividades extras: ${atividadesText}
Mensalidade cheia: ${money.format(calc.mensalidadeCheia)}
Desconto mensal: ${money.format(calc.desconto)}
Mensalidade final: ${money.format(calc.mensalidade)}
Matrícula escolar: ${money.format(calc.matricula)}
Matrícula das atividades: ${money.format(calc.atividadesMatricula)}
Material/taxas iniciais: ${money.format(calc.custosIniciais)}
Total inicial estimado: ${money.format(calc.totalInicial)}

Estamos à disposição para apresentar a escola e esclarecer qualquer dúvida.`;

      navigator.clipboard.writeText(text).then(() => showToast('Proposta copiada!'));
    }

    function copyManagement(){
      const c = getFormationCalc();
      const text = `Resumo gerencial da formação de mensalidade:

Custos fixos mensais: ${money.format(c.fixed)}
Custo variável por aluno: ${money.format(c.variable)}
Alunos matriculados: ${c.alunos}
Alunos pagantes estimados: ${c.pagantes}
Inadimplência estimada: ${(c.inad*100).toFixed(1)}%
Reserva de segurança: ${(c.reserva*100).toFixed(1)}%
Margem des
---
      selectedHour = 6;
      document.getElementById('alimentacao').checked = true;
      document.getElementById('material').checked = false;
      document.getElementById('transporte').checked = false;
      document.getElementById('estendido').checked = false;
      document.getElementById('matricula').value = 0;
      document.getElementById('desconto').value = 0;
      document.getElementById('materialInicial').value = 180;
      document.getElementById('uniformeInicial').value = 0;
      document.getElementById('agendaApp').value = 50;
      document.getElementById('eventosProjetos').value = 0;
      customActivities = [];
      renderCustomActivities();
      document.getElementById('diasLetivos').value = 22;
      document.getElementById('alimPerdas').value = 8;
      document.getElementById('alim_bercario').value = 11;
      document.getElementById('alim_maternal1').value = 10;
      document.getElementById('alim_maternal2').value = 10.5;
      document.getElementById('alim_jardim1').value = 12;
      document.getElementById('alim_jardim2').value = 12.5;

      ['natacao','judo','bale','teatro'].forEach(id => {
        document.getElementById(`act_${id}`).checked = false;
      });

      document.getElementById('act_nat
---
oney.format(calc.extraTotal)}</td></tr>
                <tr><td>Atividades extras</td><td>${atividadesText}</td></tr>
                <tr><td>Total mensal das atividades</td><td>${money.format(calc.atividadesMensal)}</td></tr>
                <tr><td>Mensalidade cheia</td><td>${money.format(calc.mensalidadeCheia)}</td></tr>
                <tr><td>Desconto mensal</td><td>${money.format(calc.desconto)}</td></tr>
                <tr><td>Matrícula escolar</td><td>${money.format(calc.matricula)}</td></tr>
                <tr><td>Matrícula das atividades</td><td>${money.format(calc.atividadesMatricula)}</td></tr>
                <tr><td>Material/taxas iniciais</td><td>${money.format(calc.custosIniciais)}</td></tr>
                <tr><td><strong>Total inicial estimado</strong></td><td><strong>${money.format(calc.totalInicial)}</strong></td></tr>
              </tbody>
            </table>

            <div class="print-note">${info.obs}</div>

            <div class="signature-area">
              <div class="signature-line">Responsável pela escola</div>
              <div class="signature-line">Responsável pelo aluno</div>
            </div>
          </div>
        </article>
      `;
    }




    function buildAlertsReport(){
     
---
            <tr><td>Atividades extras</td><td>${atividadesText}</td></tr>
                <tr><td>Total mensal das atividades</td><td>${money.format(calc.atividadesMensal)}</td></tr>
                <tr><td>Mensalidade cheia</td><td>${money.format(calc.mensalidadeCheia)}</td></tr>
                <tr><td>Desconto mensal</td><td>${money.format(calc.desconto)}</td></tr>
                <tr><td>Matrícula escolar</td><td>${money.format(calc.matricula)}</td></tr>
                <tr><td>Matrícula das atividades</td><td>${money.format(calc.atividadesMatricula)}</td></tr>
                <tr><td>Material/taxas iniciais</td><td>${money.format(calc.custosIniciais)}</td></tr>
                <tr><td><strong>Total inicial estimado</strong></td><td><strong>${money.format(calc.totalInicial)}</strong></td></tr>
              </tbody>
            </table>

            <div class="print-note">${info.obs}</div>

            <div class="signature-area">
              <div class="signature-line">Responsável pela escola</div>
              <div class="signature-line">Responsável pelo aluno</div>
            </div>
          </div>
        </article>
      `;
    }




    function buildAlertsReport(){
      let alerts = [];
      try{ alerts = buildS
---
ia: ${calc.turma.idade}
Carga horária: ${selectedHour}h
Valor base: ${money.format(calc.base)}
Extras escolares: ${extrasText}
Total de extras escolares: ${money.format(calc.extraTotal)}
Atividades extras: ${atividadesText}
Total mensal das atividades: ${money.format(calc.atividadesMensal)}
Mensalidade cheia: ${money.format(calc.mensalidadeCheia)}
Desconto mensal: ${money.format(calc.desconto)}
Mensalidade final: ${money.format(calc.mensalidade)}
Matrícula escolar: ${money.format(calc.matricula)}
Matrícula das atividades: ${money.format(calc.atividadesMatricula)}
Material/taxas iniciais: ${money.format(calc.custosIniciais)}
Total inicial estimado: ${money.format(calc.totalInicial)}

Observação: ${info.obs}`;
      navigator.clipboard.writeText(text).then(() => showToast('Orçamento copiado!'));
    }



    function pct(value, ref){
      if(!ref || ref <= 0) return 0;
      return Math.max(0, Math.min(100, (value / ref) * 100));
    }


    const currentPriceDefaults = {
      bercario:{6:1180,8:1380,10:1580,12:1780},
      maternal1:{6:1050,8:1250,10:1450,12:1650},
      maternal2:{6:980,8:1180,10:1380,12:1580},
      jardim1:{6:900,8:1080,10:1280,12:1480},
      jardim2:{6:880,8:1050,10:1250,12:1450}
    };

    function renderC
---
: ${selectedHour}h
Valor base: ${money.format(calc.base)}
Extras escolares: ${extrasText}
Total de extras escolares: ${money.format(calc.extraTotal)}
Atividades extras: ${atividadesText}
Total mensal das atividades: ${money.format(calc.atividadesMensal)}
Mensalidade cheia: ${money.format(calc.mensalidadeCheia)}
Desconto mensal: ${money.format(calc.desconto)}
Mensalidade final: ${money.format(calc.mensalidade)}
Matrícula escolar: ${money.format(calc.matricula)}
Matrícula das atividades: ${money.format(calc.atividadesMatricula)}
Material/taxas iniciais: ${money.format(calc.custosIniciais)}
Total inicial estimado: ${money.format(calc.totalInicial)}

Observação: ${info.obs}`;
      navigator.clipboard.writeText(text).then(() => showToast('Orçamento copiado!'));
    }



    function pct(value, ref){
      if(!ref || ref <= 0) return 0;
      return Math.max(0, Math.min(100, (value / ref) * 100));
    }


    const currentPriceDefaults = {
      bercario:{6:1180,8:1380,10:1580,12:1780},
      maternal1:{6:1050,8:1250,10:1450,12:1650},
      maternal2:{6:980,8:1180,10:1380,12:1580},
      jardim1:{6:900,8:1080,10:1280,12:1480},
      jardim2:{6:880,8:1050,10:1250,12:1450}
    };

    function renderCurrentPriceInputs(){
      const wrap
---
e="number" value="${d.enrolled}" min="0" step="1">
              </div>
              <div class="precision-field">
                <label>Pagantes</label>
                <input data-precision="${key}_payers" type="number" value="${d.payers}" min="0" step="1">
              </div>
              <div class="precision-field">
                <label>Desconto médio %</label>
                <input data-precision="${key}_discount" type="number" value="${d.discount}" min="0" max="100" step=".5">
              </div>
            </div>
          </div>
        `;
      }).join('');

      staffWrap.innerHTML = Object.entries(commercialTable).map(([key, turma]) => {
        const d = precisionDefaults[key];
        return `
          <div class="precision-card">
            <div class="precision-head">
              <b>${turma.nome}</b>
              <small>Equipe direta da turma</small>
            </div>
            <div class="precision-grid staff">
              <div class="precision-field">
                <label>Professores</label>
                <input data-precision="${key}_teacher" type="number" value="${d.teacher}" min="0" step=".5">
              </div>
              <div class="precision-field">
                <label>Auxili
---
/span><strong>${money.format(goalData.ticket)}</strong></div>
      `;

      let title, text;
      if(data.monthlyResult < 0){
        title = 'Atenção: operação no vermelho';
        text = 'Com os preços atuais, ocupação e custos informados, o resultado mensal estimado fica negativo. Priorize reajuste gradual, aumento de ocupação e revisão dos descontos.';
      }else if(data.occupancy < .75){
        title = 'Ocupação ainda pressiona o preço';
        text = 'A escola está positiva ou próxima disso, mas a ocupação abaixo de 75% pode deixar algumas turmas caras por aluno. Trabalhe captação e rematrícula.';
      }else{
        title = 'Gestão em faixa saudável';
        text = 'Os dados indicam uma operação mais equilibrada. Use o comparativo para corrigir pontos específicos de defasagem por turma e horário.';
      }
      document.getElementById('precisionInsight').innerHTML = `<strong>${title}</strong>${text}`;

      document.getElementById('scenarioConservative').textContent = money.format(estimateScenario(data, -.10, .03));
      document.getElementById('scenarioRealistic').textContent = money.format(data.monthlyResult);
      document.getElementById('scenarioIdeal').textContent = money.format(estimateScenario(data, .10,
---
monthlyResult)}
Meta de lucro mensal: ${money.format(goalData.goal)}
Alunos adicionais para empatar: ${goalData.studentsToBreakEven}
Alunos adicionais para atingir a meta: ${goalData.studentsToProfit}
Vagas disponíveis: ${goalData.openSeats}

Leitura:
${d.monthlyResult < 0 ? 'Operação estimada no vermelho. Priorizar reajuste, ocupação e revisão de descontos.' : d.occupancy < .75 ? 'Ocupação abaixo do ideal. Trabalhar captação e rematrícula.' : 'Operação em faixa mais saudável. Corrigir defasagens pontuais.'}`;

      navigator.clipboard.writeText(text).then(() => showToast('Diagnóstico gerencial copiado!'));
    }

    function resetPrecision(){
      Object.entries(precisionDefaults).forEach(([key, d]) => {
        Object.entries(d).forEach(([field, value]) => {
          const el = document.querySelector(`[data-precision="${key}_${field}"]`);
          if(el) el.value = value;
        });
      });
      const adj = document.getElementById('adjustPercent');
      const mode = document.getElementById('adjustMode');
      const profit = document.getElementById('profitGoal');
      const ticketMode = document.getElementById('goalTicketMode');
      if(adj) adj.value = 8;
      if(mode) mode.value = 'all';
      if(profit) profit.va
---
egura){
        alerts.push({type:'warn', title:'Mensalidade abaixo da segura', text:'A proposta cobre o mínimo, mas não protege suficientemente contra inadimplência e reserva.'});
      }else{
        alerts.push({type:'ok', title:'Mensalidade comercial saudável', text:'A simulação atual está acima da referência segura.'});
      }

      if(calc.desconto > 0){
        const pct = calc.mensalidadeCheia > 0 ? (calc.desconto / calc.mensalidadeCheia) * 100 : 0;
        alerts.push({type:pct > 12 ? 'warn' : 'ok', title:'Desconto aplicado', text:`O desconto representa ${pct.toFixed(1)}% da mensalidade cheia.`});
      }

      if(calc.alimentacaoCalculada > 350){
        alerts.push({type:'warn', title:'Alimentação elevada', text:'O custo de alimentação calculado está alto. Revise custo diário, perdas ou composição do horário.'});
      }

      if(precision){
        if(precision.occupancy < .70){
          alerts.push({type:'danger', title:'Ocupação geral baixa', text:`Ocupação estimada em ${(precision.occupancy*100).toFixed(1)}%. Isso pressiona muito o custo por aluno.`});
        }else if(precision.occupancy < .80){
          alerts.push({type:'warn', title:'Ocupação em atenção', text:`Ocupação estimada em ${(precision.occupancy*1
---
a segura', text:'A proposta cobre o mínimo, mas não protege suficientemente contra inadimplência e reserva.'});
      }else{
        alerts.push({type:'ok', title:'Mensalidade comercial saudável', text:'A simulação atual está acima da referência segura.'});
      }

      if(calc.desconto > 0){
        const pct = calc.mensalidadeCheia > 0 ? (calc.desconto / calc.mensalidadeCheia) * 100 : 0;
        alerts.push({type:pct > 12 ? 'warn' : 'ok', title:'Desconto aplicado', text:`O desconto representa ${pct.toFixed(1)}% da mensalidade cheia.`});
      }

      if(calc.alimentacaoCalculada > 350){
        alerts.push({type:'warn', title:'Alimentação elevada', text:'O custo de alimentação calculado está alto. Revise custo diário, perdas ou composição do horário.'});
      }

      if(precision){
        if(precision.occupancy < .70){
          alerts.push({type:'danger', title:'Ocupação geral baixa', text:`Ocupação estimada em ${(precision.occupancy*100).toFixed(1)}%. Isso pressiona muito o custo por aluno.`});
        }else if(precision.occupancy < .80){
          alerts.push({type:'warn', title:'Ocupação em atenção', text:`Ocupação estimada em ${(precision.occupancy*100).toFixed(1)}%. Há espaço para melhorar receita sem aumentar estrut
---
va.'});
      }else{
        alerts.push({type:'ok', title:'Mensalidade comercial saudável', text:'A simulação atual está acima da referência segura.'});
      }

      if(calc.desconto > 0){
        const pct = calc.mensalidadeCheia > 0 ? (calc.desconto / calc.mensalidadeCheia) * 100 : 0;
        alerts.push({type:pct > 12 ? 'warn' : 'ok', title:'Desconto aplicado', text:`O desconto representa ${pct.toFixed(1)}% da mensalidade cheia.`});
      }

      if(calc.alimentacaoCalculada > 350){
        alerts.push({type:'warn', title:'Alimentação elevada', text:'O custo de alimentação calculado está alto. Revise custo diário, perdas ou composição do horário.'});
      }

      if(precision){
        if(precision.occupancy < .70){
          alerts.push({type:'danger', title:'Ocupação geral baixa', text:`Ocupação estimada em ${(precision.occupancy*100).toFixed(1)}%. Isso pressiona muito o custo por aluno.`});
        }else if(precision.occupancy < .80){
          alerts.push({type:'warn', title:'Ocupação em atenção', text:`Ocupação estimada em ${(precision.occupancy*100).toFixed(1)}%. Há espaço para melhorar receita sem aumentar estrutura.`});
        }else{
          alerts.push({type:'ok', title:'Ocupação saudável', text:`Ocupação esti
---
 alerts.push({type:'ok', title:'Mensalidade comercial saudável', text:'A simulação atual está acima da referência segura.'});
      }

      if(calc.desconto > 0){
        const pct = calc.mensalidadeCheia > 0 ? (calc.desconto / calc.mensalidadeCheia) * 100 : 0;
        alerts.push({type:pct > 12 ? 'warn' : 'ok', title:'Desconto aplicado', text:`O desconto representa ${pct.toFixed(1)}% da mensalidade cheia.`});
      }

      if(calc.alimentacaoCalculada > 350){
        alerts.push({type:'warn', title:'Alimentação elevada', text:'O custo de alimentação calculado está alto. Revise custo diário, perdas ou composição do horário.'});
      }

      if(precision){
        if(precision.occupancy < .70){
          alerts.push({type:'danger', title:'Ocupação geral baixa', text:`Ocupação estimada em ${(precision.occupancy*100).toFixed(1)}%. Isso pressiona muito o custo por aluno.`});
        }else if(precision.occupancy < .80){
          alerts.push({type:'warn', title:'Ocupação em atenção', text:`Ocupação estimada em ${(precision.occupancy*100).toFixed(1)}%. Há espaço para melhorar receita sem aumentar estrutura.`});
        }else{
          alerts.push({type:'ok', title:'Ocupação saudável', text:`Ocupação estimada em ${(precision.occupan
---
urn;
      }

      dashMonthly.textContent = money.format(calc.mensalidade);
      document.getElementById('dashSubtitle').textContent = `${calc.turma.nome} · ${selectedHour}h`;
      document.getElementById('dashFull').textContent = money.format(calc.mensalidadeCheia);
      document.getElementById('dashDiscount').textContent = money.format(calc.desconto);
      document.getElementById('dashInitial').textContent = money.format(calc.totalInicial);
      document.getElementById('dashActivities').textContent = money.format(calc.atividadesMensal);

      document.getElementById('dashMinText').textContent = `Referência: ${money.format(form.minima)}`;
      document.getElementById('dashSafeText').textContent = `Referência: ${money.format(form.segura)}`;
      document.getElementById('dashIdealText').textContent = `Referência: ${money.format(form.ideal)}`;

      document.getElementById('dashMinBar').style.width = pct(calc.mensalidade, form.minima) + '%';
      document.getElementById('dashSafeBar').style.width = pct(calc.mensalidade, form.segura) + '%';
      document.getElementById('dashIdealBar').style.width = pct(calc.mensalidade, form.ideal) + '%';

      const status = document.getElementById('dashStatus');
      status.classList
---
r');

      let statusText = '';
      let adviceTitle = '';
      let adviceText = '';

      if(calc.mensalidade < form.minima){
        status.classList.add('danger');
        statusText = 'Abaixo do mínimo';
        adviceTitle = 'Atenção: risco de prejuízo';
        adviceText = 'A mensalidade simulada está abaixo da referência mínima. Reduza descontos, revise extras ou ajuste a mensalidade base antes de apresentar a proposta.';
      }else if(calc.mensalidade < form.segura){
        status.classList.add('warn');
        statusText = 'Aceitável com cuidado';
        adviceTitle = 'Proposta sensível';
        adviceText = 'A mensalidade cobre o mínimo, mas ainda não alcança a referência segura. Use apenas para casos estratégicos ou com aprovação administrativa.';
      }else if(calc.mensalidade < form.ideal){
        status.classList.add('ok');
        statusText = 'Saudável';
        adviceTitle = 'Boa proposta';
        adviceText = 'A mensalidade está acima da referência segura. É uma faixa saudável para atendimento, preservando margem e proteção contra inadimplência.';
      }else{
        status.classList.add('ok');
        statusText = 'Excelente';
        adviceTitle = 'Proposta ideal';
        adviceText = 'A mensalida
---

### supabase

### firebase

### DUPLICATE FUNCTIONS
