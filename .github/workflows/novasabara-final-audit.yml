name: NovaSabara final audit
on:
  push:
    paths:
      - 'novasabara_final_scan.py'
      - '.github/workflows/novasabara-final-audit.yml'
jobs:
  audit:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: python novasabara_final_scan.py
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add novasabara_final_findings.txt
          git diff --cached --quiet || git commit -m 'Generate NovaSabara final audit findings'
          git push
