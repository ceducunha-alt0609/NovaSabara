name: NovaSabara audit
on:
  push:
    paths:
      - 'novasabara_audit.py'
      - '.github/workflows/novasabara-audit.yml'
jobs:
  audit:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: python novasabara_audit.py
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add novasabara_audit_report.txt
          git diff --cached --quiet || git commit -m 'Generate NovaSabara audit report'
          git push
